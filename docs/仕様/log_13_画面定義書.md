# 画面定義書 決定ログ

- [画面定義書](./13_画面定義書.md) の「なぜそうしたか」を日付順に積む。本文は現在の仕様だけを持つ

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-09-01 | 上限超えのエラーにも文言を置き（「30 文字以内で入力してください」）、**起きない想定のエラーにも文言を持たせる**規則にする | F-003 の実装で、打ち切りがあるので上限超えは UI から届かないと判断し、文言を置かずに確定だけ受け付けない形にした。しかしその経路に届くと**赤枠だけが出て理由が分からない**状態になり、§2 の「エラーは入力欄の下に赤字で出す」とも食い違う。オーナー判断で、今後のエラーも含めて文言を必ず持たせることにした |
| 2026-09-01 | 同梱するフォントは丸ゴシック（Zen Maru Gothic）の Bold 1 ウェイトだけにし、本文は OS 既定に任せる | 日本語フォントは 1 ウェイトで約 3.8MB あり、2 書体を同梱するとアプリの配布サイズが数倍になる。オーナー判断で「見出し・CTA だけ同梱」を選んだ——丸ゴシックは目に付く場所に効き、本文は OS 既定でも日本語として自然に読める。ライセンス（OFL-1.1）は `configPath` で一覧に手足しした |
| 2026-09-01 | 絞り込みチップは選択中にもう一度タップすると個別に解除する。行き先が無い品目の行には「どこでも」タグを出さない | F-005 の実装中に、画面 01 の記述では決まらない 2 点として挙がった（仕様レビューが検出）。前者はチップ UI の一般的な挙動で、片方だけ外すのに「すべて」から選び直す手間が要らない。後者はカテゴリが無いときにタグを出さないのと対称で、行き先が付いているものだけが目立つほうが一覧として読みやすい。**どちらもオーナー判断** |
| 2026-09-01 | シートは開いた時点でキーボードを出さない（02・05 の自動フォーカスをやめ、規則を §2 へ置く） | MVP の動作確認で、オーナーが「＋ を押すとシートとキーボードがほぼ同時に出て、そのあとシートがキーボードのぶん持ち上がる。画面が 2 回動いて違和感が強い」と指摘。**オーナー判断**で自動フォーカスをやめた。打ち始めるのにタップが 1 回増えるが、開くたびに画面が 2 回動くほうが目に付く。**05 の検索欄も同じ動きをする**ので、片方だけ直すと作法が割れる（§2） |
| 2026-09-01 | 地を 1 段にし、アプリバーも `page` で塗る。コンテンツ面 `screen` はトークン表から落とす | MVP の動作確認で、オーナーが「アプリバーと本文で背景色が違うのが気になる」と指摘。調べると `page` と `screen` はどの画面からも直接引かれておらず、**Material の既定で `surface`（= `screen`）を拾ったアプリバーだけが別の面**になっていた。**オーナー判断**で地に揃えた——白いカードが地の上に浮く形はそのままで、画面が 2 枚に割れて見えるのだけが消える。使い道の無くなった `screen` は表から落とした |
| 2026-09-01 | 画面 09 の行は高さを一定にし、長い名前は 1 行に省略する | F-012 の着手時に、09 の記述では決まらなかった。**オーナー判断**。折り返すと行ごとに高さが変わり、ドラッグの落とし先の計算が行の高さの配列を持つ形になる。全文は長押しで開く 09b で読めるので、一覧は揃って見えるほうを採った |
| 2026-09-01 | 画面 07 のバージョンは**画面の下端に固定**する（行の最後ではなく） | F-011 の仕様レビューが「『最下部に』は行の末尾とも画面の下端とも読める」と指摘した。**オーナー判断**。行が少ないうちは下に空きが出るが、行が増えても位置が変わらないほうが、探しに来たときに見つけやすい |
| 2026-09-01 | 画面 05 の「他の行き先から」の行に行き先名を添える | F-010 の着手時に、05 の記述では決まらなかった。**オーナー判断**。このセクションだけは複数の店の品目が混ざるので、名前だけだとタップ後にどこのカゴへ入ったかが分からない。タップ後のスナックバー（「◯◯に追加しました」）だけでは、押す前に選べない |
| 2026-09-01 | 画面 04 の「買い物を終了する」は、チェック済みが 0 件でも押せる。終了時に待ち時間やスピナーは置かない | F-009 の着手時に、どちらも 04 の記述では決まらなかった。**オーナー判断**。前者は「何も買わずに帰る」も買い物の終わり方で、無効化すると ← の離脱確認しか戻る道が無くなるため。後者は旧画面が 500ms 待ってスピナーを出していた（技術改善バックログ T-27）が、確認ダイアログが無くなった 04 では**押した結果が一覧の消失として即見える**ので、待たせる理由がない |
| 2026-09-01 | チップの解除作法は画面 01 だけでなく §2（操作の共通作法）に置く | F-006 で登録シートの選択チップにも同じ作法を入れたところ、仕様レビューが「02 には書かれていない」と指摘した。**同じ操作が画面ごとに違うほうが驚く**ので、規則を上位へ 1 か所にまとめ、01 の記述は参照に変えた |
| 2026-09-05 | アプリバーは**アイコンの的を 28dp にするだけ**。当たり判定とリップルは Material の既定に触らず、添える数の右を 16dp 空ける | FB-03「＋ と ⚙ のタップ領域が狭い」。**当たり判定は最初から 48dp あり Material の最小を満たしていた**ので、広げても効かないと見て切り分けた。**オーナー判断**で「狙って外す」＝的が小さい側。まず判定 56dp を入れたが、`IconButton` は状態レイヤが `size` に追従するため**リップルまで 56dp になり「押した跡が大きすぎる」**となった。判定とリップルを分ける実装（`Box` ＋ `clickable` ＋ `ripple(radius=…)`）も書いたが**管理点が増えるので却下**。48dp に落として一度は確定したが、レビューで **`size` は当たり判定をまったく動かさない**（`minimumInteractiveComponentSize` が別に確保している）ことが判明——つまり 48dp 版の効果もリップルを 40→48dp に太らせるだけだった。**オーナー判断で `size` ごと外し**、既定へ戻した。残った「＋ が件数に吸われる感じ」は実機の計測で原因が出た——`Text` はタップを消費しないが、**件数の右端と ＋ の判定の左端の隙間が 0dp** で密着していた。**アプリバーが 64dp・ボタンが 48dp なので上下に 8dp ずつ反応しない帯が残る**が、埋めるにはリップルを大きくするしかないので許容する |
| 2026-09-05 | 一覧の行に最低高 64dp を持たせ、**4 画面すべて**（01・04・05・09）を同じ高さにする。**末尾の破線の行**（04・09 の「＋ ◯◯」）も同じ値から引いて揃える | FB-06「プールと買い物モードでカードの高さが異なる」。行の高さは中身で決まっていて、2 段の 01 が 64dp、1 段の 04・09 が 48dp、05 は行き先の有無で 48dp と 64dp が混在し、破線の行だけが 56dp だった。**中身の差なので不具合ではない**が、**05 は 1 画面の中で割れている**ので揃える側に振った。前者は**オーナー判断**で「04 だけ 64dp」ではなく全画面に効かせた——高さを決める場所が共通部品の 1 か所で済み、05 の不揃いも一緒に消える。代償は 04 の一覧が伸びること（1 画面に入る行数が 3/4 に）だが、店で押す行としては 64dp のほうが押しやすい。**低いほうへ揃える道は無い**——01 の 2 段目を削るのは表示する情報を減らすことになる。後者は動作確認のあとに**オーナー判断**で足した——揃えないと「行より高い」から「行より低い」に反転するだけで段差は残り、同じ一覧に混ざる以上そこだけ外す理由が無い |
| 2026-09-05 | 04 の離脱確認だけ「やめる」左・「続ける」右にし、**「続ける」を塗りで強調**する。**削除の確認（06・09b）は Material の既定のまま**とし、既定の規則そのものを §2 に置く | FB-12「離脱確認のボタン配置と強調」。**オーナー判断**。**押し間違えたときの損が大きいのは「やめる」側で、いちばん選ばれるのは「続ける」**というのが並べ替えと強調の理由。両ガイドラインに当たった結果（[FB-12](../案件/closed_21_FB台帳.md#fb-12) の裏取り）、**離脱確認は Apple の規則の範囲に収まるが、削除の確認は両ガイドラインとも「キャンセルが左」になる**と分かった。だから**離脱確認だけを例外にし、既定を §2 に明記して例外の在処が 1 か所で分かるようにした**。強調の仕方は**一度は角丸枠に決めたが、実機を見てオーナー判断で塗りに変えた**——枠では弱かった。**画面下部の CTA と同じ面になる**が、離脱確認で最も選ばれるのは「続ける」なので、いちばん強い見せ方でよいと判断した。**角丸枠を試した時点で色が灰色に見える問題も踏んでいる**——Material3 1.4 以降 `OutlinedButton` の既定の文字色は `onSurfaceVariant` で、本アプリはそこに補助文字の `muted` を当てている（§5 の配色トークン）ため、色を任せると強調にならない。塗り（`Button`）の既定は `primary` ＝ アクセントなので、この問題は起きない |
| 2026-09-05 | ダイアログの見出しも丸ゴシックにする（`headlineSmall` に当てる）。**大きさは Material の既定 24sp のまま**。§5 に「見出し」に当たる役割を書き添える | FB-15「ダイアログの見出しだけ書体が違う」。§5 は前から「見出し・CTA は丸ゴシック」と言っていたのに、**書体を当てていたのは画面とシートの見出しと CTA だけ**で、`AlertDialog` が引く `headlineSmall` が抜けていた。条項に実装が追いついていなかった側なので、**オーナー判断**は「書体だけ揃える」。大きさはシートの見出しより大きいままだが、**書体の話とは別**なので今回は動かさない。同じ抜けが再発しないよう、§5 に「Material の部品がどの役割を引くかで決まる」と明記した——部品を足すたびに、引かれる役割が丸ゴシック側かを確かめることになる |
| 2026-09-05 | 選択中のチップに ✓ を出す（01 の絞り込みと 02・06 の選択の両方）。カテゴリ群と行き先群の間に縦の細線を挟む。カテゴリにも絵文字（🏷）を前置する。「どこでも」の 2 通りの表記は場面で使い分けると明記する | チップ列を触る 4 件（FB-07・08・13・11）をまとめて決めた。**FB-07 は測って原因が変わった**——「視認性が低い」ではなく、選択面と地のコントラスト比が**ライトの 3 パレットで 1.02〜1.05:1**（1.00 が同色）で、手がかりが実質ゼロだった。加えて `FilterChip` は**選択すると枠が消える**ので、選択で手がかりが 1 つ減って代わりに増えるのがこの差だけという状態。**オーナー判断で ✓**——[Material の作法](https://developer.android.com/develop/ui/compose/components/chip)そのもので、色に頼らないので 3 パレット × 明暗の 6 通りに同じだけ効き、[WCAG 1.4.1](https://www.boia.org/wcag2/cp/1.4.1)（色だけで状態を伝えない）も満たす。**適用範囲も測って決めた**——当初は「シートは面が `card` なので問題が起きていない」という見立てだったが、比を出すと**ライトは 1.22:1 とましな一方ダークは 1.16:1 で 01 より低い**と分かり、**オーナー判断で両方**に付けた。トークンそのものを見直すかは [FB-16](../案件/closed_21_FB台帳.md#fb-16) が持つ。FB-08 の仕切りは**オーナー判断で縦の細線**（余白だけだと横スクロールで境が画面外に出ると群が分からない）。FB-13 は**オーナー判断でカテゴリにも付ける**（🏬 を外す案もあったが、仕切りが入っても「どこで」と「何の」の対比は残したい）。FB-11 は**オーナー判断で現状維持**とし、幅の限られるチップだけ短縮形という割り切りを §3 に明記した |
| 2026-09-06 | 下部 CTA（01・04）の高さを 56dp にする。**ダイアログやシートのボタンは既定のまま** | FB-14「下部 CTA の高さが足りない」。**当たり判定は元から 48dp あった**——`Button` は `Surface(onClick)` に委譲し、そこが `minimumInteractiveComponentSize()`（既定 48dp）を掛けるので、40dp は描かれる面の高さでしかない（[FB-03](../案件/closed_21_FB台帳.md#fb-03) の `IconButton` と同じ仕組み。**当初「Button は確保しない」と書いたのは誤りで、レビューの指摘で上流のソースを読んで正した**）。したがって直す理由は見た目——**幅が画面いっぱいに伸びる CTA に対して面が低い**——だけ。**オーナー判断で 56dp**——Material 1.9 のボタンは 32 / 40 / 56 / 96 / 136 のサイズ段を持ち、**56dp はその中間段**なので「幅が画面いっぱいの CTA に既定の 40dp は小さい」に対して段に乗った答えになる。48dp も候補だったが、Material のサイズ段には無い値なので採らなかった。**範囲は下部 CTA だけ**（オーナー判断）——離脱確認の「続ける」やシートの「登録」も同じ素の `Button` だが、シートやダイアログの中で 56dp は大きく見える。**同じ形が 2 か所に書かれていたので `ReBuyBottomCta` にまとめた**——高さを足すと同じ値が散るため |
| 2026-09-06 | 05 の検索は**名前と入力の両方を検索キーへ畳んでから部分一致**にする。畳むのはかなとカナ・全角と半角（半角カタカナを含む）・英字の大小の 3 つで、**濁点と漢字の読みは畳まない** | FB-10「かなカナくらいは対応したい」。**オーナー判断**で 2026-09-01 に「かな・カナに加えて全角・半角も」と決めており、着手時に縁を 2 つ詰めた——**半角カタカナも含める**（「全角・半角」の素直な読み。`ｶﾞ` は 2 文字なので広げるだけでは `ガ` に届かず、濁点の合成が要る）と、**英字の大小も含める**（決定文には無いが動機は同じ「打ち方の揺れ」で、実装は 1 行）。**プラットフォームの正規化 API は使わない**——JVM の `java.text.Normalizer` と Foundation の `precomposedStringWithCompatibilityMapping` は畳む範囲が完全には一致せず、同じ入力で Android と iOS の検索結果が変わりうる。純 Kotlin の `SearchKey`（`:shared:domain`）なら 1 実装で両方が揃い、ホスト段で全分岐を通せる。**14 にあった `name LIKE '%入力%'` の表記は落とした**——両辺を畳んでから比べるので SQL では絞れず、実装も前から Kotlin 側で絞っていた（条項が実装から離れていた側）。**畳みすぎる側の網も対で置いた**——「か」で「が」まで当たると探す側の意図から離れるので、`SearchKeyTest` は通す網と止める網を両方持つ |
| 2026-09-06 | 05 で**畳んで既存が当たっても「＋ この名前で登録する」は弾かない**。あわせて、結合用濁点（NFD）の非対称も畳む側へ倒す | FB-10 の副作用としてレビューで出た。「かな違いで打つと既存が当たりに出るのに、同じ名前で登録もできて別レコードになる」という形。**オーナー判断で現状のまま**——重複判定に正規化はしないと [14](./14_データモデル定義書.md) §5 が既に決めており、かな違いは別の名前。むしろ FB-10 で**既存が「追加済み」として当たりに見えるようになったぶん、選ぶ側に倒しやすくなっている**ので、弾く側に変えると「あえて両方持ちたい」場面だけを潰すことになる。条項に書き添えたのは、次に見た人が不具合と読んで直さないため。NFD のほうは**中途半端に効いていたのが問題**——合成表がカタカナ 1 組なので、貼り付けで入る NFD のカタカナは合成できてひらがなはできなかった。1 パス目でひらがなもカタカナへ寄せることで、`ｶﾞ`・`カ゛`・`か゛`・NFD の 4 通りが同じ経路を通る（表は 1 組のまま） |
| 2026-09-06 | 05 の検索で**踊り字（`ゝゞ`・`ヽヾ`）と `ゟ`・`ヿ` はかなカナを同一視しない**。条項に例外として書き添える | 3 巡目のレビューで、13 §05 が「ひらがなとカタカナを同一視する」と言い切っているのに実装には例外がある、と指摘された。**当初 KDoc に書いた却下理由（「かなとカナで並びが揃っておらず枝を分けることになる」）は誤り**で、踊り字も `+0x60` と既存の枝とまったく同じ差、`ゝ..ゟ` → `ヽ..ヿ` が連続していて `・`・`ー` にも当たらない。つまり畳むなら枝 2 つで済む。前提を正したうえで**オーナー判断は「畳まず条項に例外を書く」**——品目名に踊り字が出る見込みが低く、枝を足す価値が無いという判断。**条項は利用者への約束なので、例外は実装の KDoc ではなく条項側に書く**。`ヷ`・`ヺ` は「対応するひらがなが無い」だけで両辺が同じように残るため約束を破っておらず、書き添えない |
| 2026-09-06 | **カゴに行き先付きの品目が 1 件も無いときは 03 を開かず、01 の CTA から全件モードの 04 へ直行する**。あわせて全件モードを 03 の記述から §3 の用語表へ移す | FB-04「行き先が存在しない場合に『今日はどこへ？』→『2 件で開始』が少し違和感ある」。調べると**全件モードは「行き先を 1 つも作っていない人」だけの状態ではなく**、行き先を持つ人でも今日のカゴがたまたま全部どこでも買えるものなら入る。どちらの場合も 03 は**選択肢が 1 つしかないシート**になり、最短経路（C-2）の上の段差になっていた。見出しを差し替えて問いと答えを噛み合わせる案もあったが、**オーナー判断で「問いを直す」ではなく「問わない」**——選ぶ相手が無いときにシートを出す理由がそもそも無い。カゴの中身は 01 の一覧で見えているので、確認の場を失うことにもならない。**判定は 01 側へ移した**（03 はこの状態で開かれない画面になる）。判定に使うのは `PoolItem.destination` で、03 の内訳が `destinations` を突き合わせて作るのと同じ見方に揃う。**行き先が 1 つでも 03 は出す**——1 行でも「今日はどこへ？」の答えにはなっており、「2 つ以上あるとき」に狭めると次の行き先を作るまで 03 を見ない期間ができる |
| 2026-09-06 | 03 の内訳の下に**脚注「＋m 件はどこでも買えるもの。どの行き先にも一緒に入ります」**を淡色で置く（m が 0 なら出さない） | FB-09「どこでも（行き先なし）が一緒に入っていることが＋1 などの暗黙でしか分からない」。**03 は毎回通る場所**なので、足した説明も毎回読ませることになる（C-2・C-4）。1 タップ先の 04 に「どこでも買えるもの」の区切りが出るので**一度使えば分かる**——対処不要も候補として挙げたが、**オーナー判断で脚注**。件数の書式を「3 件＋どこでも 1 件」に変える案は行ごとに同じ語を繰り返して右端が長くなり、独立した行として出す案は**同じ形の行でタップできるものとできないものが混ざる**（03 の行は「行き先」であることが押せる理由）。脚注なら行の構造も「＋m」の書式も変えずに済み、**m が 0 のときは出ない**ので関係あるときだけ現れる |
| 2026-09-06 | `accent-soft` を 1 段濃くする（6 値。トークンは 1 つのまま分けない） | FB-16「そもそもパレットの色指定を見直すべきか」。**測り直して台帳に無い事実が 2 つ出た**。1 つは**比べる相手が役目で違う**こと——台帳は「選択面と下地」だけを測っていたが、**カゴ入り行が並ぶ相手は地ではなく隣の白い行（`card`）**。分けて測ると**ライトはチップ側（1.02〜1.05）、ダークは行側（1.16〜1.18）が弱く、明暗で弱点が逆**だった。もう 1 つは**下地そのものが薄い**こと——`card` が `page` に浮く比が 1.16〜1.30 しかなく、**選択面だけ 3:1 まで濃くすると、状態が画面の構造そのものより目立つ**。実際 `accent` へ 50% 寄せてもライトは 2.0〜2.3 止まりで、3:1 には塗りつぶしに近い濃さが要る。**そして両方の役目に既に ✓ がある**（FB-07 と 13 §01）ので [WCAG 1.4.1](https://www.boia.org/wcag2/cp/1.4.1)（色だけで状態を伝えない）は満たしており、**これは違反の修正ではなく見た目の判断**。**オーナー判断で「1 段濃く」**——「実質ゼロ」からは離れ、下地の 1.2 を上回り、それ以上は行かない（ライトのチップが 1.43〜1.51、ダークの行が 1.81〜1.86）。**トークンを役目で分ける案は採らなかった**——3 パレット × 明暗で 6 値増えるだけでなく、新しい部品ごとに「どちらを引くか」の判断が増える（C-4）。**選択チップを `accent` の塗りにする案**（3:1 に届く唯一の道）は、カゴ入り行に同じ手が使えず一覧が縞模様になるので、2 役を分けることが前提になる |
| 2026-09-06 | `accent-soft` の条項を**具体的な値の作り方ではなく上下の範囲**で書く。下限は「地とも隣の行とも、その配色の `card` と `page` の重なりより大きく離れる」、上限は「選択面の上の `ink` が AA の 4.5:1 を割らない」 | FB-16 のレビューで 2 つ出た。1 つは**条項が再現できない形だった**こと——「`accent` へ 25% 寄せた」の起点は**差し替えで消えた旧 `accent-soft`** で、条項に残る `page` から計算すると 6 値とも別物になる（`#B6CEBF` に対し `#BCCEC1` など）。レシピは導出の跡であって仕様ではないので落とした。もう 1 つは**下限の数値が実測に張り付いていた**こと——テストの閾値 1.4 に対し最小が 1.425 で、柿ライトを 8bit で 2 段明るくするだけ、寄せ率を 25% → 23% にするだけで鳴る。**オーナー判断で「`card`/`page` の重なりより大きい」という関係で書く**——マジックナンバーが消え、「下地の桁を上回ればよい」という判断そのものが条件になる。旧値 6 通りもすべてこれを下回るので検出力は落ちない。**上限はオーナー判断で「`ink` が AA を割らない」**——「構造より目立つ」より測れる基準で、WCAG の既存の値なので選ぶ必要が無い。上限が無いままだと `accent-soft` を `accent` そのものにしても 6 通り中 5 通りが緑で通っていた |
| 2026-09-06 | 補助文字 `muted` を選択面の上に置く組み合わせは**この規則の対象外**とし、[FB-17](../案件/21_FB台帳.md#fb-17) を起こす | FB-16 のレビューで、**カゴ入り行の「前回 M/D」とタグが `accent-soft` の上に `muted` で描かれている**と指摘された（`PoolScreen` が `ReBuyRowCard` の中に置く）。**この点は当初「該当する描画箇所は無い」と報告したが誤りで、実装を当たって訂正した。** 比は 2.65〜3.07 で AA を割る。ただし**割っていたのは FB-16 の前からで 6 通り中 4 通り**（藍ダークと柿ダークだけが今回 4.74 から落ちた）、かつ `muted` × 地もライトで 3.98 と割っており、**FB-16 に閉じた問題ではない**。**オーナー判断で、今回は範囲外として FB-17 に切り出す**——`muted` を含む網をいま入れると、どう扱うかを決める前に落ちる |
| 2026-09-07 | iOS の外枠は **`NavigationStack` 1 本だけ**を SwiftUI で持ち、シート・ダイアログ・下部 CTA は Compose のまま残す（§6 を新設） | [T-44](../案件/23_技術改善バックログ.md#t-44)（段 4）の着手前に、範囲をオーナーと詰めた。**外枠の載せ替えだけでは足りない**——iOS 26 の Liquid Glass はシステムが `NavigationStack` に描くが、**バーの背後をコンテンツが通らないと屈折する対象が無くただの半透明の板に見える**ので、Compose の中身がバーの下まで流れるところまでを 1 つの範囲にした（**オーナー判断**）。**シートは Compose のまま**——`.sheet` に寄せると iOS の作法は付くが 6 つが二重実装になり C-4（単純さ）と釣り合わない（**オーナー判断**。後から個別に出す道は残る）。**下部 CTA も Compose のまま**——有効・無効と件数が Compose 側の状態に紐づいており、SwiftUI へ出すと配線が増える（**オーナー判断**）。避けるべきホームインジケータは Android のジェスチャーナビゲーションと同じ扱いで、`WindowInsets` で逃がす。**タブが無いことは 13 §1 が既に決めていた**ので、11 §9 の「タブバー」を落として揃えた（[要求定義書の決定ログ](./log_11_要求定義書.md) 2026-09-07） |
| 2026-09-07 | §6 に「バーに出すものは Kotlin が決める」「04 の戻るは iOS でも離脱確認を通す」「端スワイプで戻る手段は無い」の 3 条を足す | 最初に書いた §6 に**穴が 2 つ**あると仕様レビューが指摘し、**スパイクで実物を見て埋めた**。(1) **見出しの二重化**——§6 は見出しを SwiftUI の責務としたが、§4 は 01 に「Re-Buy ＋全 n 件 ＋ ＋ ⚙」、04 に「◯◯で買い物中 ＋ x / n」を定めており、両方が描くと**シミュレータで実際にタイトルが縦に 2 つ並んだ**。バーに出すものは Kotlin が 1 つの状態として渡し、**iOS では Compose 側は描かない**形にした。**「下部 CTA を SwiftUI に出すと配線が増える」と同じ配線が、アプリバーには必ず要る**——避けられない。(2) **04 の離脱確認**——ネイティブの戻りがそのまま画面を閉じると、チェックした内容が確認なしで失われる。バーの ← は「1 つ戻る」ではなく**画面が決めた動作**を呼ぶ、と書き分けた。(3) **端スワイプ**——「iOS の作法がそのまま付く」と書いていたが、**実測すると現状の iOS 版では端スワイプがまったく効かない**（設定でも 04 でも無反応）。ライブラリ側に仕掛けは揃っているのに繋がっていないので、[T-62](../案件/23_技術改善バックログ.md#t-62) として切り出し、§6 には現在の事実だけを書いた。**あわせて下部 CTA のホームインジケータ回避は現状の実装で既に効いていることも実測**（`Scaffold` の `contentWindowInsets`） |
