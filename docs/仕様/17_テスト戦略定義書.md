# Re-Buy（仮称） テスト戦略定義書

- 版数: 1.0
- 作成日: 2026-08-30
- 更新日: 2026-08-31
- 位置づけ: 何をどこで守るかは本書が正。個々のテストの中身は各テストファイルの KDoc に書く
- 関連: [アーキテクチャ定義書](./15_アーキテクチャ定義書.md) ／ [技術改善バックログ](../案件/23_技術改善バックログ.md)

## 1. テストの段

| 段 | 置き場所 | 対象 | 実行 | 走る場所 |
|---|---|---|---|---|
| ホスト（共通） | `shared/*/src/commonTest` | プラットフォームに依らない純粋ロジック（コンバータ・UiState の派生値・Repository の遷移規則・`Navigator`） | `testAndroidHostTest` ／ `iosSimulatorArm64Test` | **Android と iOS の両方** |
| ホスト（プラットフォーム別） | `shared/*/src/androidHostTest` `src/iosTest` | 片方でしか書けないもの（`kotlin-reflect` を要するもの・日付書式の実測値） | 同上 | 片方だけ |
| 画面（iOS） | `shared/ui/src/iosTest` | `runComposeUiTest` で `ReBuyApp()` を描いて操作する（画面遷移・戻る・ボトムナビ） | `iosSimulatorArm64Test` | iOS のみ |
| インストルメンテーション | `androidApp/src/androidTest` | Room（マイグレーション・DAO）・Compose UI・リソースが APK に載っているか・プロセス death からの復元 | `pixel6Api35DebugAndroidTest`（GMD） | Android のみ |

**既定は `commonTest`。** 片方でしか書けない理由があるときだけ分ける。

**画面を動かすテストは `commonTest` に置けない。** 置くと Android 向けにもコンパイルされ、Android フレームワークの無い素の JVM（`testAndroidHostTest`）で実行されて `android.os.Build.FINGERPRINT` が null で落ちる。Robolectric が要るが、その有効化は `@RunWith` という JUnit 4 のアノテーションなので `commonTest` には書けない。Gradle 側で除外する手もあるが、**除外がソースから見えずクラス名の一致で効く**ので採らない（[§4](#4-テストが-0-件で緑になるのを止める) と同じ「黙って外れる」形になる）。Android 側は instrumented が実物の `MainActivity` を起動して同じ経路を見ている。

## 2. 原則

### 2.1 期待値をリソースから引かない

文言や書式を固定するテストは、**期待値をリテラルで書く**。`Res.string.*` や `DateTimeFormatter` から期待値を作ると自己参照になり、値が壊れても画面とテストが同じ壊れた値を見て素通りする。

**逆に、キーで引いた先に物があるかを見るテストは実装側の定数を参照する**（`LicenseLibrariesTest` の `ABOUT_LIBRARIES_PATH`）。パスをテスト側に書き写すと、書き写しどうしが一致するだけで実装の誤りを止められない。

### 2.2 網を張ったら変異で確かめる

**テストを足したら、守るはずの箇所を 1 か所ずつ壊して、狙った件だけが落ちることを確認する。** 変異はメインセッションが `Edit` で行い、git で退避しない。段 3 では素朴な実装が変異をすり抜けた例が複数ある（T-32 は 2 回）。

### 2.3 期待件数を書かない

「このモジュールは N 件」と書くと、今度はその数字の更新漏れが起きる。**件数で見るのは「0 件かどうか」だけ**（[§4](#4-テストが-0-件で緑になるのを止める)）。下限を置く場合も、依存の増減で上下しない水準に取る。

### 2.4 プラットフォーム差は両方で見る

`commonTest` に置けば両方で走る。**片方でしか走らないテストは、そのプラットフォーム固有の壊れ方しか止められない**——iOS だけで落ちる不具合（[アーキテクチャ定義書](./15_アーキテクチャ定義書.md) §4.2 の `when` の件）は Android の全テストが緑のまま通り抜けた。

## 3. 何をどこで守るか

| 守りたいもの | どこで | 落ちるとどうなるか |
|---|---|---|
| 日時の保存形式（エポックミリ秒・切り捨ての向き・境界値） | `commonTest` のコンバータテスト | 保存済みの日時が別の日時として読まれる |
| ステータス遷移の規則（同じ状態への更新は no-op） | `commonTest` の ViewModel テスト（Repository は本物、DAO だけ `FakeDatabase`） | 買い物リストの出入りが壊れる |
| 画面遷移の規則 | `commonTest` の `NavigatorTest` | タブごとの履歴が混ざる |
| UI の操作が遷移規則に結線されているか | instrumented の `NavigationTest`（Android） ／ `iosTest` の `NavigationIosTest`（iOS） | ボタンが効かない |
| iOS で 6 画面が描けること | `iosTest` の `NavigationIosTest` | その画面を開いた瞬間に落ちる（Android のテストでは捕まらない） |
| 品目が無いときの空状態（文言・ボタン・そこからの遷移） | `iosTest` の `NavigationIosTest` | 何も無い画面が出る。**iOS の網が「DB が空」を前提にしている番人でもある** |
| プロセス death からの復元 | instrumented の `NavigationStateRestorationTest` | 復帰したら別の画面にいる |
| ルート登録の 3 か所そろえ | `androidHostTest` の `ScreenSerializationTest` | その画面でプロセス保存が走ると落ちる |
| 文言の書式解釈（`\n` と `%1$s`） | instrumented の `StringResourceFormatTest` | 画面に `%1$s` がそのまま出る |
| リソースが APK に載っているか | instrumented の `LicenseLibrariesTest` | その画面を開いた瞬間に落ちる |
| ライセンス一覧の収集範囲 | instrumented の `LicenseLibrariesTest` | 一覧が空になる（表示義務に関わる） |
| DI の配線 | `androidHostTest` の `KoinModulesTest` ＋ instrumented の `KoinGraphTest` | 起動時に落ちる |
| 日付書式 | `androidHostTest` `iosTest` の `ShortDateTest` | 「最終購入」の表示が変わる |

## 4. テストが 0 件で緑になるのを止める

KMP ライブラリプラグインはホストテストが既定で無効で、開け忘れると **0 件のまま緑**になる。convention plugin `rebuy.android.base` の `failOnZeroTests()` が、**テストソースを持つ source set のうち 1 つでも「1 件も実行していない」なら落とす**。

見ているのは `testAndroidHostTest`（`commonTest` ＋ `androidHostTest`）と `iosSimulatorArm64Test`（`commonTest` ＋ `iosTest`）の 2 タスク。JUnit XML の `testsuite name` の単純クラス名と、source set 配下の `.kt` のファイル名を突き合わせる。

設計はすべて実測で決まっており、**どれを外しても止まらなくなる**。

- **テストタスクの `doLast` に置かない。** source set が外れると `NO-SOURCE` でタスクごと飛び、`doLast` も走らない
- **`finalizedBy` に加えて `check` にも繋ぐ。** `finalizedBy` だけだと、`withHostTest {}` の入れ忘れでテストタスク自体が生成されないときに検査ごと消える
- **タスクごとに自分の出力だけを見る。** `build/test-results` を丸ごと数えると、別のタスクが前に残した XML で通る
- **source set 単位で見る。** タスク単位の合計だと、`commonTest` の 106 件が丸ごと消えても `androidHostTest` の数件で緑になる
- **`skipped` を差し引く。** `tests` は `@Ignore` も数えるので、全件 `@Ignore` でも通ってしまう
- **iOS は macOS のときだけ見る。** Linux では Kotlin/Native のテストタスクが無効化されるので、検査だけが走って 0 件と判定してしまう。つまり **iOS 側の 0 件を守っているのは macOS の `ios` ジョブ 1 本だけ**

## 5. CI

| ジョブ | 走る場所 | 中身 |
|---|---|---|
| `docs` | Linux | `sh scripts/docs-check.sh` |
| `build` | Linux | `./gradlew build`（lint・ホストテスト・assemble）＋ `git diff --exit-code`（生成物の鮮度） |
| `instrumented` | Linux | GMD でインストルメンテーションテスト |
| `ios` | macOS | `iosSimulatorArm64Test` ＋ `xcodebuild`（Swift 側とフレームワークのリンク） |

**Linux は iOS のタスクを落とすのではなく「このホストでは走らない」と警告して素通りする。** `ios` ジョブが無いと iOS のコンパイルエラーが CI をすり抜ける。

`ios` のテストとアプリビルドは Kotlin/Native のコンパイルを共有しているのでジョブを分けない（分けると実時間は 1 分縮むだけでランナー消費が 55% 増える）。代わりにアプリビルドを `if: !cancelled()` で常に走らせ、1 回の実行で Kotlin 側と Swift 側の両方の結果が出るようにしている。

## 6. いま網が無いところ

**自動テストで守れていない場所を明示する。** ここに書いていないものが守られている、という意味ではない。

| 守れていないもの | 案件 |
|---|---|
| **iOS で本物の Room が動くこと**。画面のテストは DAO を `FakeDatabase` に差し替えており、Room を通らない | [T-35](../案件/23_技術改善バックログ.md#t-35) |
| iOS のカゴタブの行（`HomeListItemRow` を「すべて」タブでしか通していない） | [T-41](../案件/23_技術改善バックログ.md#t-41) |
| **iOS の実物の `.app` を起動する層**。Swift の起動経路・`.app` への同梱・OS 側の挙動 | [T-46](../案件/23_技術改善バックログ.md#t-46) |
| iOS のライセンス一覧の中身。一覧は非同期に読むので、`NavigationIosTest` はタイトルまでしか見ていない | [T-39](../案件/23_技術改善バックログ.md#t-39) |
| **タブごとの履歴保持の UI 段**。`NavigatorTest` が状態までは見ているが、`NavDisplay` までの結線を見るテストは無い。ボトムナビを出すのがトップレベル 2 画面だけなので**UI からこの分岐に到達できない**（変異で実測） | 現状は到達不能なので案件を立てていない |
| 本番と同じ driver 経路での DB オープン（`RoomMigrationTest` は旧経路を見ている） | [T-34](../案件/23_技術改善バックログ.md#t-34) |
| 旧バージョンの DB を持つ端末の起動（データ消失の方向への退行） | [T-25](../案件/23_技術改善バックログ.md#t-25) |
| **Android で** DB にデータが要る 2 経路（買い物終了でホームへ戻る・空状態のボタンから遷移）。iOS 側は `FakeDatabase` を差せるので後者を自動化済み | [T-21](../案件/23_技術改善バックログ.md#t-21) |
| テキスト入力ダイアログの入力 → 確定 → `onConfirm` の値 | [T-38](../案件/23_技術改善バックログ.md#t-38) |
| `strings.xml` の書式指定（CMP が解釈しない `%s` を書いても止まらない） | [T-37](../案件/23_技術改善バックログ.md#t-37) |
| **§4 の 0 件検査が届かないところ**——`:androidApp` の instrumented（KMP モジュールにしか効かない）、ターゲットや source set を増やしたときの追随、モジュールが `rebuy.android.base` を付け忘れた場合 | [T-45](../案件/23_技術改善バックログ.md#t-45) |

## 7. 決定ログ

判断の経緯は本文に書かず、日付順に積む。段 3 までの経緯は [log_23_技術改善バックログ.md](../案件/log_23_技術改善バックログ.md) にある。**本書に由来する判断が出た時点で `log_17_テスト戦略定義書.md` を作る。**
