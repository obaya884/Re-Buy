# Re-Buy（仮称） テスト戦略定義書

- 版数: 1.0
- 作成日: 2026-08-30
- 更新日: 2026-09-01
- 位置づけ: 何をどこで守るかは本書が正。個々のテストの中身は各テストファイルの KDoc に書く
- 関連: [アーキテクチャ定義書](./15_アーキテクチャ定義書.md) ／ [技術改善バックログ](../案件/23_技術改善バックログ.md)

## 1. テストの段

| 段 | 置き場所 | 対象 | 実行 | 走る場所 |
|---|---|---|---|---|
| ホスト（共通） | `shared/*/src/commonTest` | プラットフォームに依らない純粋ロジック（コンバータ・UiState の派生値・Repository の遷移規則・`Navigator`） | `testAndroidHostTest` ／ `iosSimulatorArm64Test` | **Android と iOS の両方** |
| ホスト（プラットフォーム別） | `shared/*/src/androidHostTest` `src/iosTest` | 片方でしか書けないもの（`kotlin-reflect` を要するもの・日付書式の実測値・native にしか無い `Room.inMemoryDatabaseBuilder`） | 同上 | 片方だけ |
| 画面（iOS） | `shared/ui/src/iosTest` | `runComposeUiTest` で `ReBuyApp()` を描いて操作する（画面遷移・戻る・ボトムナビ） | `iosSimulatorArm64Test` | iOS のみ |
| インストルメンテーション | `androidApp/src/androidTest` | Room（マイグレーション・DAO）・Compose UI・リソースが APK に載っているか・プロセス death からの復元 | `pixel6Api35DebugAndroidTest`（GMD） | Android のみ |

**既定は `commonTest`。** 片方でしか書けない理由があるときだけ分ける。

**画面を動かすテストは `commonTest` に置けない。** 置くと Android 向けにもコンパイルされ、Android フレームワークの無い素の JVM（`testAndroidHostTest`）で実行されて `android.os.Build.FINGERPRINT` が null で落ちる。Robolectric が要るが、その有効化は `@RunWith` という JUnit 4 のアノテーションなので `commonTest` には書けない。Gradle 側で除外する手もあるが、**除外がソースから見えずクラス名の一致で効く**ので採らない（[§4](#4-テストが-0-件で緑になるのを止める) と同じ「黙って外れる」形になる）。Android 側は instrumented が実物の `MainActivity` を起動して同じ経路を見ている。

## 2. 原則

### 2.1 期待値をリソースから引かない

文言や書式を固定するテストは、**期待値をリテラルで書く**。`Res.string.*` や `DateTimeFormatter` から期待値を作ると自己参照になり、値が壊れても画面とテストが同じ壊れた値を見て素通りする。

**逆に、キーで引いた先に物があるかを見るテストは実装側の定数を参照する**（`LicenseLibrariesTest` の `ABOUT_LIBRARIES_PATH`）。パスをテスト側に書き写すと、書き写しどうしが一致するだけで実装の誤りを止められない。

### 2.2 網を張ったら変異で確かめる

**テストを足したら、守るはずの箇所を 1 か所ずつ壊して、狙った件だけが落ちることを確認する。** 変異はメインセッションが `Edit` で行い、git で退避しない。段 3 では素朴な実装が変異をすり抜けた例が複数ある（T-32 は 2 回）。

### 2.3 期待件数を書かない

「このモジュールは N 件」と書くと、今度はその数字の更新漏れが起きる。**件数で見るのは「0 件かどうか」だけ**（[§4](#4-テストが-0-件で緑になるのを止める)）。下限を置く場合も、依存の増減で上下しない水準に取る。

### 2.4 プラットフォーム差は両方で見る

`commonTest` に置けば両方で走る。**片方でしか走らないテストは、そのプラットフォーム固有の壊れ方しか止められない**——iOS だけで落ちる不具合（[アーキテクチャ定義書](./15_アーキテクチャ定義書.md) §4.2 の `when` の件）は Android の全テストが緑のまま通り抜けた。

## 3. 何をどこで守るか

| 守りたいもの | どこで | 落ちるとどうなるか |
|---|---|---|
| 日時の保存形式（エポックミリ秒・切り捨ての向き・境界値） | `commonTest` のコンバータテスト | 保存済みの日時が別の日時として読まれる |
| ステータス遷移の規則（同じ状態への更新は no-op） | `commonTest` の ViewModel テスト（Repository は本物、DAO だけ `FakeDatabase`） | 買い物リストの出入りが壊れる |
| プールの一覧・カゴの出し入れ（チェック済みからも常駐へ戻る）・絞り込み（カテゴリと行き先の AND・「どこでも」は行き先なしだけ・解除）・空状態の出し分け・**カゴ件数は絞り込んでも変わらないこと** | `commonTest` の `PoolViewModelTest` | 絞り込みが緩い側へ壊れても行は出るので気づけない |
| プールの**画面段**（チップを押すと一覧が絞られる・行にカテゴリと行き先と「前回 M/D」が出る・絞り込み 0 件の文言・**＋ から登録して一覧の末尾に現れること**） | `iosTest` の `PoolIosTest`。**状態だけを見る ViewModel テストでは結線の抜けが捕まらない**（変異で実測） | チップが効かない、行にタグが出ない、登録しても一覧に出ない |
| 登録シートの規則（名前だけで登録・登録直後はカゴに入れない・「続けて登録」は名前だけ消してチップを残す・02b で作ったものが選択済みになる・並びの末尾）と、**閉じるときに捨てるもの 6 つ** | `commonTest` の `RegisterViewModelTest` | 登録した品目がいきなりカゴに入る、続けて登録でチップを選び直すことになる、前回の入力が次に開いたときに残る |
| 品目編集シートの規則（開いた値が入る・名前とカテゴリと行き先をまとめて反映・重複判定から自分自身を除く・弾かれたら何も書かない・「なし」で外す・削除・開き直しで前の状態が残らない） | `commonTest` の `ItemEditViewModelTest` | 名前が弾かれたのにカテゴリだけ変わる、前に開いた品目のエラーが残る |
| 行の長押しで編集シートが開き、保存と削除が一覧に反映されること | `iosTest` の `PoolIosTest`。**長押しは行タップと同じ行に載っている**ので、取り違えるとカゴに入るだけになる | 長押しで何も起きない、削除の確認を挟まずに消える |
| シートが**開き直せる**ことと、保存せずに閉じたら入力が残らないこと | `iosTest` の `PoolIosTest`（下スワイプは `Dismiss` のセマンティクスから踏む） | 一度登録すると二度と開けない（実際に踏んだ） |
| Android で ＋ から登録シートが開き、端末の戻るで閉じること | instrumented の `NavigationTest`。**`ModalBottomSheet` は Android と skiko で実装が分かれる**（§2.4） | Android だけシートが出ない／戻るで閉じない |
| 日付の書式（一覧は「M/D」でゼロ埋めなし、編集シートは「YYYY-MM-DD」でゼロ埋めあり。端末のタイムゾーンで日付に落とす） | `androidHostTest` の `LocalDateTest` ／ `iosTest` の `LocalDateIosTest`。**ロケールに従う `formatShortDate` とは別物** | 日付が 1 日ずれる、桁が揃わない |
| 名前の規則のうち DB を引かないぶん（トリム・全角空白・空・30 文字上限・文字種を制限しないこと） | `:shared:domain` の `commonTest` の `NameRuleTest` | 空白だけの名前が通る、上限が効かない |
| 名前の規則のうち DB を引くぶん（同種内の重複・自分自身は除く・保存はトリム後） | `commonTest` の `NameValidationRepositoryTest`（Repository は本物、DAO だけ `FakeDatabase`）。`existsName` の SQL は `iosTest` の `AppDatabaseIosTest` | 同じ名前が 2 つ入る、または自分自身と同じ名前へ保存できない |
| 名前入力欄の UI 側の網（31 文字目以降の打ち切り・**3 つの理由すべての文言**の表示） | `iosTest` の `NameTextFieldIosTest`。**ここでしか見られない**——打ち切りを外しても他は全件緑（変異で実測） | 上限超えが入力できてしまう。エラーの文言が出なければ、赤枠だけで理由の分からない画面になる |
| 弾かれたときにダイアログを閉じないこと | `commonTest` の `CategoryEditViewModelTest` | エラーが見えないままダイアログが閉じ、入力が消える |
| 改名ダイアログが今の名前を出し、打ち直した値が `onConfirm` に渡ること | `iosTest` の `TextFieldEditDialogIosTest` | 弾かれた後に名前を直せない（F-003 でここが主導線になった） |
| カテゴリー・行き先の並び順の採番（新規は末尾）と品目の行き先の付け替え | `commonTest` の `DataModelRepositoryTest`（画面がまだ無いので Repository を直に叩く） | 追加した行が先頭に出る、行き先が付け替わらない |
| 名前の UNIQUE と `ABORT`・並び順の `ORDER BY` が**本物の SQLite で**効くこと | `iosTest` の `AppDatabaseIosTest`（3 テーブルとも）。`FakeDatabase` は真似ているだけで、`onConflict` はスキーマ JSON にも出ない | 同名の登録が既存の行を消す（品目なら最終購入日ごと） |
| テーマの選択（既定は藍・保存と読み出し・知らない名前は既定に倒す）と配色トークン（パレットごとにアクセントが違う・共通トークンは変わらない） | `commonTest` の `ThemeTest` | 起動のたびにテーマが変わる、パレットを選んでも見た目が変わらない |
| テーマ画面へ辿り着けることと、タップした瞬間に選択が変わること | `iosTest` の `ThemeIosTest` | 設定から開けない、押しても変わらない（保存ボタンが無いので気づきにくい） |
| 同梱した書体が APK に載っていること | instrumented の `BundledFontTest` | 載っていなくても既定の書体で描かれるので、**画面は動いたまま見た目だけ変わる** |
| 同梱した書体が見出しと CTA に当たっていて、本文は既定のままであること | `iosTest` の `ThemeIosTest` | 載せたのに使われない（変異で実測。`BundledFontTest` だけでは素通りする）、または本文にも当てて配布サイズの判断が崩れる |
| 画面遷移の規則 | `commonTest` の `NavigatorTest` | タブごとの履歴が混ざる |
| UI の操作が遷移規則に結線されているか | instrumented の `NavigationTest`（Android） ／ `iosTest` の `NavigationIosTest`（iOS） | ボタンが効かない |
| iOS で 7 画面が描けること | `iosTest` の `NavigationIosTest` | その画面を開いた瞬間に落ちる（Android のテストでは捕まらない） |
| プールの CTA がカゴの有無で有効・無効に切り替わることと、CTA から買い物へ入れること | `iosTest` の `NavigationIosTest`（`FakeDatabase` でカゴを作れる） | カゴが空でも押せる、または入れたのに押せない |
| 品目が無いときの空状態（文言・ボタン・そこからの遷移） | `iosTest` の `NavigationIosTest` | 何も無い画面が出る。**テスト間で DB が空へ戻ることの番人でもある**ので、品目を置くテストより後に宣言する |
| 品目がある行と、品目が無いときの空状態の出し分け | `iosTest` の `NavigationIosTest` | 行が出ない、または空状態が出ない |
| プロセス death からの復元 | instrumented の `NavigationStateRestorationTest` | 復帰したら別の画面にいる |
| ルート登録の 3 か所そろえ | `androidHostTest` の `ScreenSerializationTest` | その画面でプロセス保存が走ると落ちる |
| 文言の書式解釈（`\n` と `%1$s`） | instrumented の `StringResourceFormatTest` | 画面に `%1$s` がそのまま出る |
| リソースが APK に載っているか | instrumented の `LicenseLibrariesTest` | その画面を開いた瞬間に落ちる |
| ライセンス一覧の収集範囲（両プラットフォーム分が `targets` 付きで入っていること） | instrumented の `LicenseLibrariesTest` ／ `iosTest` の `LicenseLibrariesIosTest` | 一覧が空になる、または片方のプラットフォームの依存が落ちる（表示義務に関わる） |
| ライセンス一覧のプラットフォーム絞り（接頭辞一致・`targets` 空は表示） | 規則は `commonTest` の `PlatformLibrariesTest`。画面が絞りを通すことは instrumented（iOS 専用の行を描かないこと）、iOS の絞った結果は `LicenseLibrariesIosTest`。**絞った結果のデータ段の検証は置かない**——リソースの `targets` と規則から導出できる | 提供していない依存が一覧に出る、または手足しした entry が黙って消える |
| DI の配線（Android） | `androidHostTest` の `KoinModulesTest` ＋ instrumented の `KoinGraphTest` | 起動時に落ちる |
| 日付書式 | `androidHostTest` `iosTest` の `ShortDateTest` | 「最終購入」の表示が変わる |
| iOS で Room が動くこと（生成 `actual`・native の SQLite・`@TypeConverters` の往復・`@Relation` の join） | `iosTest` の `AppDatabaseIosTest` | iOS だけ保存も読み出しもできない |
| iOS の DI の配線（本番の `dataModule` から `AppDatabase` と DAO が解けること） | `iosTest` の `DataModuleIosTest` | 起動した瞬間に落ちる |

## 4. テストが 0 件で緑になるのを止める

KMP ライブラリプラグインはホストテストが既定で無効で、開け忘れると **0 件のまま緑**になる。convention plugin `rebuy.android.base` の `failOnZeroTests()` が、**テストソースを持つ source set のうち 1 つでも「1 件も実行していない」なら落とす**。

見ているのは `testAndroidHostTest`（`commonTest` ＋ `androidHostTest`）と `iosSimulatorArm64Test`（`commonTest` ＋ `iosTest`）の 2 タスク。JUnit XML の `testsuite name` の単純クラス名と、source set 配下の `.kt` のファイル名を突き合わせる。

設計はすべて実測で決まっており、**どれを外しても止まらなくなる**。

- **テストタスクの `doLast` に置かない。** source set が外れると `NO-SOURCE` でタスクごと飛び、`doLast` も走らない
- **`finalizedBy` に加えて `check` にも繋ぐ。** `finalizedBy` だけだと、`withHostTest {}` の入れ忘れでテストタスク自体が生成されないときに検査ごと消える
- **タスクごとに自分の出力だけを見る。** `build/test-results` を丸ごと数えると、別のタスクが前に残した XML で通る
- **source set 単位で見る。** タスク単位の合計だと、`commonTest` の 106 件が丸ごと消えても `androidHostTest` の数件で緑になる
- **`skipped` を差し引く。** `tests` は `@Ignore` も数えるので、全件 `@Ignore` でも通ってしまう
- **iOS は macOS のときだけ見る。** Linux では Kotlin/Native のテストタスクが無効化されるので、検査だけが走って 0 件と判定してしまう。つまり **iOS 側の 0 件を守っているのは macOS の `ios` ジョブ 1 本だけ**

## 5. CI

| ジョブ | 走る場所 | 中身 |
|---|---|---|
| `docs` | Linux | `sh scripts/docs-check.sh` |
| `build` | Linux | `./gradlew build`（lint・ホストテスト・assemble）＋ `git diff --exit-code`（生成物の鮮度） |
| `instrumented` | Linux | GMD でインストルメンテーションテスト |
| `ios` | macOS | `iosSimulatorArm64Test` ＋ `xcodebuild`（Swift 側とフレームワークのリンク） |

**Linux は iOS のタスクを落とすのではなく「このホストでは走らない」と警告して素通りする。** `ios` ジョブが無いと iOS のコンパイルエラーが CI をすり抜ける。

`ios` のテストとアプリビルドは Kotlin/Native のコンパイルを共有しているのでジョブを分けない（分けると実時間は 1 分縮むだけでランナー消費が 55% 増える）。代わりにアプリビルドを `if: !cancelled()` で常に走らせ、1 回の実行で Kotlin 側と Swift 側の両方の結果が出るようにしている。

## 6. いま網が無いところ

**自動テストで守れていない場所を明示する。** ここに書いていないものが守られている、という意味ではない。

| 守れていないもの | 案件 |
|---|---|
| **iOS の実物の `.app` を起動する層**。Swift の起動経路・`.app` への同梱・OS 側の挙動・**本番の DB ファイルを実際に開くこと**——`DataModuleIosTest` は組み立てまでしか見ない（クエリを投げると `NSDocumentDirectory` に実ファイルを作ることになり、テストバイナリはアプリのサンドボックスで動かないので新品のシミュレータで落ちる） | [T-46](../案件/23_技術改善バックログ.md#t-46) |
| **iOS の本番の DB 設定**（`DataModule.ios.kt` の `setDriver` など）。`AppDatabaseIosTest` はその設定の複製で DB を組むので、本番側を壊しても緑のまま（変異で実測） | [T-46](../案件/23_技術改善バックログ.md#t-46) |
| 外部キー制約が native の driver で効いているか（`PRAGMA foreign_keys` は接続設定依存）。**行き先の削除だけは `AppDatabaseIosTest` が見ている**（効いていることを実測）。カテゴリー側と Android 側は未着手 | [T-49](../案件/23_技術改善バックログ.md#t-49) |
| `iosArm64`（実機ターゲット）は誰も検査していない。CI はシミュレータ向けしか回さない | [T-50](../案件/23_技術改善バックログ.md#t-50) |
| iOS でライセンス一覧が**画面に**描かれること。読み込みが非同期で、`LicenseLibrariesIosTest` はデータと絞りまで、`NavigationIosTest` はタイトルまでしか見ない | 画面は Android と共通実装で、Android 側は instrumented が表示まで見ている。iOS 固有の壊れ方が見えたら起票する |
| **SQL 段の並び順のタイブレーク**（`ORDER BY sortOrder, id` の `, id`）。落として実測しても素通りする——SQLite が返す順が rowid 昇順＝ id 昇順とたまたま一致するため。`FakeDatabaseTest` が fake 段で同じ規則を守っている | 実 SQL では観測できないので案件を立てていない。`, id` は順序を実装依存にしないための明示的な契約として残す |
| **タブごとの履歴保持の UI 段**。`NavigatorTest` が状態までは見ているが、`NavDisplay` までの結線を見るテストは無い。ボトムナビを出すのがトップレベル 2 画面だけなので**UI からこの分岐に到達できない**（変異で実測） | 現状は到達不能なので案件を立てていない |
| 本番と同じ driver 経路での DB オープン（`RoomMigrationTest` は旧経路を見ている） | [T-34](../案件/23_技術改善バックログ.md#t-34) |
| 旧バージョンの DB を持つ端末の起動（データ消失の方向への退行） | [T-25](../案件/23_技術改善バックログ.md#t-25) |
| **Android で** DB にデータが要る経路（買い物終了でプールへ戻る・**プールの CTA から買い物へ入る**）。CTA はカゴが空だと押せず、instrumented は本物の DB を使うので品目を置けない。iOS 側は `FakeDatabase` を差せるので自動化済み | [T-21](../案件/23_技術改善バックログ.md#t-21) |
| **トップレベルルートをまたいだ状態の復元**（`topLevelRoute` と 2 本目の backstack）。プールにボトムナビが無くなり、Android から買い物へ入るには DB のデータが要るようになったため、`NavigationStateRestorationTest` は 1 本の backstack しか踏めない | [T-21](../案件/23_技術改善バックログ.md#t-21)。買い物モードを 03 経由に組み替える F-009 で、そもそもトップレベルが 1 つになる見込み |
| テキスト入力ダイアログのうち**追加**ダイアログの入力 → 確定 → `onConfirm` の値。改名ダイアログ側は `iosTest` の `TextFieldEditDialogIosTest` が見ている | [T-38](../案件/23_技術改善バックログ.md#t-38) |
| `strings.xml` の書式指定（CMP が解釈しない `%s` を書いても止まらない） | [T-37](../案件/23_技術改善バックログ.md#t-37) |
| **§4 の 0 件検査が届かないところ**——`:androidApp` の instrumented（KMP モジュールにしか効かない）、ターゲットや source set を増やしたときの追随、モジュールが `rebuy.android.base` を付け忘れた場合 | [T-45](../案件/23_技術改善バックログ.md#t-45) |

## 7. 決定ログ

判断の経緯は本文に書かず、日付順に積む。段 3 までの経緯は [log_23_技術改善バックログ.md](../案件/log_23_技術改善バックログ.md) にある。**本書に由来する判断が出た時点で `log_17_テスト戦略定義書.md` を作る。**
