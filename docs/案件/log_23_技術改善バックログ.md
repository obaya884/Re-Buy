# 技術改善バックログ 決定ログ

- [技術改善バックログ](./23_技術改善バックログ.md) の「なぜそうしたか」を日付順に積む。本文は現在の状態だけを持つ

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-08-29 | T-28a の未確認事項（埋め込み Kotlin で KGP をコンパイルできるか）は解消した | Gradle 9.7.1 の埋め込み Kotlin は 2.2 ではなく 2.4.0 で、KGP 2.4.10 と同じマイナーだった。KGP の型に触れる捨てファイルを実際にコンパイルして確認したので、退避策（`java-gradle-plugin` ＋ 素の `Plugin` クラス）は不要 |
| 2026-08-29 | convention plugin は `extensions.configure<CommonExtension>` 1 つで application と library の両方を設定する | AGP 9 の `CommonExtension` は型引数を持たなくなり、`compileSdk` も `defaultConfig` も 1 つの型から触れる。Gradle の拡張検索は登録型の上位型にも一致するので、`ApplicationExtension` と `LibraryExtension` を分けて扱う必要がない（実測で確認）|
| 2026-08-29 | build-logic では `dependencyResolutionManagement` を使う | 版の二重管理を避けてルートと同じ catalog を読むため。CLAUDE.md の「使わない」はルートの settings の話で、build-logic は独立したビルドなので影響しない |
| 2026-08-30 | `androidx.sqlite` は Room が引いてくる 2.6.2 に合わせ、lint の「2.7.0 がある」警告は残す | 版を上げると Room が内部で使う driver と実装が二重になりうる。警告が 1 件増えるより、Room が実際に解決する版と一致しているほうが読み手に正確 |
| 2026-08-30 | Compose Multiplatform は 1.12.0 を選ぶ | 最新の安定版。Kotlin 2.2.20 でビルドされているが、Kotlin コンパイラは古いメタデータを読めるので 2.4.10 から使える |
| 2026-08-30 | KMP 化したモジュールでは `jvmTarget` をモジュール側で指定する | `rebuy.android.base` の `compileOptions` は KMP モジュールに当たらず、書かないとバイトコード版がビルド環境の JDK で決まる。手元の JBR 25 と CI の Temurin 21 で別物が出る状態だった。T-28b で convention plugin へ運ぶまでの暫定 |
| 2026-08-30 | KMP の android ターゲットは `targets.withType` で型から引く | `KotlinMultiplatformAndroidLibraryTarget` は `KotlinTarget` を継承しているので `targets` から型で拾える。拡張名で引いて未チェックキャストする形より、DSL 名が変わったときにコンパイルで気づける。非 KMP 側を `extensions.configure<CommonExtension>` にしたのと同じ理屈 |
| 2026-08-30 | iOS でも setQueryCoroutineContext は Dispatchers.IO にする | Native の `Dispatchers.IO` は internal ではなく、`Dispatchers` のメンバではなく拡張プロパティ。`import kotlinx.coroutines.IO` が要るだけだった。「internal だから使えない」と誤診して coroutines の版を上げ下げしたが、原因は import の不足 |
| 2026-08-30 | iosMain には使っていない compose.* も書く | `compose.material3` が自分より古い `foundation` を推移的に引くため、未使用として外すと版が 1.12.0 から 1.9.1 へ下がる。CMP の互換検査の警告で気づいた |
| 2026-08-30 | タイムゾーン固定の検査だけ androidHostTest に残す | 既定タイムゾーンを差し替える API が common に無い。CI は TZ=UTC で走るので、この検査を捨てるとタイムゾーン依存の混入が見えなくなる。共通の 20 件とは別クラスにして 2 件だけ JVM 側に置いた |
| 2026-08-30 | iOS の DB は Documents ディレクトリに置く | バックアップ対象で、アプリを消すまで残る。Android の `getDatabasePath` と対になる場所 |
| 2026-08-30 | iosApp のビルド設定は Xcode の GUI ではなく Config.xcconfig で持つ | GUI で保存すると pbxproj 全体がキャノニカル形式に書き直され、差分が読めなくなる。構成（Debug / Release）で差の無い設定は xcconfig に置き、pbxproj には差のあるものだけ残す |
| 2026-08-30 | pbxproj の疑似 UUID は Xcode と同じ 24 桁にする | 22 桁でも Xcode は動くが、外部ツール（xcodeproj gem・XcodeGen・Tuist）は 24 桁前提の実装が多い。連番かつ AA01 プレフィックスなので衝突の危険はランダム生成より小さい |
| 2026-08-30 | iOS の framework は段 3 では debug だけ作る | 既定の debug ＋ release だと `./gradlew build` が 7 分 17 秒になり、release のリンクがヒープ 4GB を要求する。段 3 の目的はシミュレータで動かすことなので release は要らない。実機配布が要る段 4 で足す |
| 2026-08-30 | iOS ターゲットは iosArm64 と iosSimulatorArm64 の 2 つだけにする | iosX64 は Intel Mac のシミュレータ用で、開発機も GitHub の macOS ランナーも arm64。要るようになったら build-logic に 1 行足せばよい |
| 2026-08-30 | ライセンス一覧はステップ 5〜14 の間だけ空を許容する | AboutLibraries 15.2.0 の KMP 経路が AGP の KMP android ターゲットから依存を拾えず、`:shared:ui` を KMP 化すると 0 件になる。プラグインは最新版で上げる先が無い。生成済み json をソースツリーに凍結する案は、生成物をコミットすることになるうえ debug の表示件数が変わる。ステップ 14 でどのみち composeResources へ移すので、そこまで待つ |
| 2026-08-30 | `Version.kt` を生成するタスクは convention plugin ではなくモジュールに置く | 計画は convention plugin に置くと書いていたが、`rebuy.versionName` を要るのは `:shared:ui` だけ。単一利用のタスクを共有 plugin に置くと、どのモジュールに効くのかが読めなくなる |
| 2026-08-30 | `jvmTarget` の暫定対応を T-28b で convention plugin へ移した | モジュール側に書く形は、KMP 化する 2 モジュール目・3 モジュール目で同じ書き忘れが起きうる。書き忘れてもビルドもテストも緑のままなので、モジュールの記述に頼らない形にした |
| 2026-08-30 | Compose Resources の `Res` は `publicResClass = true` で公開する | `:androidApp` の instrumented テストが画面のタイトルを文言で突き合わせている。テスト側に文字列をハードコードすると、文言を変えたときにテストが古い文言を主張したまま落ちる。既定の internal では外から引けない |
| 2026-08-30 | `compose.components.resources` だけは commonMain に置く | 「`compose.*` は iosMain だけ」という規約の唯一の例外。リソースが commonMain にある以上ほかに置き場が無い。Android では JetBrains の別名アーティファクトが androidx へ解決するので BOM とはぶつからない（21 画面の画素比較で確認）|
| 2026-08-30 | Android の表示同一性は画素で確かめる | 文言 49 件のうち `\n` を含む 4 件と `%1$s` を含む 3 件は、Compose Resources が Android と同じに解釈する保証が無い。`pm clear` から決まった順に操作する走査を作り、移送の前後で 21 画面のスクリーンショットと `uiautomator` の dump を撮って突き合わせた |
| 2026-08-30 | 未使用の文言 12 件はそのまま運ぶ | ステップ 11 は移送であって整理ではない。消す判断は「その画面を作るのか」という要求の判断で、④ の機能見直しに属する。T-36 に登録した |
| 2026-08-30 | 固定する文言は 49 件のうち 6 件だけにする | `\n` を含む 4 件と `%1$s` を含む 3 件（重なりを除いて 6 件）は AAPT と CMP で解釈が分かれうる。残る 43 件は読み方が 1 通りしかなく、固定しても文言を変えるたびにテストを直す負債になるだけ |
| 2026-08-30 | 固定テストの期待値だけはリテラルで持つ | ほかのテストが文言を `Res.string.*` から引くのは実装と同じ正を見るためで正しいが、そのぶん値が壊れても画面とテストが同じ壊れた値を見て素通りする。リソースから引いた瞬間に自己参照に戻り、網でなくなる |
| 2026-08-30 | 段 3 のステップ 12 と 13 の境界を引き直す | 「12 = 画面、13 = ナビ基盤」の順は依存の向きと逆だった。画面 6 枚はすべて `Navigator` を引数に取り、`Screen` は `ReBuyApp.kt` の中にある。`commonMain` から `androidMain` は見えないので、ナビ基盤が先に移らないと画面は 1 枚も動かせない。ステップ数は増やさず、12 を「ナビに依存しない部品」に絞って画面をまとめて 13 へ送った |
| 2026-08-30 | `ReBuyTheme` の `dynamicColor` 分岐は `expect/actual` にせず削除する | 既定が `false` で呼び出し側も指定しておらず、一度も通っていなかった。使うと決めたときに足すほうが、通らない分岐を 2 プラットフォームぶん抱えるより読める |
| 2026-08-30 | 日付書式の期待値はロケールとタイムゾーンを固定してリテラルで書く | `DateTimeFormatter` から期待値を作ると自己参照になって網でなくなる。固定書式へ「揃える」変更と UTC 固定への変更を変異で入れ、それぞれ 1 件ずつ落ちることを確認した |
| 2026-08-30 | アイコンは JetBrains の凍結版 `material-icons-core` 1.7.3 を `commonMain` に引く | CMP に対応物が無く `compose.material3` も推移的に引かない。JB 版の android バリアントは androidx 1.7.6 を要求するだけなので、BOM を外した `androidMain` では解決版が 1.7.8 から 1.7.6 へ下がる。**1.7.6 と 1.7.8 のソースを 281 ファイル突き合わせたところ違いは著作権表記の年だけ**で、パスデータは同一だった。9 個を `ImageVector` としてコピーする案は依存ゼロで両ターゲット完全一致になるが 300〜500 行増える。drawable 化する案は `android:autoMirrored` を CMP のパーサが読むので RTL は保てるものの、パスデータを手で移す誤写か Material Symbols から取り直して別の形になるかのどちらかになる。**`material-icons-core` 自体は非推奨ではない**——「必要なものをコピーせよ」は `material-icons-extended` の話（2026-08-30 オーナー判断）|
| 2026-08-30 | `navigation3-ui` だけ JetBrains のフォークを使う | `NavDisplay` を持つこの成果物だけ androidx が iOS 向けを publish していない（`navigation3-runtime` と `lifecycle-viewmodel-navigation3` はある）。JB 版の android バリアントは androidx への依存を宣言するだけのリダイレクトなので、Android に載るものは変わらない。Google の KMP 公開が追いついたら外す |
| 2026-08-30 | `Screen` を `sealed class` から `@Serializable sealed interface` にする | 開いた多相のサブクラス登録に `subclass(Screen.Xxx::class)` が要り、そのために各ルートが独立して `@Serializable` である必要がある。`sealed interface` なら `sealedSubclasses` で列挙できるので、登録漏れの検査も書ける |
| 2026-08-30 | 登録漏れの検査は `sealedSubclasses` で列挙する | 登録側と検査側で同じ表を二重に持つと、両方に足し忘れる形の漏れを見逃す。reflection が要るので JVM 側に置くが、守っているのは iOS |
| 2026-08-30 | `LicenseScreen` は画面ごとではなく中身だけを `expect/actual` にする | 画面ごと分けると TopAppBar・戻るボタン・testTag の 20 行が 2 つの actual に重複し、片方だけ直す事故が起きる（instrumented は Android しか見ない）。プラットフォーム差があるのは AboutLibraries の読み込みだけなので、`LicenseContent(modifier)` に絞った |
| 2026-08-30 | `kotlinx-serialization` は json ではなく core を引く | `@Serializable` / `SerializersModule` / `PolymorphicSerializer` はすべて core の API で、`kotlinx.serialization.json` の import はリポジトリに 1 つも無い。保存形式は savedstate が決める |
| 2026-08-30 | 「登録漏れは iOS だけで出る」という前提を捨てる | Android を救っていたのは `NavKeySerializer` の reflection 経路だけで、ステップ 13 でそれを使うのをやめたので差が消えた。登録を落として instrumented を回すと Android でも `SerializationException` で落ちることを実測。**壊れ方が揃ったぶん Android 側に網を張れる**ようになったので、instrumented に復元テストを 3 件置いた |
| 2026-08-30 | ライセンス一覧の元データはコミットする | AboutLibraries も SCM に入れる運用を挙げている。Room のスキーマと同じ扱いで、**依存を足し引きしたときの一覧の変化が diff に出る**のが利点。1 行 88KB では読めないので `prettyPrint` を入れて 136KB にした。コミットしてもタスク依存は要る——生成物がリソースのソースディレクトリの中に出るので、読む側に `dependsOn("exportLibraryDefinitions")` を明示する |
| 2026-08-30 | 生成物を `build/` へ逃がす案は取れなかった | `compose.resources { customDirectory(...) }` は既定のディレクトリを**足すのではなく置き換える**（CMP プラグインの `?:`）。`commonMain` に指定した瞬間に文言 49 件と drawable 3 件が一斉に `Unresolved reference` になった。生成物をソースツリーの外に置く道はここで閉じている |
| 2026-08-30 | 収集は `filterVariants` で `android` に絞る | 絞らないと 139 件になり、Android に載らない 5 件（`skiko` / `ui-uikit` / `kotlinx-browser` / `atomicfu` / `kotlinx-datetime`）が混ざる。`:androidApp` の `debugRuntimeClasspath` に 1 つも無いものを表示するのは正しくない。**バリアント名（`androidMain`）で書くと 0 件に戻る**ことは変異で実測した。iOS 側の欠落は T-39 |
| 2026-08-30 | `androidResources { enable = true }` は R を引かなくなっても外さない | `:shared:ui` の Kotlin から `R` を引く場所は無くなったが、**Compose Resources は Android では assets 経由で載り、CMP はその配線を `variant.sources.assets` に繋いでいる**。無効にすると assets ごと消え、ビルドは緑・APK に `assets/composeResources/` が 1 件も入らない・起動して全画面が落ちる。実際に踏んだ |
| 2026-08-30 | 一覧が空になる退行を instrumented で止める | ステップ 5 から 9 ステップぶん、この退行を検出できるのは「人が実機で一覧を見ること」だけだった。リソースを画面と同じ経路で読んで件数を見る 1 件と、実際に画面へ遷移して 1 行目が描かれるまで待つ 1 件を置いた。**リソースが読めることと画面に届いていることは別**なので 2 件に分けている |
| 2026-08-30 | ライセンス一覧の網は件数だけでなく中身も見る | 収集の壊れ方は 2 方向ある。**絞りすぎると 0 件**（Kotlin ターゲット名ではなく AGP のバリアント名で `filterVariants` を書いた場合）、**絞らないと Android に無いものが混ざる**（`skiko` や `ui-uikit` がメタデータ解決から入る）。件数の下限は前者しか止められないので、Android の classpath からしか出ない依存（`koin-android` / `room-runtime`）が入っていることと、`skiko` / `uikit` が入っていないことを別々に見る。厳密な件数は書かない——依存を足すたびに数字を書き換えるだけになって網でなくなる |
| 2026-08-30 | リソースのパスは実装側の定数を公開してテストに使わせる | `Res.readBytes` は文字列で引くのでタイプミスをコンパイラが止められない。テスト側にパスを書き写すと、書き写しどうしが一致するだけで実装の誤りを止められない。**キーで引いた先に物があるかを見るテスト**なので、値そのものの正しさを見る `StringResourceFormatTest`（リテラルで持つ）とは逆の判断になる |
| 2026-08-30 | `data object` の分岐は `is` ではなく等値で書く | iOS で `is <data object>` が、分岐に `@Composable` 呼び出しを含む `when` の中で一致しない。`tab is HomeTab.All` が `true` を返した直後に同じ `tab` を見る `when` が `NoWhenBranchMatchedException` を投げる。Android では起きず、Android のテストでは捕まらない。`if/else` でも回避できるが、**`data object` は singleton なので等値のほうがそもそも読みやすい**。上流への報告は T-41 |
| 2026-08-30 | iOS の画面確認に macOS の合成クリックを使わない | `osascript` で Simulator を前面に出してから固定座標をクリックする方法は、**その間に人が別のウィンドウを触るとフォーカスが移り、クリックが他のアプリに飛ぶ**（実際に Chrome へ飛んだ）。誤操作の危険があり、成否も安定しない。前面が Simulator かを確かめてから打つ形にして今回はしのいだが、恒久策は別に立てる（T-42） |
| 2026-08-30 | T-35 を先に置かずにステップ 15 へ進んだ | T-35 の着手条件は「ステップ 15 の前」だったが、置かないまま進めた。ステップ 15 は **iOS で初めて実物が動くかどうか**が焦点で、動く前に書いた網は「落ちる理由」を先回りして当てにいくことになる。実際に動かしてみて、DB の往復（カテゴリー追加・削除）もシミュレータで通った。**代わりに分かったのは、iOS には画面を触る手段が何も無いこと**——T-42 を立て、T-35 と地続きの課題として扱う。着手条件を満たさずに進んだ逸脱として記録する |
| 2026-08-30 | CI の `build` はタスクの明示列挙にせず、macOS の `ios` ジョブを足す | §11 の 1 は「`./gradlew build` からタスクの明示列挙へ」と書いていたが、列挙はモジュールやターゲットを足すたびに追随が要り、**書き忘れがまた同じ「緑なのに通っていない」を生む**。`ios` ジョブが `iosSimulatorArm64Test` と `xcodebuild` を回せば、Linux の `build` が iOS を素通りしても穴は残らない。`xcodebuild` まで回すのは Swift 側（`iOSApp.swift` / `ContentView.swift`）を見るのが Xcode しか無いため |
| 2026-08-30 | T-32 の検査はテストタスクの `doLast` ではなく `finalizedBy` に置く | source set が外れると **テストタスクが `NO-SOURCE` で飛び、`doLast` も走らずに緑になる**——止めたい形がそのまま素通りする（実測）。`finalizedBy` なら飛んだときも走る。さらに `build/test-results` を丸ごと数える形も駄目で、**別のタスクが前に残した XML で通ってしまう**（これも実測）。タスクごとに自分の出力だけを見る形にした。**さらに iOS の検査は macOS のときだけ回す**——Linux では Kotlin/Native のテストタスクが無効化されるので、検査だけが走って 0 件と判定する。落とし穴 19 を検査の側で踏んだ（CI で実測） |
| 2026-08-30 | `ARCHS = arm64` を xcconfig に書く | CI の `xcodebuild -destination 'generic/platform=iOS Simulator'` が x86_64 も要求し、Gradle 側が `Unknown iOS simulator arch: 'x86_64'` で落ちた。**Kotlin 側は `iosArm64` と `iosSimulatorArm64` の 2 つしか作っていない**（iosX64 を落とした判断は同日の別行）ので、制約は CI のオプションではなくプロジェクトの設定に書くのが正しい。ローカルで同じ destination を使ったときも同様に落ちる |
| 2026-08-30 | `ios` ジョブは分けず、アプリビルドを常に走らせる | Android の `build` と `instrumented` は別々の仕事をしているので分ける利得が出るが、**iOS のテストとアプリビルドは Kotlin/Native のコンパイルを共有している**。実測でテスト 496 秒・アプリビルド 44 秒——44 秒で済むのは前段がコンパイルを終えているから。ジョブを分けると実時間は 1 分縮むだけでランナー消費が 55% 増える（しかも macOS 枠）。「1 回の実行で Kotlin 側と Swift 側の両方の結果が欲しい」という要求は、`if: !cancelled()` で 44 秒足すだけで満たせる。ワークフロー単位の分割は T-43 |
| 2026-08-30 | iOS 関連 5 件の順序を T-39 → T-42 → T-35 → T-44 → T-41 にする | 段 3 が終わって iOS の案件が 5 件たまったので順序を決めた（オーナー判断）。**T-39 を先頭に置く**のは、小さく自己完結していて Android 側の網が既にあり、機構が無くても着手できるため。**T-42 を段 4（T-44）より前に置く**のは、段 4 が iOS だけを触る変更で **Android のテストが 1 件も網にならない**ため——段 3 では iOS 固有の不具合を人が偶然踏んで見つけている。T-35 は T-42 で決めた機構によって置き場所が変わるのでその後。**T-41 は最後**——回避済みで実装が動かず、上流へ投げるだけ |
| 2026-08-30 | 着手順に合わせた T 番号の付け直しはしない | 「まだ着手していないので番号を順序に揃えたい」という提起があったが、**`T-35` / `T-41` / `T-42` はすでにマージ済みコミットのメッセージから参照されている**（各 2 件）。付け直すと、書き換えられない履歴が存在しない番号を指す。番号は動かさず、一覧の直後に着手順を 1 ブロック置く形にした |
| 2026-08-30 | T-32 の 0 件検査を source set 単位に作り直す | ステップ 16a のレビューで、**T-32 が動機とした 2 つの事故のどちらも塞げていない**ことを 2 人のレビュアーが独立に指摘した。(a) タスク単位の合計なので `commonTest` の 106 件が消えても `androidHostTest` の数件で緑になる——これは T-32 の着手条件が名指ししていた形そのもの、(b) `finalizedBy` だけなので `withHostTest {}` の入れ忘れでタスクごと消えると検査も消える。source set 単位の突き合わせに変え、`check` にも繋いだ。あわせて `tests` が `@Ignore` を数える件も直した（`skipped` を差し引く）。**変異で 3 通り確かめてから緑にしたつもりだったが、変異の当て先が足りていなかった** |
| 2026-08-30 | レビューを飛ばして 16a をマージした | ステップ 16a は「docs とツール整備だから軽微」と自分で判断してレビューを起動せずにマージした。実際には T-32 という判断の要るコードが入っており、上の行のとおり 2 つの穴があった。**CLAUDE.md の起動契機は「差分が何に触れたか」で引くと定めてあり、`build-logic` は本番コードではないが判断の要るコードである**。軽微かどうかを行数や種別で決めない |
| 2026-08-31 | iOS 関連 4 件を ④ の前にまとめ、段 4（T-44）は ④ の後に回す | 順序を T-42 → T-35 → T-39 → ④ → T-44 にした（オーナー判断）。**段 4 は利用者から見た価値を生まない**（iOS を配るのは ⑤）のに対し、④ は「自分で使い始める」で終わる。④ で画面構成が変われば外枠は作り直しにもなる。**代わりに網（T-42）を ④ より前に置く**——④ で作る機能は `commonMain` に入るのでそのまま iOS にも載り、網が無いまま積むと iOS 固有の壊れ方が溜まる。**「実装に入る前にやる」という約束にしない**のは、T-35 の着手条件が実際に滑ったから。先に置けば約束ではなく事実になる。T-35 を T-42 の直後に置いたのは機構の記憶が新しいうちがいちばん安いため、T-39 をその後に置いたのは T-42 の網がある状態で iOS 側の一覧を確かめられるため |
| 2026-08-31 | iOS の画面テストは `commonTest` ではなく `iosTest` に置く | `commonTest` に置くと Android 向けにもコンパイルされ、Android フレームワークの無い素の JVM（`testAndroidHostTest`）で実行されて `android.os.Build.FINGERPRINT` が null で落ちる（実測）。Robolectric が要るが、有効化は `@RunWith` という JUnit 4 のアノテーションなので `commonTest` には書けない。Gradle のタスク側でクラス名を弾く手もあるが、**除外がソースから見えず名前の一致で効く**ので、名前を変えた瞬間に黙って外れる——T-32 で塞いだ「0 件のまま緑」と同じ形になる。`withDeviceTest {}` を開けて Android は GMD へ回せば `commonTest` を嘘にせずに済むが、`:androidApp` の instrumented が既に同じ経路を見ているので得るものが薄い |
| 2026-08-31 | iOS の網は (c) `runComposeUiTest` にし、(d) Maestro は採らないが候補には残す | 実測で 4 つ分かった。(1) CMP 1.12.0 の iOS はアクセシビリティ階層を出し、`testTag` が `AXIdentifier` に対応づく（Maestro の issue #1549 は解消済み）。(2) `runComposeUiTest` はテストバイナリの中で Compose Resources を読め、`initKoin()` が動き、`ReBuyApp()` を丸ごと描ける——Koin から Room までの実物が組み上がる。(3) CI は `ios` ジョブが `iosSimulatorArm64Test` を回しているので追加コストがゼロ。(4) 文言を `Res.string.*` から引ける。(d) は「外から動かす」枠として残り、そこが要るときは Swift を書く (b) より安い |
| 2026-08-31 | 落とし穴 22 は現状のコードでは再現しない | T-42 の着手時に `HomePagerTabList` の 2 か所を `is HomeTab.All` に戻して変異させたが、`iosSimulatorArm64Test` のテストバイナリでも**シミュレータで動かした実物のアプリでも**落ちなかった。空状態が描かれていることは診断用テストで確認済みなので、その `when` は通っている。踏んだときと Kotlin も CMP も同じ版なので、再現には別の条件が要る。3 か所目の `HomeListItemRow` は品目が 1 件以上ないと描かれず、iOS のテストは空の DB でしか走れないので確かめられていない。**この網が落とし穴 22 を止める、とは言えない**——T-41 の着手条件に「まず最小再現を作る」を足した |
| 2026-08-31 | iOS のテスト API は v2 を使う | `androidx.compose.ui.test.runComposeUiTest` は非推奨で、`androidx.compose.ui.test.v2.runComposeUiTest` を勧められる（`verifier` が拾った。メインセッションは警告行を捨てて実行していて見落としていた）。v2 は同じ `ComposeUiTest` インターフェースを使い、既定のディスパッチャが `StandardTestDispatcher` になる——**本番に近い**（コルーチンが即時実行されず積まれる）。import を差し替えるだけで 8 件とも通ったので移行した |
| 2026-08-31 | iOS では「空状態のボタンから遷移」を自動化できる | テスト戦略定義書 §6 はこれを「DB にデータが要る 2 経路」として手動確認に残していたが、**それは Android の事情**（本物の `ReBuyApplication` が本番 DB を読むので空状態を保証できない）。iOS のテストは自分で Koin を立て、DB は必ず空から始まるので**いま書ける**。空状態の文言・ボタンと、そこからアイテム一覧への遷移の 2 件を置いた。この 2 件は同時に**「DB が空」という前提の番人**でもある——タイトルしか見ないテストは品目が入っていても全部緑で通ってしまう |
| 2026-08-31 | タブごとの履歴保持は UI から到達できないので網を張らない | `NavigationState.stacksInUse` の 2 要素の分岐を UI 経由で見るテストを書こうとして、書けないことが分かった。**ボトムナビを出すのはトップレベルの 2 画面だけ**なので、ホームのスタックに設定を積んだ状態からタブを切り替える操作が存在しない。変異（`listOf(startRoute, topLevelRoute)` → `listOf(topLevelRoute)`）を当てると `NavigatorTest` だけが落ち、Android・iOS の UI テストは 1 件も落ちない。**到達できない以上、網が無いことは穴ではない**ので案件を立てず、テスト戦略定義書 §6 に事実として書いた |
| 2026-08-31 | iOS の DB 差し替えは T-21 では賄えないので T-48 を立てる | §6 は当初 iOS の「DB が空しか見られない」穴を T-21 に向けていたが、T-21 の対応方針は `loadKoinModules` で本物の `ReBuyApplication` が起動した Koin を差し替える **Android 限定の手**で、テスト自身が `startKoin` する iOS には当てはまらない。`.app` の穴には T-46 という能動的なエントリが立っているのに DB の穴だけ閉じた案件を指していたので、非対称を解消した |
| 2026-08-31 | iOS の画面テストは本物の Room を使わず DAO を FakeDatabase に差し替える | PR #28 の `ios` ジョブで **8 件すべてが `Unable to open database` で落ちた**。テストバイナリはアプリのサンドボックスではなく**シミュレータ共有のデータ領域**で動くので、新品のシミュレータには `data/Documents` が無い。手元で緑だったのは過去の実行がディレクトリを作っていたから。本番の `create = false`（Documents は OS が必ず用意するので黙って作らない）は**アプリでは正しい**ので触らず、テスト側が持ち場を持つ形にした。Koin 起動の直後に `loadModules(..., allowOverride = true)` で `ItemDao` / `CategoryDao` を差し替える。**`allowOverride(false)` は起動時の `modules()` にしか効かない**ので本番の方針は緩んでいない（Koin 4.1.0 のソースで確認）。**差し替えは `setContent` より前でなければ効かない**——`AppDatabase` → DAO → Repository はすべて `single` で、上書きは「これから作るもの」にしか効かないため。DB ファイルを退避して「CI と同じ何も無い状態」を再現し、緑になることと DB が作られないことを実測した |
| 2026-08-31 | iOS で本物の Room が動くことは画面テストではなく T-35 が見る | 上の差し替えで `NavigationIosTest` は Room を通らなくなった。当初は「Koin から Room までの実物が組み上がる」ことを (c) の利点に挙げていたが、**画面遷移のテストが DB の実体まで抱える必要は無い**。`:shared:data` の iosTest に置くほうが、DB のパスも後片付けも制御できる |
| 2026-08-31 | 落とし穴 22 の 3 か所目はまだ踏めていない | 差し替えで品目を置けるようになり `HomeListItemRow` を初めて通したが、3 か所目の分岐（`tab == HomeTab.InBasket`）は**カゴタブにいないと通らない**。「すべて」タブでは `is HomeTab.InBasket` が正しく不一致になるだけで、バグの再現にならない（変異を当てて実測）。T-41 の最小再現はここから |
| 2026-08-31 | 落とし穴 22 は完全な条件でも再現しないと結論づけた | T-42 のレビューで「カゴタブへ行っても**ボタンの文言を assert していないので緑のまま**」と指摘され、検出器を足した——品目を `IN_SHOPPING_LIST` で置き、カゴタブを開き、`リストから削除` が出ることを見る。この状態で `if (tab == HomeTab.InBasket)` を `when (tab) { is HomeTab.InBasket -> ... }` に畳んでも**緑のまま**だった。`is` が外れれば `追加済み` を拾って落ちる形なので検出力はある。**画面の中の 3 か所すべてで否定の結果**が出たので、T-41 の最小再現は画面の外（`when` の形・分岐数・呼び出しの深さ）から当たる |
| 2026-08-31 | テストごとの戻しは「品目を置くテストを先に宣言する」ことで守る | `startTestKoin` の `seed()`（空へ戻す）を消しても、当初の宣言順では**全件緑のまま**だった——空状態を見る 2 件が、品目を置くテストより前に宣言されていたため。順番を入れ替えて後ろへ回したところ、同じ変異で 2 件が落ちるようになった。**Kotlin/Native のテストは宣言順に走る**ことに依存しているので、並べ替えるときは注意が要る |
| 2026-08-31 | 差し替えの順序はテストで守れていない | `startTestKoin()` を `setContent` の**後**へ移す変異を当てたが緑のままだった。`setContent` は即座に composition を走らせないので、後ろで呼んでも間に合ってしまう。**「`setContent` の前に置く」は約束事であって網ではない**ことを KDoc に明記した。実際の不変条件は「composition に入る前」 |
| 2026-08-31 | iOS の Room は in-memory で見て、本番のパスは見ない | T-35 の網を `shared/data/src/iosTest` に置いた。`Room.inMemoryDatabaseBuilder` は native にしか無いので `commonTest` には置けない。**本番の `NSDocumentDirectory` は通していない**——テストバイナリはアプリのサンドボックスではなくシミュレータ共有のデータ領域で動くので、そこに DB を作ると T-48 で直した問題に戻る。実ファイルを開く経路が見られるのは**実物のアプリを起動する層**だけなので、その穴は T-46 に寄せた。**生成された `actual` と driver は、壊れれば DB を組む時点で落ちるので変異を当てる先が無い** |
| 2026-08-31 | iOS の DB テストは「アプリが実際に読む経路」に合わせる | 最初に書いた 2 件は `getAllItems()` を通していたが、**アプリの 3 つの ViewModel はどれも `getAllItemsWithCategory()` を通り、`getAllItems()` はどこからも呼ばれていない**（レビューで指摘）。`@Transaction` ＋ `@Relation` は Room が別経路を生成するので、生成コードとしては別物。`categoryDao()` の生成実装も 1 度も動いていなかった。カテゴリー付きの品目を関連ごと取り出す 1 件を足し、カテゴリーが無い側（`@Relation` の非マッチ）も同じ件で見るようにした |
| 2026-08-31 | 「null のほうは変換を通らない」は誤りだった | 変異（`InstantConverter.toInstant` を 1 ミリ秒ずらす）で `lastBoughtAt` が null のテストが落ちなかったのを「変換を通らないから」と書いたが、**`createdAt` / `updatedAt` は非 null なので通っている**。通らないのではなく**見ていなかった**だけ。生成時刻を固定して両方の件で往復を assert するように直した。**変異が落ちない理由を、確かめずに説明してはいけない** |
| 2026-08-31 | 本番の DB 設定は守れていないことを実測した | `AppDatabaseIosTest` が組む DB は `DataModule.ios.kt` の設定の**複製**なので、本番側の `setDriver` を消して `databaseFilePath()` を壊しても **iOS のテストは全件緑**（実測）。守れているのは「native で Room と SQLite が動くこと」まで。テスト戦略定義書 §3 の文言もそれに合わせて書き分けた |
| 2026-08-31 | iOS の DI はローカルの Koin コンテナで見る | 本番の `platformDataModule` は iOS のどのテストでも一度も評価されておらず、`databaseFilePath()` の `checkNotNull` も Koin 定義の欠落も**起動時クラッシュでしか気づけなかった**。`koinApplication { modules(dataModule) }` のローカルコンテナで解決するだけの 1 件を置いた。**global な `startKoin` を使わない**のは、同じプロセスで走る `:shared:ui` の `iosTest` が global を差し替えているため。**クエリは 1 本も投げない**——Room は最初のクエリで接続を開くので、投げると T-48 で直した問題に戻る。実際に DB ファイルが作られないことを実行後に確認した。変異 2 本（パスの `checkNotNull` を壊す・`dataModule` から `ItemDao` の定義を消す）でどちらも落ちる |
| 2026-08-31 | T-21 の差し替えは Android のほうが条件が厳しいと分かった | T-48 で iOS の DB 差し替えを入れたとき、機構（`loadModules` に `allowOverride = true` を渡す）と制約（**上書きは「これから作るもの」にしか効かない**）の両方が実測で分かった。iOS はテスト自身が Koin を起動するので解決前に差し込めたが、**Android は本番の `Application` がプロセス起動時に Koin を立て、`createAndroidComposeRule<MainActivity>()` が Activity を起動してからテスト本体へ入る**。T-21 の対応方針（テストで `loadKoinModules`）がそのままでは成立しない可能性がある。**composition がどこまで進むかは未実測**なので、着手時に最初に測ることを T-21 に書いた |
| 2026-08-31 | T-39 は上流待ちにする。2 案とも原理的に成立しない（**同日撤回**——すぐ下のエントリ） | `collect { includeTargets = true }` は `targets` に `android`(134) と `metadata`(75) しか出さず、`filterVariants` に `iosSimulatorArm64` を渡すと 0 件（どちらも実測）。**原因はプラグインが収集対象に選ぶ設定名で、`CompileClasspath` と `RuntimeClasspath` しか見ていない**（クラスの文字列定数で確認）。Kotlin/Native は `iosSimulatorArm64CompileKlibraries` を使うので一致しない。上流のドキュメントは `"iosArm64" in it.targets` という使い方を示しているが**実装が追いついていない**。利用側の設定では回避できないので、報告の下書きだけ `notes/`（git 追跡外・採番なし）に残して上流待ちにした。**絞りを外して 139 件を両方に出す案は採らない**——足りない側は埋まるが提供していないものまで出るうえ、139 が iOS の真の集合の上位集合である保証が無い（native 固有の推移依存は収集されていない） |
| 2026-08-31 | 「T-39 は利用側では回避できない」を撤回し、`collect { all = true }` で実装する | 再調査で名前照合の**手前の分岐**を見落としていたと判明。`collect { all = true }`（15.0.4 からある）は解決可能な全構成を対象にし、`filterVariants` が**部分一致**に変わるので klib 構成を名指しで拾える。ターゲット名は Kotlin ターゲットモデル（`compileDependencyConfigurationName` → ターゲット名）から引かれ、klib 構成にも正しく付く。上流の `MergePlatformArtifactsFunctionalTest` が**まさにこのレシピで native の `targets` を assert しており**、意図された経路。この repo でも実測: 138 件、`android` 134 / `iosArm64` 72 / `iosSimulatorArm64` 72、ラベル無し 0・ライセンス欠け 0。上流への報告は不要になり `notes/` の下書きは調査記録に書き換えた。ついでに、当初「iOS で欠ける 5 件」と数えた `kotlinx-browser` は **iOS のターゲットに載っていない**ことも判明（metadata 解決の混入。正しくは 4 件） |
| 2026-08-31 | ライセンス一覧は単一 json のまま、表示時にプラットフォームで絞る | export を 2 回回して json を出し分けるより、`includeTargets = true` で `targets` を持たせて実行時に絞るほうが、生成物とコミット差分が 1 か所に収まり、上流ドキュメントの示す使い方（`filter { "iosArm64" in it.targets }`）とも一致する。絞りは接頭辞（Android = `android`、iOS = `ios`——実機とシミュレータの 2 ターゲットをまとめる）。**`targets` が空の entry は表示に倒す**——`configPath` で手足しする entry には `targets` が付かないので、絞ると黙って一覧から消える方向の事故になる。R.raw へ出す変則経路は `all = true` でも空のまま（variant 名での後段フィルタが `LibraryPostProcessor` に残るため）を確認した |
| 2026-08-31 | T-39 の網から導出可能な 2 本を落とす | オーナーが「網が過剰では」と提起し、同じ PR で削った。落としたのは (a) instrumented の「Android の画面に渡る一覧」——リソースの `targets` 検証（同クラス）と絞りの規則（commonTest）から帰結が導出でき、結合部（`platformTargetPrefix` の actual）も commonTest が Android ホストで走ることで守られている。(b) iosTest の「全件にライセンス」——instrumented が同じ json の全件を見ており完全な部分集合。残した 4＋2＋1 本はそれぞれ固有の不可視な壊れ方（パッケージング・収集設定・素通し化・画面が絞りを通すこと）に対応する |
| 2026-09-01 | 破壊的マイグレーションを外す作業（T-51）は 23 に置く | 成果物は Migration とそのテストで、目的は「版が上がってもユーザーから見た状態が変わらないこと」。挙動を変えるための活動ではないので要求軸（22）ではなくこちらに置いた。仕様の条項はデータモデル定義書 §8 が持ち、T-51 はその期限付きの実行だけを持つ |
| 2026-09-02 | 署名の検査は python3 で書き、値は組み立てて出す | 最初は sh ＋ grep/sed で書いたが、レビューで 3 種類の穴が出た。**サブシェルの `exit` は呼び出し元を止めない**（コマンド置換の中で走るので、対象が読めなくても合格で終わっていた）／**`sed` で行から値を消す引き算式は、値に `=` があるときと 1 行に 2 設定あるときに消し残る**（CI ログは public に残るので取り返しがつかない）／**2 つの正規表現をコピペすると片方だけ枝が欠ける**。いずれも `docs-check.sh` と同じ `exec python3 - "$@"` の形にすると構造的に消える。伏せ字は行から値を消すのではなく、**行番号と設定名だけを組み立てて出す**——閉じた集合からしか作らないので、新しい書き方が来ても漏れない |
| 2026-09-02 | 検査対象は列挙で決め、コメントの中も見る | 対象を定数で持つと、置き場所が増えた日に黙って網が縮む（`Debug.xcconfig`・`ExportOptions.plist`・リポジトリ直下の xcconfig が実測で素通りした）。`git ls-files` の列挙に変え、拡張子で読み方を分ける。**xcconfig の `//` 以降を落とす処理は入れない**——誤検知（設定名を含む説明文）を消すために入れたが、`// 例: DEVELOPMENT_TEAM = <実値>` が素通りする穴と引き換えだった。public リポジトリでは fail-closed に倒し、こちらのコメントを「設定名に `=` を続けない」書き方に直した |
| 2026-09-02 | 検査の検査は持たない | テーブル駆動の検査（`scripts/test/`）を一度書いたが、オーナーの判断で捨てた——「検査の検査はキリが無い」。網の網を持つと、そちらも育てて壊れないよう見る対象になる。代わりに**網を変えたその場で変異させて確かめ、結果をコミットメッセージに残す**。正常系はリポジトリの現状そのものなので、正規表現から 1 語落ちても緑のままで、変えたことを何も示さない |
| 2026-09-02 | 塞げない 2 経路は起票して限界を明記する | この検査が見るのは作業ツリーと index だけなので、**汚す commit → 消す commit** を積むと履歴に値が残る（[T-57](./23_技術改善バックログ.md#t-57)）。また pre-commit は `core.hooksPath` を打った人にしか回らず、打ち忘れは**エラーも出さずに無効**になる（[T-56](./23_技術改善バックログ.md#t-56)）。どちらも「入れたことで安心しない」ために、CI は push 後の検知にしかならない＝**予防はフックにしかできない**ことを起票文と [git 運用定義書](../仕様/16_git運用定義書.md) §1.4 の両方に書いた |
| 2026-09-02 | 署名の検査を変異で確かめた記録 | 使い捨てリポジトリで 27 形式を作業ツリー・index の両モードで回し、鳴るべき 14 件と通すべき 13 件がすべて一致・値の露出 0 件を実測した。前巡で素通りしたリポジトリ直下の xcconfig・ExportOptions.plist・entitlements・追跡されたプロファイルが鳴ること、対象の欠落と Local.xcconfig の追跡が同時に報告されることも確かめた。網の各要素は変異させた——`CODE_SIGN_STYLE` を網から外す／参照の判定を先頭 1 文字に戻す／コメントの扱いを変える／対象を定数 2 本に戻す、のいずれでも**狙ったケースだけ**が落ちる。実リポジトリで回した時点で回帰が 1 件出た（`\s*` が改行をまたぎ、空値のキーが次の行を値として拾う。全文走査へ変えたときに入った）——**正常系だけでは何も示せない**ことの実例 |
| 2026-09-02 | 署名の網を「値の形」から「ファイルの形」へ広げた | 5 巡目のレビューで、**条件付きキーが 2 つ並ぶ形**（`[sdk=…][arch=…]`）が pbxproj・xcconfig とも 1 件もマッチしないと分かった。条件 1 つなら鳴るので、変異でも全形式テストでも差が出ない種類の穴。あわせて、拡張子の照合だけが大文字小文字を区別していた（中身は `IGNORECASE` なのに `D.XCCONFIG` が素通り。macOS の既定では書けてしまう）。範囲も広げた——`.p8`（App Store Connect の API 鍵）・`.pem`・`.key`、旧形式（ASCII plist）の `キー = 値;`、entitlements の `<array>` 値、`.xcarchive/Info.plist` の `Team` / `SigningIdentity`。**`teamID` は `TEAM_ID` と一致しない**（大文字小文字を無視してもアンダースコアが違う）ので、plist にはキー集合を分けて当てる |
| 2026-09-02 | コメントを走査する方針の代償を、値の側だけ切ることで払った | 行ごと `//` を落とすと `// 例: DEVELOPMENT_TEAM = 実値` を見逃す。一方で落とさないと `DEVELOPMENT_TEAM = $(TEAM_ID)  // 説明` という**正当な xcconfig** が鳴る。**値の側だけを `//` で切る**と両立する——コメント行のほうは `//` の後ろから一致するので影響を受けない。Config.xcconfig を編集するときの規律（コメントで設定名の直後に `=` を書かない）はスクリプトのヘッダに書いた |
| 2026-09-02 | CI 側の網を厚くすることには投資しない | レビューで「CI は PR と main への push でしか走らないので、トピックブランチを push しただけの状態では 1 度も鳴らない」と分かり、全ブランチの push で回す専用ワークフローを一度入れた。**オーナーの判断で取り下げ**——CI は原理的に予防できず（public では push した時点で公開済み）、埋まるのは「push したが PR を開いていない数分」だけ。代償（全 push に 1 ジョブ、ワークフロー 1 本の維持）に見合わない。残る穴は 16_git運用定義書 §1.4 に明記した |
| 2026-09-02 | 網を広げた変更が、それ自身で 2 つの穴を作っていた | 6 巡目のレビューで発覚。(a) **拡張子の照合を小文字に落とす**変更を入れたのに、定数側の `.certSigningRequest` だけ camelCase のまま残り、**どんなパスにも一致しなくなっていた**（小文字化する前の素の `endswith` なら拾えていた）。(b) **配列を読む**変更を入れたが最初の `<string>` しか見ておらず、しかも 1 要素目は Xcode が自動で書く `$(AppIdentifierPrefix)<bundle id>`（＝許可される値）なので、**2 要素目以降が常に遮蔽される**。どちらも「塞げていないと分かっている穴」ではなく「塞げたと思っている穴」で、記録（この log とスクリプトのヘッダ）が実装と食い違っていた。**網を広げたら、広げた先が本当に効くかを個別に壊して確かめる** |
| 2026-09-02 | 許可リストの bundle id はファイルから読み、plist にだけ効かせる | entitlements の `$(AppIdentifierPrefix)<bundle id>` は参照を畳むと bundle id だけが残るので、通す口が要る。ただし (a) 手で写すと ⑤ で逆ドメインを変えた日にここだけ取り残されるので **Config.xcconfig の `BUNDLE_ID` から読む**、(b) キーもファイルも見ずに通すと `DEVELOPMENT_TEAM = <bundle id>` まで素通りするので **plist / entitlements にだけ効かせる**。App Group（`group.<bundle id>`）と App Extension（`<bundle id>.<接尾>`）も同じ理由で通す——ここを塞ぐと capability を足した日に xcconfig へ移せない値で詰まり、`--no-verify` を覚える方向に効く |
| 2026-09-02 | 接頭辞付きの自作キーも見る。コメントは行頭のものだけ見る | キー照合の先読みが `_` を拒んでいたため、`MY_TEAM_ID` に実値を入れて `DEVELOPMENT_TEAM = $(MY_TEAM_ID)` と書けば網を抜けられた。誤検知の側に倒す方針と食い違うので `_` を許した（`APP_DEVELOPMENT_TEAM_NOTE` のような別物は、キーの直後に `=` を要求するので当たらない）。一方コメントは、**行頭からのもの（`// KEY = 実値`）だけを見て、代入の後ろの行末コメントは見ない**。両方見ると正当な行末コメントが全部鳴る。ヘッダの宣言が「コメントの中も見る」だけだと後者を約束したように読めるので、範囲を明記した |
| 2026-09-02 | 検査を sh の中の python から Python ファイルへ出す | オーナーの指示。`docs-check.sh` / `ledger-move.sh` に倣って `sh` ＋ `exec python3 - "$@" <<'PY'` の形にしていたが、中身が 400 行の Python になった時点で殻が読みにくさにしかなっていない。`scripts/check-ios-signing.py` として単体で持ち、リポジトリルートへの移動も Python 側で行う。呼び出しは `python3 scripts/check-ios-signing.py [--staged]` に統一（pre-commit・CI・CLAUDE.md・16・17） |
| 2026-09-02 | 許可リストを作る側が新しい穴を作っていた | 6 巡目で入れた `bundle_id()` は、`^BUNDLE_ID[ \t]*=[ \t]*(\S+)` という**第 2 の xcconfig パーサ**だったため、`BUNDLE_ID[config=Debug] = …` も `$(inherited)` 前置も読めず、読めなかったときは**理由を捨てて**いた。結果は「正しい entitlements が鳴り、直し方も誤って案内される」で、`git()` の docstring に自分で書いた原則（握ると異常が合格に見える）の裏返し——**握った結果、異常だから鳴り続ける**。既存の `setting()` で読み、読めなければ 1 件の指摘として言う形にした。同じく 6 巡目の `<array>` 対応も、自己閉じ `<array/>` を中身ありと見なして次の配列を飲み込み（誤検知）、入れ子の `</array>` で早く切れて兄弟要素を見落とす（偽陰性）状態だった。**開き閉じを数える**形に置き換えた |
| 2026-09-02 | 3 つのリストは揃えて持ち、CLAUDE.md からは列挙を外す | 署名の実体は `.gitignore`・`.claude/settings.json` の deny・スクリプトの `MATERIAL_SUFFIXES` の 3 か所に並ぶ。今回 deny だけが追随しておらず（CLAUDE.md は「deny で読み取りも塞いである」と宣言していた）、`Local.xcconfig`——この案件が一本化した秘密そのもの——も deny に無かった。拡張子を持たない `keystore.properties` はスクリプトだけ落ちていた。CLAUDE.md の本文からは拡張子の列挙を外し、**全量は `.gitignore` と deny が正**と書いた——上位文書に写しを置くと、次に足す人が 4 か所を思い出せない |
| 2026-09-02 | 検出用のパターンを解決に流用してはいけない | `bundle_id()` を 7 巡目に `setting()` で書き直したところ、**許可リストが乗っ取られる**ようになった（8 巡目のレビューで実測）。`setting()` は検出用なので過剰一致が安全側だが、解決では過剰一致がそのまま誤検知の量産になる——**polarity が逆**。実測では `WIDGET_BUNDLE_ID` を 1 行足すだけで、本物の漏洩が黙り、正しい `application-identifier` が鳴った。条件付きキー `BUNDLE_ID[config=Debug]` は 6 巡目の素朴な `^BUNDLE_ID[ \t]*=` なら正しく飛ばせていたので、7 巡目で悪化していた。解決用は行頭に錨を打ち、条件も接頭辞も許さず、**一意に決まらなければ指摘として出す**形にした |
| 2026-09-02 | 緩和はキーに紐づける | 「フラグしか無い値は通す」を全キーに掛けたため、`--keychain=/Users/<名前>/…`（`=` でくっついた 1 トークン）が素通りし、`DEVELOPMENT_TEAM = -<値>` も通っていた。**空白で区切れば鳴るのに `=` で書くと消える**——同じ意味の 2 つの書き方で結果が割れる網は信用できない。緩和を `OTHER_CODE_SIGN_FLAGS` に限り、トークンも「`-` ＋小文字英数字だけ」に絞った |
| 2026-09-02 | 読めなかったことは、値が無いことと区別して言う | `array_span()` が対応を取れないとき黙ってそのキーを飛ばしていた（コメント中の `<array>`・空白入りの閉じタグ・閉じていない XML）。これは 7 巡目に `bundle_id()` で潰したアンチパターンと同型で、**この検査で唯一の fail-open** だった。深さを数えるときだけコメントを空白で覆い（長さと改行を保つのでオフセットがずれない）、それでも対応が取れなければ「中を検査できていません」と出す。**値の走査にはコメントを覆ったテキストを使わない**——コメントに書いた実値は鳴らす |
| 2026-09-02 | 網を動機の範囲へ戻し、fixture を置く | オーナーの判断。**守るのは「Xcode がプロジェクトを開いた拍子に pbxproj へ焼き込む署名設定をコミットさせない」ことだけ**で、それを越える防御は今は張らない。plist / entitlements（capability を足したときや archive したときの産物）と、そのために足した `bundle_id()`・`array_span()`・許可リストの bundle 派生を丸ごと落とした——**8 巡目に出た高 2 件はどちらもその部分**で、網を広げた変更がそれ自身で穴を作る形が 3 巡続いていた。あわせて「検査の検査は持たない」（2026-09-01）を今回だけ改め、`scripts/test/check-ios-signing_test.py` を置く。**ケースも動機の範囲に留める**——見ないと決めた形はテストにも置かない |
| 2026-09-02 | 網を絞った直後に、実際の焼き込みを検出した | 絞った検査を実リポジトリで回したところ、`project.pbxproj` に Team ID と `CODE_SIGN_STYLE` が入っていた（Debug と Release の 2 構成に 4 行ずつ。他の編集は無し）。**作業ツリーだけの変更で index には入っておらず**、コミットもされていなかったので公開はされていない。`git restore` で戻した。pre-commit が鳴らなかったのは設計どおり——見るのは index で、`git add` していない変更はコミットに入らない。**ただし作業ツリーに置きっぱなしの間は誰も鳴らない**（CI の作業ツリーモードは push 後）。Xcode を開くたびに黙って焼き込まれ続けることは、運用として知っておく |
| 2026-09-02 | 動機の外のキーも落とす | `CODE_SIGN_KEYCHAIN` と `OTHER_CODE_SIGN_FLAGS` は**人が手で書くもので、Xcode が開いた拍子には書かない**。9 巡目のレビューで気づき、範囲を絞る判断に合わせて網から外した。道連れに「裸のフラグしか無い値は通す」という緩和（`BARE_FLAG`）とその 2 ケースも消え、8 巡目に見つかった `--flag=値` の穴ごと無くなった。**網を狭めると、その網のための例外も一緒に消える** |
| 2026-09-02 | `xcuserdata/` の追跡も見る | 動機のど真ん中を取りこぼしていた。Xcode が**開いただけで**書き、ディレクトリ名に macOS のアカウント名が入る。`Local.xcconfig` については「`.gitignore` は `git add -f` を止められない」と明記して追跡チェックを置いていたのに、まったく同じ論法が当たるこちらは `.gitignore` 頼りだった。あわせて `.ipa` を `MATERIAL_SUFFIXES` へ戻した——`.gitignore` と deny にはあり、「拡張子は 3 点セットで」と明文化した当のコミットで自分が破っていた |
| 2026-09-02 | 検査の import を副作用なしにする | `if __name__ == "__main__"` を付けて「別文脈から呼べる」形を宣言しながら、モジュールの読み込み時に **argv の解釈・`os.chdir`・リポジトリ外なら import 自体が死ぬ**の 3 つが走っていた。`main(argv)` に畳み、`check(staged)` を引数化した |
| 2026-09-02 | fixture は終了コードだけでなく報告の中身を見る | 9 巡目の変異で、**指摘を 1 件も出さずに落ちる**・1 件目で打ち切る・行番号や設定名を固定値にする、のいずれもテストが緑のままだと分かった。鳴るケースでは**対象のパス・行番号・設定名・見出し**まで assert する形に変えたところ、13 個の変異すべてを捕捉するようになった（うち 5 個は直前まで生き残っていたもの） |
| 2026-09-02 | 検査を .py にしたことで `__pycache__` が入り込んだ | 10 巡目のレビューで、`scripts/__pycache__/*.pyc` がコミットに入っていると分かった。**`.pyc` にはコンパイル元の絶対パス（＝ホームディレクトリ名）が埋まる**。`sh` の中に Python を書いていた頃は生成物が出なかったので、ファイル化そのものがこの経路を初めて開いた。`.gitignore` に `__pycache__/` と `*.pyc` を足した。拾ったのは私の `git add -A` で、**焼き込みを戻さずに置いていた作業ツリーでこれをやる危うさ**をそのまま示している |
| 2026-09-02 | 検査の書式に依存するテストは、整形だけで無言の no-op になる | fixture の「3 点セット」検査が、`MATERIAL_SUFFIXES` を**ソースから正規表現で**拾っていた。1 行に畳むだけで抽出が 0 件になり、`.gitignore` を空にしても緑（実測）。`importlib` で読み込んで定数そのものを見る形に変えた——**import できるのは検査側に副作用が無いからで、この検査自体がその性質のテストにもなっている**。下限（18 件以上）と「小文字で持つ」不変条件も併せて見る |
| 2026-09-02 | 網を足すときは、鳴らない側のケースも同時に足す | `xcuserdata/` の照合を `USER_STATE in f"/{p}"` と書いたが、`USER_STATE` が `/` で始まらないため錨が効かず、`Notxcuserdata/x` を誤検知していた（実測）。正の側のケースしか無かったので気づけない。`Local.xcconfig` を固定パスで見ていた件も同様で、`.gitignore` がディレクトリを問わず効くのと網の広さがずれていた。**どちらも「鳴ってはいけない側」「別の場所に置いた側」のケースを足して初めて変異で落ちるようになった** |
| 2026-09-02 | テストのディレクトリ名を内容から作ると衝突する | 構造検査の使い捨てリポジトリ名を `名前 + 失敗件数` から作っていたため、2 つの `Local.xcconfig` のケースが同じディレクトリを再利用し、**前のケースの汚れを引き継いで誤って緑になっていた**。変異が落ちないことで気づいた。番号で必ず別のディレクトリにする |
| 2026-09-02 | deny の完全一致を消してワイルドカードに置き換えない | 8 巡目に `Bash(cat keystore.properties)` を `Bash(cat *keystore.properties)` へ「揃えた」が、Bash の規則は末尾 `*` の前方一致が基本で、先頭の `*` がグロブとして効く保証が薄い。**確実に効いていた 1 本を落とす**形になっていた。両方を並べて持つ |
| 2026-09-02 | 報告の断片を全体への部分一致で見ない | fixture が「対象パス」「行番号: 設定名」「見出し」を**stderr 全体に対して独立に**部分一致させていたため、断片が別々のブロック由来でも合格していた。`os.chdir` を落とす変異が生き残ったのがそれで、パスがルート相対に壊れているのに「欠落」ブロックのほうにパスが一致して通っていた。**対象パスで始まるブロックを 1 つ選んでから**その中で見る形に変え、サブディレクトリのケースでは「欠落が出ていないこと」まで固定した |
| 2026-09-02 | 汚れた行が末尾とは限らないケースを置く | 45 ケースが全部「clean な土台の末尾に 1 行足す」形だったので、行番号を `len(text.splitlines())`（＝総行数）にする変異が素通りした。**定数化は潰せていたが、ファイル依存の別の誤りは残っていた**。先頭に置くケースと、離れた 2 行に literal を置くケースを足した |
| 2026-09-02 | 値の露出検査は実行の共通後処理に置く | `check_case` の中だけで見ていたため、構造検査・「2 件同時」・サブディレクトリの経路が素通りしていた。実測で「追跡された Local.xcconfig の報告に中身を混ぜる」変異が生き残る——**この案件が一本化した秘密そのもの**が漏れても緑だった。`run()` の戻りで毎回見る形に移し、あわせて「直し方に破壊的なコマンドを出さない」という設計条項も同じ場所で見る |
| 2026-09-02 | 環境差はテスト側で固定する | 非 ASCII パスの引用は `core.quotepath` 次第で、手元は `false`・CI は既定の `true`。そのため `git ls-files -z` を外す変異が手元では生き残っていた。使い捨てリポジトリで `true` を明示して、CI と同じ条件で測るようにした |
| 2026-09-02 | キーの前の錨を外す（**次行で撤回。この状態は出荷していない**） | `MY_TEAM_ID` は塞いだのに `MYTEAM_ID`（アンダースコア無し）が抜けていた。どちらも「実値を別のキーに入れて参照する」同じ迂回なので、先読みごと外した。`APP_DEVELOPMENT_TEAM_NOTE` のような別物は、キーの直後に `=` を要求するので当たらない |
| 2026-09-02 | キーの前の錨を戻し、接頭辞付きの自作キーは見ないと決める | 錨を外した結果、`GCC_PREPROCESSOR_DEFINITIONS = $(inherited) HAS_TEAM_ID=1` や `-DUSE_TEAM_ID=1` のような**値の中の define が鳴る**ようになっていた（実測 3 形）。しかも報告に出る設定名がその行に存在しないので、直しようがない誤検知になる。**意図的な迂回は動機（Xcode が開いた拍子に焼き込む設定）の外**で、塞ぐ代償のほうが大きい。錨を `(?<![A-Za-z0-9_])` に戻し、鳴らない側のケースを 4 つ置いた |
| 2026-09-02 | xcconfig の値は `//` の手前で終える | 値終端が `[^\n]*` だったため 1 行 1 マッチになり、**左辺が監視対象だと行末コメントの中の実値が隠れていた**（`DEVELOPMENT_TEAM = $(TEAM_ID) // 例: DEVELOPMENT_TEAM = 実値` が素通り）。fixture の行末コメントのケースが緑だったのは左辺が監視対象外だったからで、**通っていた理由が意図と違った**。値を `//` の手前で止めると走査がそこから再開するので、コメントを捨てる処理ごと消える |
| 2026-09-02 | 3 点セットは集合の等値で見る | `.gitignore` / deny の全文への部分文字列検索だったので、**短い拡張子が長い拡張子に吸収されて**（`.cer` ⊂ `.certsigningrequest`、`.key` ⊂ `.keychain`）削除を検知できなかった。件数の下限（`< 18`。実測 19）も 1 件の脱落を素通りさせていた。`.gitignore` の署名ブロックを解析して**集合として等値比較**し、deny は規則の全体（`Bash(cat *.cer)`）と突き合わせる |
| 2026-09-02 | 使い捨てリポジトリは名前の重複で落とす | 構造検査の連番化は症状への対処で、`make_repo` が既存ディレクトリでも成功する原因は残っていた（`git init` は再初期化されるだけで前のケースの追跡ファイルが残る）。`os.makedirs` に `exist_ok` を付けず、重複を必ず落とす |
| 2026-09-02 | 失敗の帰属は引数の既定値で救わない | `run(note="")` の既定値のせいで、`note` を渡し忘れた 6 経路の失敗が `worktree:` としか出なかった。**渡し忘れが `TypeError` で必ず出る**ように必須引数にし、`run()` を通らずに直接起動していた 3 経路（`--help` / 知らない引数 / リポジトリの外）も通した |
| 2026-09-02 | `Local.xcconfig` の照合を走査除外と追跡禁止で揃える | 走査除外は完全パス・追跡禁止は basename だったため、`other/Local.xcconfig` が **2 つの見出しで報告され、片方の直し方が「Local.xcconfig へ移せ」**になっていた。`is_local()` に一本化する |
| 2026-09-02 | 誤検知時の直し方を、コメント行にも効く形にする | コメントの中の実値を鳴らすのは設計上の意図なのに、直し方は「該当行を消し、手元だけの値は Local.xcconfig へ」しか出さない。コメントに対しては噛み合わず、**docstring が警戒している「誤検知で `--no-verify` を覚える」に最も近い導線**だった。tail を「値を消す（コメントなら設定名の直後の `=` を外す）」に変え、規律そのものを `Config.xcconfig` の冒頭にも置く——編集する人が最初に見るのはそこで、それまで規律は検査スクリプトの docstring にしか無かった |
| 2026-09-02 | 検査せずに終わる経路は `parse()` の中で終了する | 11 巡目に `(staged, 終了コード)` のタプルに変えたが、「片方だけが None でない」という**型でも表明でも守られない不変条件**を docstring で宣言する形になっていた。`git()` が既に `sys.exit` を使っているので、そちらに揃える。`--help` は `sys.exit(文字列)` が stderr ＋ 終了コード 1 になるため、`print` ＋ `SystemExit(0)` に分ける |
| 2026-09-02 | `.gitignore` の節は次の見出しまでを読む | 空行で切っていたため、節の中を空行で区切っただけで「一覧のみ: 10 件」と出て、**犯人が `.gitignore` の空行だと読めない**失敗になっていた。次の `#` 見出しまでを節とし、除外（`!`）やパス付きの狭いパターンが混ざったときは「想定外の行」として別の文言で落とす |
| 2026-09-02 | 変異が生き残る領域にケースを足す | 変異テスト 130 個で生存 10 件。いずれも「実装は正しいが、その性質を固定するケースが無い」形——`//` と `/` の区別／一般名の 3 語／必須 2 本のうち xcconfig 側／サブディレクトリに置いた実体／汚れた状態での `-h`（使い方を出した後に検査まで走る実装が通っていた）／`xcuserdata` の後ろ側の境界／小文字の `local.xcconfig`／実体 2 件の並列／報告ブロックの区切り／直し方の誘導先。**種類ごとに 2 つ以上置く**とループの打ち切りとブロック分割が同時に固定できるので、「2 件同時」を「4 件同時」に広げた |
| 2026-09-02 | 使い捨てリポジトリではフックも切る | 署名と `core.quotepath` は隔離していたのに `core.hooksPath` が素通しで、**このリポジトリ自身がそれを使う**ため手元では fixture の `git commit` が落ちうる（誤って緑にはならないが原因が読めない）。`-c core.hooksPath=/dev/null` を隣に並べる |
| 2026-09-02 | ケースを足したら、変異が殺せることまで測る | 生存変異に対してケースを 10 本足したが、そのうち 2 本は**変異後の実装でも鳴ってしまい**、足しただけでは殺せていなかった。`PROVISIONING_PROFILE_SPECIFIER = a/実値` は `/` 1 個で切る実装でも前半の `a` で鳴る（実値を `/` の後ろだけに置く形に直した）。小文字の `local.xcconfig` は、照合を大小区別に戻すと走査対象に回って**中身で鳴る**ので終了コードもブロック数も変わらない（「追跡されています」という見出しまで見る形に直した）。**「ケースを足した」は「網が増えた」を意味しない** |
| 2026-09-02 | 誤検知の直し方は「実値を消す」を第一指示にする | 12 巡目に足した「値を消す（コメントなら設定名の直後の `=` を外す）」は、括弧内を字義通りに実行すると `// 例: DEVELOPMENT_TEAM 実値` になり、**検査は通って実値は追跡ファイルに残る**（実測）。「説明のつもりで実値を書いても公開されるのは同じ」という設計意図を、検査自身の指示が裏切る形だった。`Config.xcconfig` に置いた規律も同じ誤りで、「`=` を書くな」と読める。どちらも「実値を書かない」を第一に置き、`=` の話を条件付きに落とす。**規律は 3 か所（docstring・Config.xcconfig・報告の tail）に散るので、docstring から「変えるなら両方」と名指しする** |
| 2026-09-02 | 未マージのパスと git の失敗理由を潰さない | `git ls-files -z` はコンフリクト中のパスをステージごとに 3 回出すため、同じ指摘が 3 つ並んでいた（フックからは到達しないが、`verifier` が毎回回す経路では出る）。あわせて `git show :パス` のどんな失敗も「index に無い」に潰していたので、コンフリクトや破損が「削除された」と読めた。重複は `dict.fromkeys` で潰し、失敗は stderr を添えて出す |
| 2026-09-02 | 追跡外ファイルが在る状態をケースにする | 使い捨てリポジトリは毎回 `git add -Af` してから検査していたので、**追跡外のファイルが 1 つも無い**状態しか測れていなかった。手元の `Local.xcconfig` とビルド生成物が置いてあるのは全開発者の常態で、そこで鳴る実装は毎コミット鳴って `--no-verify` を覚えさせる。**いちばん恐れている誤検知の方向に、動機の中心ファイルの網が無かった** |
| 2026-09-02 | 「ケースを足した ≠ 網が増えた」はアサーションにも当てはまる | 13 巡目に足した「直し方の誘導先を見る」アサーションは、**見出しにも `Config.xcconfig` が出る**ため、誘導を消す変異が見出しで満たされて生き残った。ブロックの最終行だけを見る形に直したところ、今度は報告が 1 ブロックのときに末尾の改行で最終行が空文字になり、全ケースが落ちた。**足したアサーションごとに、狙った変異が落ちるかを測る**（[17](../仕様/17_テスト戦略定義書.md) §2.2 に本文として上げた） |
| 2026-09-02 | 3 点セットは一致だけでなく中身も固定する | 不変条件が 3 か所の**対称性しか見ていなかった**ため、**3 か所を揃えて消す編集**——本書が「拡張子を足すときは 3 点セットで」と指示している、まさにその形——が緑のまま通っていた。実測で 18 拡張子のうち 16 件が協調削除で素通りし、`.mobileprovision` と `.p12` を追跡させても exit 0 になる検査ができあがる。裏返しに `.md` を 3 か所へ足す変異も緑で、その検査は追跡された Markdown を残らず「署名の実体」として鳴らす。**外せない一覧はテスト側にリテラルで持つ**（モジュールから引くと自己参照になって同じ穴が開く）。あわせて、実体を `xcuserdata` の外に置いたケースを足す——中に置くと `USER_STATE` の枝が同じ文言で鳴り、実体としての判定が消えても気づけない |
| 2026-09-02 | `git show` はステージ番号まで書く | `:パス` は git のリビジョン構文なので、`0:x.xcconfig` のような名前だと「ステージ 0 の `x.xcconfig`」と解釈されて**別のファイルを読み、素通りする**（実測）。現実には起きない形だが、`:0:パス` と書けば消えるうえ意図も明確になる |
