# 開発基盤（ロードマップ ②）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

- 作成日: 2026-08-29
- 位置づけ: [開発基盤検討](../検討/31_開発基盤検討.md) の実装手順。タスクは技術改善バックログ（23）の T-01〜T-10 と 1:1 に対応する。② が完了したら本書は `archive_25_開発基盤実装計画.md` に改名して凍結する

**Goal:** hitosuji の AI 主導開発体制（docs が正・台帳・レビュアー agent・機械検査・CI）を Re-Buy に骨格だけ移植し、リポジトリを public にする。

**Architecture:** docs/ を hitosuji の番号体系（1X 仕様 / 2X 案件 / 3X 検討）に再編し、`scripts/docs-check.sh` と CI で構造を機械的に守る。実装の検証・レビューは `.claude/agents/` の 4 本（verifier / code-quality-reviewer / spec-reviewer / test-reviewer）に委譲し、CLAUDE.md がその運用を定める。インストルメンテーションテストは Gradle Managed Device で、ローカル・verifier・CI が同じ 1 コマンドで回す。

**Tech Stack:** GitHub（private → public）、GitHub Actions、Dependabot、Gradle 9.7.1 / AGP 9.3.2、Gradle Managed Device（aosp-atd API 35）、POSIX sh + python3（scripts）、Claude Code（agents / settings.json）

**Spec:** [docs/検討/31_開発基盤検討.md](../検討/31_開発基盤検討.md)

## Global Constraints

- GitHub リポジトリ名は `obaya884/Re-Buy`。② 完了時まで private
- ライセンスファイルは置かない。README に「閲覧のみ可、複製・改変・再配布・商用利用は許諾しない」を明記
- docs の番号は全体で一意な 2 桁。1X 仕様 / 2X 案件 / 3X 検討。予約: 12・13・14・21（④）、15・17（③）
- 表のセル内に `|` を書かない（docs-check が列を数えられなくなる）
- 台帳の完了エントリは状態更新と同じコミットで `closed_` へ移す
- git 運用: docs・軽微 → main 直コミット／コード・挙動 → `<id>-<slug>` ブランチ → PR → CI 緑 → 合図で squash マージ。② の作業はツール整備なので main 直でよいが、**T-06 だけは PR を通して CI を検証する**
- ② の間は push ＝ デプロイではない（private、未リリース）ので、各タスクの末尾で main を push してよい
- コード変更は原則 Edit / Write。機械的な一括置換は python / sed を使ってよいが直後に `git diff` を提示する
- 署名鍵・証明書（`*.jks` `*.keystore` `*.p12` `*.pfx` `*.pem` `*.key` `keystore.properties`、`keytool` `apksigner` `jarsigner` `openssl` `security`）には触れない
- コミットメッセージは日本語、末尾に `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` と `Claude-Session:` 行（既存コミットに倣う）
- hitosuji の原本が必要なタスクでは `HITOSUJI=$(mktemp -d)/hitosuji && gh repo clone obaya884/hitosuji "$HITOSUJI" -- --depth 1 -q` で取得し、作業後に削除する

---

### Task 1: GitHub リポジトリの作成と初回 push（T-01）

**Files:**
- なし（リモートの作成のみ）

**Interfaces:**
- Produces: リモート `origin` = `https://github.com/obaya884/Re-Buy.git`。以降のタスクは `git push origin main` できる

- [ ] **Step 1: リモートが無いことを確認する**

Run: `git remote -v`
Expected: 出力なし

- [ ] **Step 2: private リポジトリを作成して push する**

```bash
gh repo create obaya884/Re-Buy --private --source=. --remote=origin --push \
  --description "Re-Buy（仮称）— くりかえし使える買い物リスト"
```

Expected: `✓ Created repository obaya884/Re-Buy on GitHub` と push 完了のログ

- [ ] **Step 3: マージ方式とブランチ自動削除を設定する**

```bash
gh repo edit obaya884/Re-Buy \
  --enable-squash-merge=true \
  --enable-merge-commit=false \
  --enable-rebase-merge=false \
  --delete-branch-on-merge=true
```

- [ ] **Step 4: 設定を確認する**

Run: `gh repo view obaya884/Re-Buy --json visibility,squashMergeAllowed,mergeCommitAllowed,rebaseMergeAllowed,deleteBranchOnMerge`
Expected: `{"deleteBranchOnMerge":true,"mergeCommitAllowed":false,"rebaseMergeAllowed":false,"squashMergeAllowed":true,"visibility":"PRIVATE"}`

- [ ] **Step 5: ローカル main が origin/main を追跡していることを確認する**

Run: `git status -sb | head -1`
Expected: `## main...origin/main`

---

### Task 2: docs の番号付き構造と台帳の新設（T-02）

**Files:**
- Move: `docs/憲章.md` → `docs/仕様/11_憲章.md`
- Move: `docs/ロードマップ.md` → `docs/案件/24_ロードマップ.md`
- Create: `docs/仕様/16_git運用定義書.md`
- Create: `docs/案件/22_要件バックログ.md`
- Create: `docs/案件/closed_22_要件バックログ.md`
- Create: `docs/案件/23_技術改善バックログ.md`
- Create: `docs/案件/closed_23_技術改善バックログ.md`
- Modify: `docs/検討/31_開発基盤検討.md`（関連リンクのパス）、`docs/案件/25_開発基盤実装計画.md`（本書。同上）、`docs/仕様/11_憲章.md`、`docs/案件/24_ロードマップ.md`（相互参照のパス）

**Interfaces:**
- Produces: 台帳 23 に T-01〜T-10 のエントリ。Task 3 の docs-check が参照する 4 ファイル（22 / closed_22 / 23 / closed_23）

- [ ] **Step 1: ディレクトリを作り、既存 2 文書を移す**

```bash
mkdir -p docs/仕様 docs/案件
git mv docs/憲章.md docs/仕様/11_憲章.md
git mv docs/ロードマップ.md docs/案件/24_ロードマップ.md
```

- [ ] **Step 2: 移した 2 文書の相互参照を直す**

`docs/仕様/11_憲章.md` のヘッダにある `[ロードマップ](./ロードマップ.md)`（2 か所）を `[ロードマップ](../案件/24_ロードマップ.md)` に、本文の「ロードマップ ④」等の語はそのまま。
`docs/案件/24_ロードマップ.md` の「置き場所」行を `- 置き場所: docs/案件/24（② で番号付き構造に組み込み済み）` に書き換える。
`docs/検討/31_開発基盤検討.md` のヘッダ `[ロードマップ](../ロードマップ.md) ／ [憲章](../憲章.md)` を `[ロードマップ](../案件/24_ロードマップ.md) ／ [憲章](../仕様/11_憲章.md)` に。本書（25）の Spec 行はそのまま（既に `../検討/` 相対）。

- [ ] **Step 3: `docs/仕様/16_git運用定義書.md` を作る**

```markdown
# Re-Buy（仮称） git 運用定義書

- 版数: 1.0
- 作成日: 2026-08-29
- 更新日: 2026-08-29
- 位置づけ: git／ブランチ運用のルールは本書が正。CLAUDE.md「git 運用」節は本書の要約
- 関連: [CLAUDE.md](../../CLAUDE.md) ／ [開発基盤検討](../検討/31_開発基盤検討.md)

## 1. git 運用ルール

### 1.1 リスク別ルーティング

変更の種類で入口を分ける。判断に迷ったらリスクの高い側（ブランチ＋PR）に倒す。

| 変更の種類 | 経路 |
|---|---|
| docs・軽微（typo・文言・挙動に触れない小修正）・ツール整備 | `main` 直コミット。push はオーナーの合図でまとめて行ってよい |
| コード・挙動の変更 | ブランチ → PR → CI 緑 → オーナー動作確認 → **合図で squash マージ** |

未リリースの間は push ＝ デプロイではない。ストア公開後（ロードマップ ⑤）にリリースブランチの扱いを本書へ足す。

### 1.2 ブランチ命名

- **`<id>-<slug>`**。すべて小文字、使える文字は `a-z 0-9 -` のみ。スラッシュは使わない
- `id` は台帳の ID をそのまま小文字にしたもの: `t-XX`（技術改善バックログ 23）、`f-XXX` / `n-XX`（要求。④ 以降）、`fb-XX`（ユーザーフィードバック 21。④ 以降）。ID の無い作業は先に台帳へ起票する
- **1 ブランチ＝1 PR。ブランチは使い回さない**。マージ時にリモートブランチは自動削除される（リポジトリ設定）。ローカルは squash のためマージ検出できないので、マージ済みを確認のうえ `git branch -D` で消す
- **ブランチは push 済みの main から切る**。squash は派生元からの全差分を 1 コミットに畳むので、未 push のコミットが main に残ったまま切るとその内容まで PR に取り込まれる。切る前に `git status -sb` で ahead が無いことを見る
- `dependabot/*` ブランチはこの命名の対象外

### 1.3 マージ規律

- **push・マージは常にオーナーの合図が前提**。自動マージ・ブランチ保護は設けない
- マージ方式は squash のみ（merge commit / rebase はリポジトリ設定で無効）
- CI（`docs` と `verify`）が緑でないものはマージしない

## 2. 並行開発

現時点では設けない。複数タスクを同時に進める必要が出たら、hitosuji の「git 運用と並行開発体制定義書」§2〜§5（ヘッド 1＋ワーカー N、worktree、マージキュー）を持ち込む。

## 3. 決定ログ

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-08-29 | hitosuji のリスク別ルーティングを採用する | ツール整備・docs の 1 行更新に PR を要求すると手が止まる一方、コードは PR の diff がレビュアー agent の対象になる |
| 2026-08-29 | squash マージのみ許可 | main のログを線形に保つ。方式を取り違えられないようリポジトリ設定で塞ぐ |
```

- [ ] **Step 4: `docs/案件/22_要件バックログ.md` を作る**

```markdown
# 要件バックログ

- 作成日: 2026-08-29
- 上位文書: [憲章](../仕様/11_憲章.md)（④ で要求一覧 F-XXX / N-XX を起こしたらそちら）
- 本書はプロダクト（要求 F-XXX / N-XX）軸の台帳。挙動を変えない技術活動は[技術改善バックログ](./23_技術改善バックログ.md)が持つ
- 運用ルールをなぜそう定めたかは本書に書かず、`log_22_要件バックログ.md` へ日付順に積む（最初のエントリが出た時点で作る）

未実装要求を**決定状態（レーン）＋熟度タグ＋着手トリガ**で管理する。挙動レベルの仕様判断が発生したら**先に**仕様書群へ反映し、本書は一行の要約に留める。

## 書式

**1 エントリ＝レーンの表の 1 行**。列は次の 6 つで、**すべて埋める**。

| 列 | 中身 |
|---|---|
| ID | `F-XXX` / `N-XX` |
| タイトル | 要求の名前 |
| 熟度 | **着手時にオーナーの仕様判断が要るか**を、参照先 docs の実体で判定する（下表）。レーンとは直交する軸 |
| 内容 | 何を作るかの一行要約。無ければ `-` |
| トリガ | **着手条件。ここを空にしない**——トリガの無いエントリは次の選出で永久に拾われない |
| 参照 | 反映先の条項・検討ドキュメント |

### 熟度の判定

| 熟度 | 条件（**参照先を開いて確かめる**） | 着手したらどこから始まるか |
|---|---|---|
| 仕様済 | 画面定義書（＋必要ならデータモデル定義書）に操作仕様の実体がある | 実装 |
| 設計済 | 検討ドキュメントに方式がある。画面への展開はこれから | 画面定義書への展開判断 |
| 列済 | データ列だけ用意済み。UI は白紙 | UI の仕様判断 |
| 未詰め | 要求文しかない | 仕様判断（オーナーとの対話） |
| `-` | 未評価 | 不明。次に触るとき判定する |

- **セル内に `|` を書かない**
- レーンは `確定・着手待ち` / `候補・見極め中` / `監視項目` の 3 つ。**状態は行に持たず、どの表に置くかで表す**
- 見送りの出口: やらないと決めたら憲章 §6 スコープ外＋§12 決定ログへ落とす
- **完了したエントリは[closed_22_要件バックログ.md](./closed_22_要件バックログ.md)へ移す**。移す際は 1 行を `## F-XXX タイトル（完了日）`＋対応・反映先の節に展開する

## 現況

② 開発基盤の整備中。要求一覧（F-XXX / N-XX）は ④ で憲章から起こす。それまで本書のレーンは空。

## 確定・着手待ち

**現在このレーンは空**。

## 候補・見極め中

| ID | タイトル | 熟度 | 内容 | トリガ | 参照 |
|---|---|---|---|---|---|

## 監視項目（実装ではない）

作るのではなく観察する。閾値を越えたら要求（F/N）や技術改善（T）を起こす。

| ID | タイトル | 熟度 | 内容 | トリガ | 参照 |
|---|---|---|---|---|---|

## 完了記録

完了は[closed_22_要件バックログ.md](./closed_22_要件バックログ.md)へ日付順で積む。
```

- [ ] **Step 5: `docs/案件/closed_22_要件バックログ.md` を作る**

```markdown
# 要件バックログ完了記録

- 作成日: 2026-08-29
- 目的: 完了した要求（F-XXX / N-XX）の記録を保管する。ライブ台帳は[要件バックログ](./22_要件バックログ.md)
- 並びは**新しい順**（先頭に積む）。移送は手作業（1 行を `## ID タイトル（完了日）` の節に展開する）

（まだ完了したエントリはない）
```

- [ ] **Step 6: `docs/案件/23_技術改善バックログ.md` を作る**

```markdown
# 技術改善バックログ

- 作成日: 2026-08-29
- 目的: 技術負債の返済・リファクタリング・開発ツール整備など、**プロダクト挙動を変えない技術活動**を、要求（F-XXX / N-XX）とは別軸で管理・計画する
- 関連文書: [開発基盤検討](../検討/31_開発基盤検討.md) ／ [要件バックログ](./22_要件バックログ.md)（プロダクト軸の台帳）

## この文書の守備範囲

プロダクトの**挙動**に関わる変更は扱わない。ここに書くのは、ユーザーから見た挙動を変えずに**開発の健全性を上げる活動**——依存追随・CI/ツール整備・内部設計の改善・テスト補強・調査など。

- 採番は **T-XX**。1 系列で通し、種別で区別する
- 種別: **何に触るか＝着手時に何が網になるか**で分ける（下表）
- 優先度: `高` / `中` / `低` / `様子見`
- 状態: `未着手` / `進行中` / `完了` / `保留`
- **1 エントリ＝[一覧](#一覧)の 1 行＋[詳細](#詳細)の 1 節**。一覧は索引に徹し `ID / タイトル / 種別 / 優先度 / 状態 / 詳細` の 6 列を持つ
  - **見出しは `### T-XX` の ID のみ**
  - **セル内に `|` を書かない**
  - **種別・優先度・状態を詳細節に書かない**。一覧が正
  - 詳細節の項目は `背景`（必須）に続けて `対応方針` / `着手条件` / `優先度の根拠` / `関連` を必要なだけ
  - 詳細節の並び順は一覧と同じにする
- **完了になったエントリは、状態更新と同じコミットで[closed_23_技術改善バックログ.md](./closed_23_技術改善バックログ.md)へ移す**（本書には未着手・進行中・保留だけを残す）。**一覧の行と詳細節はセットで移す**——`sh scripts/ledger-move.sh T-XX` が両方を移す
- 判断の経緯は本文に書かず、`log_23_技術改善バックログ.md` へ日付順に積む（最初のエントリが出た時点で作る）

### 種別

| 種別 | 何に触るか | 着手時に何が網になるか |
|---|---|---|
| 内部設計 | `app/src/main` の構造（挙動不変） | 既存テスト。**変異させて振る舞い保存を確かめる** |
| テスト | テストコードのみ（本番コード不変） | 本番コードに触れないので回帰の心配がない |
| ツール整備 | `scripts` / CI / `.claude` / 開発環境 | **自動テストの網が無い**。実行して確かめる |
| 依存追随 | 依存ライブラリ・Gradle・AGP の更新 | 上流待ちが多く自分では動かせない。**再確認の契機を必ず書く** |
| 調査 | 何も変えない。判断を記録する | 成果物は決定そのもの |

## 一覧

| ID | タイトル | 種別 | 優先度 | 状態 | 詳細 |
|---|---|---|---|---|---|
| T-01 | GitHub リポジトリの作成と初回 push | ツール整備 | 高 | 未着手 | [詳細](#t-01) |
| T-02 | docs の番号付き構造と台帳の新設 | ツール整備 | 高 | 未着手 | [詳細](#t-02) |
| T-03 | docs-check.sh の移植 | ツール整備 | 高 | 未着手 | [詳細](#t-03) |
| T-04 | ledger-move.sh の移植 | ツール整備 | 中 | 未着手 | [詳細](#t-04) |
| T-05 | Gradle Managed Device の導入 | テスト | 高 | 未着手 | [詳細](#t-05) |
| T-06 | CI と Dependabot | ツール整備 | 高 | 未着手 | [詳細](#t-06) |
| T-07 | サブエージェント 4 本 | ツール整備 | 高 | 未着手 | [詳細](#t-07) |
| T-08 | .claude/settings.json | ツール整備 | 中 | 未着手 | [詳細](#t-08) |
| T-09 | CLAUDE.md の全面改訂と README | ツール整備 | 高 | 未着手 | [詳細](#t-09) |
| T-10 | リポジトリの public 化 | ツール整備 | 高 | 未着手 | [詳細](#t-10) |

## 詳細

### T-01

- 背景: リモートが無く、CI もバックアップも無い
- 対応方針: `obaya884/Re-Buy` を private で作成して push。squash のみ・ブランチ自動削除を設定
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §3

### T-02

- 背景: docs が `docs/憲章.md` `docs/ロードマップ.md` の平置きで、台帳が無い
- 対応方針: hitosuji の番号体系（1X 仕様 / 2X 案件 / 3X 検討）へ再編し、16 git 運用・22 要件バックログ・23 本書とそれぞれの `closed_` を新設
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §4

### T-03

- 背景: docs の表構造・リンク切れ・台帳の書式を機械で守る手段が無い
- 対応方針: hitosuji の `scripts/docs-check.sh` を移植し、台帳定義を 22・23 に絞る
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §7

### T-04

- 背景: 台帳の完了エントリを一覧と詳細の 2 か所から `closed_` へ移す作業を手でやると片方を置き忘れる
- 対応方針: hitosuji の `scripts/ledger-move.sh` を移植し、対象を T-XX（23）のみにする
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §7

### T-05

- 背景: インストルメンテーションテスト（`RoomMigrationTest`）を回すのに手動でエミュレータを起動する必要があり、CI で回せない
- 対応方針: `app/build.gradle.kts` に Gradle Managed Device `pixel6Api35`（aosp-atd）を定義し、`./gradlew pixel6Api35DebugAndroidTest` の 1 コマンドにする
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §8

### T-06

- 背景: CI が無い
- 対応方針: GitHub Actions に `docs`（docs-check）と `verify`（build＋GMD）の 2 ジョブ。Dependabot は gradle と github-actions を weekly。**この作業だけは PR を通して CI の動作を検証する**
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §8

### T-07

- 背景: 検証・レビューをメインセッションが全部抱えるとコンテキストが実装に使えない
- 対応方針: `.claude/agents/` に verifier / code-quality-reviewer / spec-reviewer / test-reviewer を Android 向けに書く
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §6

### T-08

- 背景: 権限の allow / ask / deny が無く、毎回確認が出るか、逆に危険な操作が素通りする
- 対応方針: `./gradlew` `scripts` `gh pr` を allow、マージ・公開設定を ask、署名鍵・証明書を deny
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §9

### T-09

- 背景: 今の CLAUDE.md は技術メモで、運用（docs 先行・agent・git ルーティング・public 配慮）が書かれていない。README が無い
- 対応方針: hitosuji の構成で全面改訂。README を新規作成しライセンス方針を明記
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §3・§5

### T-10

- 背景: ② の完了条件
- 対応方針: 履歴に資格情報・到達手段が無いことを確認して public に切り替え、ロードマップ ② を完了にする
- 着手条件: T-01〜T-09 が完了していること
- 関連: [開発基盤検討](../検討/31_開発基盤検討.md) §3・§12
```

- [ ] **Step 7: `docs/案件/closed_23_技術改善バックログ.md` を作る**

```markdown
# 技術改善バックログ完了記録

- 作成日: 2026-08-29
- 目的: 完了した技術活動（T-XX）を原文のまま保管する。ライブ台帳は[技術改善バックログ](./23_技術改善バックログ.md)（未着手・進行中・保留のみを持つ）
- エントリは完了への状態更新と同じコミットで本書へ移す。**並びは完了日の古い順**（[一覧](#一覧)・[詳細](#詳細)とも末尾に積む）——移送は `sh scripts/ledger-move.sh` が担い、追記先が末尾で固定されているため。**手で並べ替えない**

## 一覧

| ID | タイトル | 種別 | 優先度 | 状態 | 詳細 |
|---|---|---|---|---|---|

## 詳細
```

- [ ] **Step 8: 旧パスへの参照が残っていないことを確認する**

Run: `grep -rn "docs/憲章.md\|docs/ロードマップ.md\|(\./憲章.md)\|(\./ロードマップ.md)\|(\.\./憲章.md)\|(\.\./ロードマップ.md)" docs CLAUDE.md`
Expected: 出力なし

- [ ] **Step 9: 配置を確認してコミット・push する**

Run: `find docs -type f | sort`
Expected:
```
docs/仕様/11_憲章.md
docs/仕様/16_git運用定義書.md
docs/検討/31_開発基盤検討.md
docs/案件/22_要件バックログ.md
docs/案件/23_技術改善バックログ.md
docs/案件/24_ロードマップ.md
docs/案件/25_開発基盤実装計画.md
docs/案件/closed_22_要件バックログ.md
docs/案件/closed_23_技術改善バックログ.md
```

```bash
git add docs
git commit -m "docs を番号付き構造へ再編し、git 運用定義書と台帳 22・23 を新設（T-02）"
git push origin main
```

（T-01・T-02 の完了処理は Task 4 で `ledger-move.sh` を使って行う）

---

### Task 3: docs-check.sh の移植（T-03）

**Files:**
- Create: `scripts/docs-check.sh`（hitosuji からコピーして編集）

**Interfaces:**
- Produces: `sh scripts/docs-check.sh`。終了コード 0＝エラーなし（警告は出てもよい）、1＝エラー。Task 6 の CI `docs` ジョブと Task 7 の verifier が呼ぶ

- [ ] **Step 1: hitosuji からコピーする**

```bash
HITOSUJI=$(mktemp -d)/hitosuji && gh repo clone obaya884/hitosuji "$HITOSUJI" -- --depth 1 -q
mkdir -p scripts
cp "$HITOSUJI/scripts/docs-check.sh" scripts/docs-check.sh
```

- [ ] **Step 2: hitosuji 固有の箇所を編集する（Edit ツールで 1 か所ずつ）**

1. 冒頭コメント（2〜3 行目）:
   - 旧: `# docs の機械検査（T-36 で台帳3冊の表構造から始まり、T-86 で docs 全体へ拡張した）。` `# 設計は docs/案件/closed_23_技術改善バックログ.md T-86。`
   - 新: `# docs の機械検査。hitosuji の scripts/docs-check.sh を移植した（T-03）。` `# 設計は docs/検討/31_開発基盤検討.md §7。`
2. 使い方（6 行目）: `#   npm run docs:check` → `#   sh scripts/docs-check.sh`
3. リポジトリ外のメッセージ: `echo "hitosuji リポジトリの中で実行してください" >&2` → `echo "Re-Buy リポジトリの中で実行してください" >&2`
4. `TARGETS` 直前のコメント 2 行:
   - 旧: `# 参照は docs 内だけでなく CLAUDE.md・README・\`.claude/\` にも散る（CLAUDE.md「書き方の規約」）。` `# AGENTS.md だけは \`next dev\` が生成し直すので対象から外す`
   - 新: `# 参照は docs 内だけでなく CLAUDE.md・README・\`.claude/\` にも散る（CLAUDE.md「書き方の規約」）。`（2 行目を削除）
5. `LEDGERS` 辞書から `"21": {...},` のブロック（`"21": {` から対応する `},` まで 6 行）を削除する。残るのは `"22"` と `"23"`
6. `Phase 表記が残っている（現況は要件定義書 §3・要件バックログが持つ）` → `Phase 表記が残っている（現況はロードマップと要件バックログが持つ）`

- [ ] **Step 3: 編集後に検査対象と台帳定義を目視する**

Run: `sed -n '1,8p' scripts/docs-check.sh; grep -n '"2[123]": {' scripts/docs-check.sh; grep -n 'AGENTS\|hitosuji\|npm' scripts/docs-check.sh`
Expected: 冒頭が新コメント、台帳は `"22"` と `"23"` の 2 つ、最後の grep は出力なし

- [ ] **Step 4: 実行して通ることを確認する**

Run: `sh scripts/docs-check.sh; echo "exit=$?"`
Expected: `exit=0`。警告（更新日など）が出た場合は内容を記録する。エラーが出た場合は Task 2 の成果物（表の列数・リンク先）を直す——スクリプト側を緩めない

- [ ] **Step 5: 壊れた表とリンクを検出できることを確かめる（負のテスト）**

一時ファイルを作る:
```bash
cat > docs/検討/99_tmp.md <<'EOF'
# tmp

| a | b |
|---|
| 1 | 2 | 3 |

[切れたリンク](./存在しない.md)
EOF
sh scripts/docs-check.sh; echo "exit=$?"
```
Expected: `exit=1`。出力に `docs/検討/99_tmp.md` の列数エラーとリンク切れエラーが含まれる

```bash
rm docs/検討/99_tmp.md
sh scripts/docs-check.sh; echo "exit=$?"
```
Expected: `exit=0`

- [ ] **Step 6: コミット・push する**

```bash
rm -rf "$HITOSUJI"
git add scripts/docs-check.sh
git commit -m "docs-check.sh を hitosuji から移植（T-03）"
git push origin main
```

---

### Task 4: ledger-move.sh の移植（T-04）

**Files:**
- Create: `scripts/ledger-move.sh`（hitosuji からコピーして編集）
- Modify: `docs/案件/23_技術改善バックログ.md`、`docs/案件/closed_23_技術改善バックログ.md`（T-01〜T-04 の完了処理）

**Interfaces:**
- Produces: `sh scripts/ledger-move.sh T-XX [--status '<状態列>']`。以降のタスクは完了時にこれで台帳を閉じる

- [ ] **Step 1: hitosuji からコピーする**

```bash
HITOSUJI=$(mktemp -d)/hitosuji && gh repo clone obaya884/hitosuji "$HITOSUJI" -- --depth 1 -q
cp "$HITOSUJI/scripts/ledger-move.sh" scripts/ledger-move.sh
```

- [ ] **Step 2: hitosuji 固有の箇所を編集する（Edit ツールで 1 か所ずつ）**

1. 冒頭コメント 2〜3 行目:
   - 旧: `# 台帳のエントリを完了記録へ移す（T-36）。` `# 設計は docs/案件/closed_23_技術改善バックログ.md T-36 / docs/仕様/16_git運用と並行開発体制定義書.md §3.4。`
   - 新: `# 台帳のエントリを完了記録へ移す。hitosuji の scripts/ledger-move.sh を移植した（T-04）。` `# 設計は docs/検討/31_開発基盤検討.md §7。`
2. 使い方 6〜8 行目を次の 2 行に置き換える:
   ```
   #   sh scripts/ledger-move.sh T-05
   #   sh scripts/ledger-move.sh T-05 --status '完了 2026-08-29'
   ```
3. 10 行目 `# 台帳21（FB-XX）と台帳23（T-XX）の当該エントリを、それぞれの完了記録へ移す。` → `# 台帳23（T-XX）の当該エントリを完了記録へ移す（台帳21 FB-XX は ④ で足す）。`
4. `echo "hitosuji リポジトリの中で実行してください" >&2` → `echo "Re-Buy リポジトリの中で実行してください" >&2`
5. 使い方メッセージ: `print("使い方: npm run ledger:move -- <FB-XX|T-XX> [--status '<新しい状態列>']", file=sys.stderr)` → `print("使い方: sh scripts/ledger-move.sh <T-XX> [--status '<新しい状態列>']", file=sys.stderr)`
6. `LEDGERS` 辞書から `"FB": {...},` のブロック（`"FB": {` から対応する `},` まで 6 行）を削除する
7. `matched = re.fullmatch(r"(FB|T)-\d+", entry_id)` → `matched = re.fullmatch(r"(T)-\d+", entry_id)`
8. `print(f"ID は FB-XX または T-XX の形で指定してください（受け取った値: {entry_id}）", file=sys.stderr)` → `print(f"ID は T-XX の形で指定してください（受け取った値: {entry_id}）", file=sys.stderr)`
9. 末尾: `print(f"npm run docs:check で構造と、{entry_id} を指す既存リンクの向き先を確認してください")` → `print(f"sh scripts/docs-check.sh で構造と、{entry_id} を指す既存リンクの向き先を確認してください")`

- [ ] **Step 3: 残存を確認する**

Run: `grep -n 'FB\|npm\|hitosuji\|21_' scripts/ledger-move.sh`
Expected: 出力は 3 行目の「台帳21 FB-XX は ④ で足す」のコメント 1 件のみ

- [ ] **Step 4: T-01〜T-04 を完了にして移す（これが動作テスト）**

```bash
for id in T-01 T-02 T-03 T-04; do
  sh scripts/ledger-move.sh "$id" --status '完了 2026-08-29'
done
sh scripts/docs-check.sh; echo "exit=$?"
```
Expected: 各 ID について `T-0N を docs/案件/closed_23_技術改善バックログ.md へ移しました（一覧の行と詳細節の2か所）` `状態列を差し替えました`。docs-check は `exit=0`

Run: `grep -c '^| T-' docs/案件/23_技術改善バックログ.md; grep -c '^| T-' docs/案件/closed_23_技術改善バックログ.md; grep -c '^### T-' docs/案件/closed_23_技術改善バックログ.md`
Expected: `6` `4` `4`（ライブに T-05〜T-10、closed に T-01〜T-04 が一覧・詳細とも）

- [ ] **Step 5: コミット・push する**

```bash
rm -rf "$HITOSUJI"
git add scripts/ledger-move.sh docs/案件/23_技術改善バックログ.md docs/案件/closed_23_技術改善バックログ.md
git commit -m "ledger-move.sh を hitosuji から移植し、T-01〜T-04 を完了記録へ移す（T-04）"
git push origin main
```

---

### Task 5: Gradle Managed Device の導入（T-05）

**Files:**
- Modify: `app/build.gradle.kts`（`android {}` 内に `testOptions` を追加）
- Modify: `.gitignore`（GMD の生成物）

**Interfaces:**
- Produces: Gradle タスク `pixel6Api35DebugAndroidTest`。Task 6 の CI と Task 7 の verifier が呼ぶ

- [ ] **Step 1: 現状、instrumented test を回す手段がエミュレータ手動起動しか無いことを確認する**

Run: `./gradlew tasks --all 2>/dev/null | grep -i 'AndroidTest' | grep -iv 'connected\|assemble\|compile\|lint\|bundle\|package\|process\|merge\|generate\|check\|ksp\|hilt\|create\|extract\|transform\|dex\|write\|validate\|strip\|javaPre\|prepare\|collect\|aggregate\|copy\|map\|desugar\|verify\|parse\|unit'`
Expected: `pixel6Api35DebugAndroidTest` は無い

- [ ] **Step 2: `app/build.gradle.kts` の `android {}` 内、`packaging {}` ブロックの直後に追加する**

```kotlin
    testOptions {
        managedDevices {
            localDevices {
                create("pixel6Api35") {
                    device = "Pixel 6"
                    apiLevel = 35
                    systemImageSource = "aosp-atd"
                }
            }
        }
    }
```

- [ ] **Step 3: タスクが生成されることを確認する**

Run: `./gradlew tasks --all 2>/dev/null | grep '^pixel6Api35DebugAndroidTest'`
Expected: `pixel6Api35DebugAndroidTest - ...` の 1 行

- [ ] **Step 4: ローカルで実行する（初回はシステムイメージのダウンロードで数分かかる）**

Run: `./gradlew pixel6Api35DebugAndroidTest 2>&1 | tail -15`
Expected: `BUILD SUCCESSFUL`。途中でライセンス同意を求められて止まる場合は `yes | "$HOME/Library/Android/sdk/cmdline-tools/latest/bin/sdkmanager" --licenses` を案内する（cmdline-tools が無ければ `android sdk install cmdline-tools/latest`）

- [ ] **Step 5: `RoomMigrationTest` が実行されたことを結果ファイルで確認する**

Run: `find app/build/outputs/androidTest-results/managedDevice -name 'TEST-*.xml' | head -3; grep -ho 'tests="[0-9]*" .*failures="[0-9]*" errors="[0-9]*"' $(find app/build/outputs/androidTest-results/managedDevice -name 'TEST-*.xml')`
Expected: `RoomMigrationTest` の XML が 1 つあり、`tests="1" ... failures="0" errors="0"`

- [ ] **Step 6: 生成物が git に入らないことを確認する**

Run: `git status --short`
Expected: `M app/build.gradle.kts` のみ。それ以外（`app/build/`・`.gradle/`）は既に `.gitignore` 済み。もし GMD が `~/.android/avd` 以外に AVD を作って作業ツリーに出た場合はそのパスを `.gitignore` に足す

- [ ] **Step 7: 台帳を閉じてコミット・push する**

```bash
sh scripts/ledger-move.sh T-05 --status '完了 2026-08-29'
sh scripts/docs-check.sh
git add app/build.gradle.kts docs/案件/23_技術改善バックログ.md docs/案件/closed_23_技術改善バックログ.md
git commit -m "Gradle Managed Device pixel6Api35 を導入（T-05）"
git push origin main
```

---

### Task 6: CI と Dependabot（T-06。PR を通す）

**Files:**
- Create: `.github/workflows/ci.yml`
- Create: `.github/dependabot.yml`

**Interfaces:**
- Consumes: `sh scripts/docs-check.sh`（Task 3）、`./gradlew pixel6Api35DebugAndroidTest`（Task 5）
- Produces: PR と main push で走る `docs` / `verify` ジョブ

- [ ] **Step 1: push 済みの main からブランチを切る**

Run: `git status -sb | head -1`
Expected: `## main...origin/main`（ahead が無い。あれば先に push）

```bash
git checkout -b t-06-ci
```

- [ ] **Step 2: `.github/workflows/ci.yml` を作る**

```yaml
name: CI

# PR と main への push で docs の機械検査と、build（lint・unit test 込み）＋
# インストルメンテーションテスト（Gradle Managed Device）を回す。
# マージは手動（docs/仕様/16_git運用定義書.md §1.3）。

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # docs の機械検査。JDK も Gradle も要らないので verify とは別ジョブで並列に回す
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: sh scripts/docs-check.sh

  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v6

      # lint・unit test・debug/release の assemble を含む
      - run: ./gradlew build

      # GMD のシステムイメージ（約 500MB）を実行間で使い回す。
      # キーはイメージ名で固定——イメージの更新はキーを変えて行う
      - name: Cache GMD system image
        uses: actions/cache@v5
        with:
          path: ${{ env.ANDROID_HOME }}/system-images/android-35/aosp_atd/x86_64
          key: gmd-system-image-android-35-aosp_atd-x86_64

      # エミュレータのハードウェアアクセラレーション。ubuntu-latest は KVM を持つが
      # 既定では runner ユーザーが /dev/kvm を開けない
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      # ランナーには GPU が無いのでソフトウェアレンダリングを指定する
      - name: Instrumented tests (Gradle Managed Device)
        run: ./gradlew pixel6Api35DebugAndroidTest -Pandroid.testoptions.manageddevices.emulator.gpu=swiftshader_indirect

      - name: Upload test reports on failure
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: test-reports
          path: |
            app/build/reports/
            app/build/outputs/androidTest-results/
```

- [ ] **Step 3: `.github/dependabot.yml` を作る**

```yaml
# Dependabot のバージョン更新設定。趣味ペースなので weekly。
# 更新 PR は CI（.github/workflows/ci.yml）が検証する。マージは手動

version: 2
updates:
  # Gradle の依存（gradle/libs.versions.toml と build.gradle.kts）
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      # マイナー・パッチ更新はまとめて 1 つの PR にする
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  # GitHub Actions（.github/workflows/*）
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"
```

- [ ] **Step 4: Actions の最新メジャーを確認して合わせる**

Run: `for r in actions/checkout actions/setup-java gradle/actions actions/cache actions/upload-artifact; do printf "%-28s " "$r"; gh api "repos/$r/releases/latest" --jq '.tag_name'; done`
Expected: `v7.x` `v6.x` `v6.x` `v5.x` `v5.x` 前後。ci.yml の `@vN` がメジャーと食い違っていれば合わせる（2026-08-29 時点: checkout v7.0.1 / setup-java v6.0.0 / gradle/actions v6.3.0）

- [ ] **Step 5: コミットして push し、PR を作る**

```bash
git add .github
git commit -m "CI（docs / verify）と Dependabot を追加（T-06）"
git push -u origin t-06-ci
gh pr create --title "CI（docs / verify）と Dependabot を追加（T-06）" --body "$(cat <<'EOF'
## 概要
- `docs` ジョブ: `scripts/docs-check.sh`
- `verify` ジョブ: `./gradlew build` と Gradle Managed Device でのインストルメンテーションテスト
- Dependabot: gradle / github-actions を weekly

設計は docs/検討/31_開発基盤検討.md §8。
この PR は T-06 の成果物であると同時に、② で唯一の「PR を通して CI を検証する」作業。

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 6: CI を待つ**

Run: `gh pr checks --watch`
Expected: `docs` と `verify` の両方が pass。**verify が落ちたら** `gh run view --log-failed` で原因を見て直す。典型: KVM が使えない（`/dev/kvm` の権限）、システムイメージのダウンロード失敗、キャッシュパスの不一致（`echo $ANDROID_HOME` をステップに足して実パスを確認）。キャッシュが効かなくても失敗にはならないので、まず緑を優先し、キャッシュの効果は 2 回目の実行時間で見る

- [ ] **Step 7: オーナーの合図を待ってからマージする（メインセッションが行う）**

サブエージェントはここで止まり、PR の URL・CI の結果・verify ジョブの所要時間を報告する。オーナーが承認したらメインセッションが:

```bash
gh pr merge --squash --delete-branch
git checkout main && git pull --ff-only
git branch -D t-06-ci 2>/dev/null || true
```

- [ ] **Step 8: 台帳を閉じてコミット・push する（main 直）**

```bash
sh scripts/ledger-move.sh T-06 --status '完了 2026-08-29'
sh scripts/docs-check.sh
git add docs/案件/23_技術改善バックログ.md docs/案件/closed_23_技術改善バックログ.md
git commit -m "T-06 を完了記録へ移す"
git push origin main
```

Run: `gh run list --branch main --limit 1`
Expected: main への push でも CI が起動している

---

### Task 7: サブエージェント 4 本（T-07）

**Files:**
- Create: `.claude/agents/verifier.md`
- Create: `.claude/agents/code-quality-reviewer.md`
- Create: `.claude/agents/spec-reviewer.md`
- Create: `.claude/agents/test-reviewer.md`

**Interfaces:**
- Consumes: `./gradlew build`、`./gradlew pixel6Api35DebugAndroidTest`（Task 5）、`sh scripts/docs-check.sh`（Task 3）
- Produces: Task 9 の CLAUDE.md「サブエージェント運用」が参照する 4 つの agent 名

- [ ] **Step 1: `.claude/agents/verifier.md` を作る**

```markdown
---
name: verifier
description: 実装後の機械的な検証（build・lint・unit test・インストルメンテーションテスト・docs 検査）を実行し、結果の要約だけを報告する。コードは修正しない。実装の一区切りごとに使う。
tools: Bash, Read, Grep, Glob
model: sonnet
---

あなたは Re-Buy プロジェクトの検証実行係。検証コマンドを実行して合否と失敗の要点だけを報告する。**修正は行わない**（修正はメインセッションの仕事）。

## 検証手順

呼び出し時に対象範囲の指定があればそれに絞る。指定がなければ以下を順に実行:

1. `./gradlew build`（lint・unit test・debug/release の assemble を含む。`app/build/reports/lint-results-debug.html` に Lint の結果が出る）
2. `./gradlew pixel6Api35DebugAndroidTest`（Gradle Managed Device でインストルメンテーションテスト。エミュレータの手動起動は不要。初回はシステムイメージのダウンロードで数分かかる。結果は `app/build/outputs/androidTest-results/managedDevice/` の XML）
3. `sh scripts/docs-check.sh`（`docs/` / CLAUDE.md / README / `.claude/` に差分があるときのみ。CI の `docs` ジョブと同じ検査。**警告行は落ちないが報告に含める**）

## 制約

- **ファイルを一切書き換えない**。検証は読み取りとコマンド実行だけで行う（`tools` に Edit / Write は無い。Bash 経由でも書かない）
- **変異テスト（コードを壊してテストが落ちるか確かめる）は引き受けない**。頼まれたら「変異はメインセッションが Edit で行う」と返し、変異後の実行だけを担う
- **作業ツリーを変える git 操作をしない**（`stash` / `reset` / `checkout -- ` / `clean` / `restore`）。**メインセッションの未コミットの作業が消える**。「差分を消して比べたい」は禁止操作の典型的な入口——比較は作業ツリーを触らずにできる: 起点との比較は `git diff <起点>`、起点側のファイルの中身は `git show <起点>:<パス>`。それでも切り分けられないものは**切り分けずに「差分由来か不明」と報告する**
- `./gradlew clean` は頼まれたときだけ実行する（KSP / Hilt の生成コードが壊れた症状のときに有効）
- 自分が起動したのではないエミュレータ・Gradle デーモンを kill しない
- 署名鍵・証明書（`*.jks` `*.keystore` `*.p12` `*.pem` `keystore.properties`）には触れない

## 報告形式

冒頭に全体の結論（マージ可能な状態か）。続けてステップごとに 成功/失敗 を 1 行で。失敗時はエラーメッセージの該当箇所のみ抜粋（ログ全文は貼らない）し、原因の推定を 1〜2 文で添える。
```

- [ ] **Step 2: `.claude/agents/code-quality-reviewer.md` を作る**

```markdown
---
name: code-quality-reviewer
description: 実装差分の可読性・重複・命名・簡潔さ等のコード品質を検査する読み取り専用レビュアー。実装の一区切りで verifier と並列に使う。コードは修正しない。
tools: Read, Grep, Glob, Bash
model: opus
---

あなたは Re-Buy プロジェクトのコード品質レビュアー。実装差分の**内部品質**——可読性・重複・命名・簡潔さ——だけを検査する。仕様整合（spec-reviewer の担当）でも機械的検証（verifier の担当）でもない。コードの修正は一切行わない（修正はメインセッションの仕事）。

## 手順

1. レビュー対象を特定する。呼び出し時にスコープ（機能名・ファイル・ID）が指定されていればそれを優先し、なければ `git diff` / `git log` で直近の変更範囲を把握する
2. 変更ファイルと**その周辺の既存コード**を読み、このプロジェクトの idiom を把握する。正は CLAUDE.md「アーキテクチャ」節（ViewModel が複数の `MutableStateFlow` を `combine` して UiState に畳む形、Repository にステータス遷移を集約、DI は 1 依存 1 モジュール、画面 Composable のシグネチャ統一、派生値は UiState の `get()`）
3. 下記のチェックリストで品質観点を突き合わせる

## 品質チェックリスト（毎回確認。仕様整合・lint は扱わない）

- **重複/再利用**: 既存の共通部品（`ReBuyAppScaffold`、`TextFieldAddDialog` / `TextFieldEditDialog`、`FlowExt.kt` の `combine`）の再利用漏れ、コピペ、DRY 違反がないか
- **命名/可読性**: 意図が名前から読めるか、周辺の命名慣習（`XxxScreen` / `XxxViewModel` / `XxxScreenUiState`、`showXxxDialog` / `hideXxxDialog`）と揃っているか
- **簡潔さ/複雑度**: 過剰なネスト・分岐、不要な抽象、デッドコード・未使用の import / 関数がないか。Compose では不要な `remember` や過剰な再コンポーズを招く構造がないか
- **一貫性**: 周辺コードのスタイル・コメント密度（日本語コメント）に馴染んでいるか
- **コメントの質**: 差分に足されたコメントが役割を逸脱していないか——コードの言い換え／判断の経緯・却下案・過去チケット（T-XX / FB-XX）の症状説明／仕様書本文の長い引き写しは削減を指摘する。**残すべきコメント**——仕様条項への参照（1〜2 行）・型で表せない契約や不変条件・外部 API/環境の落とし穴・命名の意図——は指摘しない
- **Kotlin の流儀**: null 安全（`!!` の濫用）、`when` の網羅、data class / sealed class の使いどころ、コルーチンのスコープ（`viewModelScope` 外で launch していないか）
- **テストの質**: テストが意図を読めるか・意味のある検証か（「読めるか・意味があるか」だけに絞る。観点の網羅・正しさは test-reviewer、仕様条項との対応は spec-reviewer の領域）

## 制約（棲み分けを明確に）

- **仕様整合は見ない**（docs/ との一致は spec-reviewer の担当）
- **lint・build・テストの実行はしない**（verifier の担当）
- **バグ探しの網羅はしない**。ただし品質観点のレビュー中に明らかなバグに気づいたら重要度「高」で報告してよい
- コードは修正しない。**作業ツリーを変える git 操作もしない**（`stash` / `reset` / `checkout -- ` / `clean` / `restore`）——メインセッションの未コミットの作業が消える

## 報告形式

指摘ごとに: 重要度（高・中・低）/ カテゴリ（重複・命名・複雑度 等）/ `file:line` / 内容と改善案。
指摘がなければ「指摘なし」とし、確認した観点を挙げる。ログや長いコード引用は貼らず、要点のみ。
```

- [ ] **Step 3: `.claude/agents/spec-reviewer.md` を作る**

```markdown
---
name: spec-reviewer
description: 実装と docs/ 仕様書群（憲章、④ 以降は要件・画面・データモデル定義書）との整合を検査する読み取り専用レビュアー。仕様が絡む変更のレビュー時、または docs に触れた変更のレビュー時に使う。コードは修正しない。
tools: Read, Grep, Glob, Bash
model: sonnet
---

あなたは Re-Buy プロジェクトの仕様適合レビュアー。挙動の契約はコードではなく docs/ 配下のドキュメント群にある。実装がその契約を守っているかだけを検査し、コードの修正・仕様書の変更は一切行わない。

## 手順

1. レビュー対象を特定する。呼び出し時にスコープ（機能名・ファイル・ID）が指定されていればそれを優先し、なければ `git diff` / `git log` で直近の変更範囲を把握する
2. 対象に関係する仕様条項を洗い出す:
   - `docs/仕様/11_憲章.md` — 重視する価値 C-1〜C-5（§5）、スコープ外（§6）、収益モデル（§7）、プラットフォーム方針（§8）
   - `docs/仕様/16_git運用定義書.md` — ブランチ・PR の規律
   - CLAUDE.md「アーキテクチャ」節 — レイヤ構成と実装パターン（③ で `15_アーキテクチャ定義書` へ移る）
   - （④ 以降）`docs/仕様/12_要件定義書.md`・`13_画面定義書/`・`14_データモデル定義書.md`
3. 実装と仕様を突き合わせる

## 設計原則チェックリスト（毎回確認）

- `ItemStatus` の `value`（0 / 1 / 2）を変えていないか（DB に Int で保存されており互換が壊れる）
- 更新系の DAO クエリが `updatedAt` を更新しているか
- スキーマ変更なら `AppDatabase.version` の加算・`Migration` の追加・`RoomMigrationTest.ALL_MIGRATIONS` への登録・`app/schemas/` の新 JSON が揃っているか
- 新しい画面が `Screen`（sealed class）と `NavHost` の両方に登録されているか
- ステータス遷移が Repository に集約されているか（ViewModel や Composable で直接 DAO を叩いていないか）
- 憲章のスコープ外（共有・同期・バックエンド・レシピ・家計簿・バーコード・地図）に踏み込む実装が入っていないか
- 端末内完結（C-3）を崩すネットワークアクセス・アカウントが入っていないか

## docs 品質チェックリスト（差分が docs/ を含むとき）

- **同じ規則を 2 文書に全文で書いていないか** — 詳細は下位文書に 1 か所だけ書き、上位文書は 1〜2 行の要約＋参照リンク（CLAUDE.md「書き方の規約」）
- **本文に判断の経緯を書き足していないか** — 「なぜそうしたか」は同階層の `log_<文書名>.md` へ。本文は現在の仕様のみ
- 台帳の完了エントリがライブ台帳に残っていないか（`closed_` へ移したか）

## 指摘の分類

- **仕様違反**: 実装が仕様条項と食い違う
- **仕様先行違反**: 仕様書に書かれていない挙動レベルの判断がコードに入っている（このプロジェクトは docs 更新が先、が最重要ルール。該当したらどのドキュメントに何を追記すべきかを添える）
- **仕様の曖昧さ**: 仕様がどちらとも読める（オーナーの判断が必要な論点として報告）

## 制約

- 読み取り専用。コード・docs とも変更しない
- **作業ツリーを変える git 操作をしない**（`stash` / `reset` / `checkout -- ` / `clean` / `restore`）——メインセッションの未コミットの作業が消える

## 報告形式

指摘ごとに: 分類 / 重要度（高・中・低）/ `file:line` / 該当仕様条項（文書名と節番号）/ 内容と根拠。
指摘がなければ「指摘なし」とし、照合した仕様条項の一覧を挙げる。ログや長いコード引用は貼らず、要点のみ。
```

- [ ] **Step 4: `.claude/agents/test-reviewer.md` を作る**

```markdown
---
name: test-reviewer
description: テスト観点の正しさ・網羅性を検査する読み取り専用レビュアー。差分にテストファイルが含まれるとき、spec-reviewer と並列で使う。コードは修正しない。
tools: Read, Grep, Glob, Bash
model: opus
---

あなたは Re-Buy プロジェクトのテスト観点レビュアー。テストケースの**網羅性・正しさ**だけを検査する。テストが読めるか（code-quality-reviewer の担当）でも、実装と docs の整合（spec-reviewer の担当）でも、テストの実行合否（verifier の担当）でもない。正常系しか無い・境界値の突き漏らし・常に true になる偽陽性テストといった**テスト設計の穴**を突く。コードの修正は一切行わない（修正はメインセッションの仕事）。

## 手順

1. レビュー対象を特定する。呼び出し時にスコープが指定されていればそれを優先し、なければ `git diff --name-only` でテストファイル（`app/src/test/**`・`app/src/androidTest/**`）と対応する本番コードを把握する
2. 対象に関係する仕様条項を洗い出す（テストが網羅すべき観点の母集団になる）: `docs/仕様/11_憲章.md`、CLAUDE.md「アーキテクチャ」節、（④ 以降）`12_要件定義書` `13_画面定義書` `14_データモデル定義書`
3. テストファイルと本番コードを読み、突き合わせる
4. 下記チェックリストで観点の穴を洗い出す

## テストの段

| 段 | 置き場所 | 対象 | 実行 |
|---|---|---|---|
| ユニット | `app/src/test` | Android 非依存の純粋ロジック（コンバータ・UiState の派生値・Repository の遷移規則） | `./gradlew testDebugUnitTest`（JVM） |
| インストルメンテーション | `app/src/androidTest` | Room（マイグレーション・DAO）・Compose UI | `./gradlew pixel6Api35DebugAndroidTest`（GMD） |

## チェックリスト（毎回確認）

- **条項の取りこぼし**: 実装した仕様条項（憲章 C-x、④ 以降は F-XXX / 画面定義書の §）に対応するテストが揃っているか
- **異常系の網羅**: 例外を投げる経路（`InstantDateFormatStringConverter` の範囲外年、DAO の制約違反）を検証するテストがあるか。正常系だけになっていないか
- **境界値**: `Instant` の 0 年 / 9999 年の両端、`ItemStatus` の 3 状態すべてからの遷移（同じ状態への更新は no-op）、`categoryId = null`（カテゴリ削除で `SET_NULL`）、空リスト、`lastBoughtAt = null`
- **テストの正しさ（偽陽性防止）**: 常に true になる検証・自明すぎる assert になっていないか。`assertEquals(actual, expected)` の引数順が逆で失敗メッセージが読めなくなっていないか。全フィールドを比べるべき所で一部だけ見ていないか
- **段の妥当性**: Android 非依存のロジックを androidTest に置いていないか（遅い）。逆に Room・Compose をユニット段でモックして意味の無い検証にしていないか
- **Room の健全性**: マイグレーションテストが `app/schemas/` の JSON を使っているか。`ALL_MIGRATIONS` に新しい `Migration` が登録されているか。DAO テストが in-memory DB（`Room.inMemoryDatabaseBuilder`）で状態を持ち越していないか
- **コルーチン / Flow**: `Flow` を返す DAO・Repository のテストで最初の emit だけ見て満足していないか。`runTest` を使っているか

## 制約（棲み分けを明確に）

- **読みやすさ・命名・重複は見ない**（code-quality-reviewer の担当）
- **docs 仕様そのものとの整合判定はしない**（spec-reviewer の担当）
- **テストの実行合否・lint・build は扱わない**（verifier の担当）。必要なら結果 XML（`app/build/test-results/` `app/build/outputs/androidTest-results/`）を読むだけに留める
- コードは修正しない。**作業ツリーを変える git 操作をしない**（`stash` / `reset` / `checkout -- ` / `clean` / `restore`）

## 報告形式

指摘ごとに: 重要度（高・中・低）/ カテゴリ（条項漏れ・異常系漏れ・境界値・偽陽性・段違い 等）/ `file:line` / 内容と**追加すべきテストケース**。
指摘がなければ「指摘なし」とし、照合した条項・観点を挙げる。ログや長いコード引用は貼らず、要点のみ。
```

- [ ] **Step 5: frontmatter が壊れていないことと docs-check を確認する**

Run: `for f in .claude/agents/*.md; do echo "== $f"; sed -n '1,6p' "$f" | grep -E '^(name|description|tools|model):' | cut -c1-40; done; sh scripts/docs-check.sh; echo "exit=$?"`
Expected: 4 ファイルそれぞれに `name:` `description:` `tools:` `model:` の 4 行。docs-check は `exit=0`（`.claude/**/*.md` も検査対象に入る）

- [ ] **Step 6: 1 本を実際に起動して動くことを確かめる**

メインセッションで `verifier` を Agent ツール（`subagent_type: "verifier"`）で起動し、「スコープ: docs-check のみ」で頼む。
Expected: `sh scripts/docs-check.sh` を実行し、冒頭に結論、続けて合否 1 行の形式で返る

- [ ] **Step 7: 台帳を閉じてコミット・push する**

```bash
sh scripts/ledger-move.sh T-07 --status '完了 2026-08-29'
sh scripts/docs-check.sh
git add .claude/agents docs/案件/23_技術改善バックログ.md docs/案件/closed_23_技術改善バックログ.md
git commit -m "サブエージェント 4 本を追加（T-07）"
git push origin main
```

---

### Task 8: `.claude/settings.json`（T-08）

**Files:**
- Create: `.claude/settings.json`

**Interfaces:**
- Produces: プロジェクト共有の権限設定（git 管理。個人設定は `.claude/settings.local.json` で上書きする）

- [ ] **Step 1: `.claude/settings.json` を作る**

```json
{
  "permissions": {
    "allow": [
      "Bash(./gradlew build)",
      "Bash(./gradlew build *)",
      "Bash(./gradlew assembleDebug)",
      "Bash(./gradlew assembleDebug *)",
      "Bash(./gradlew assembleDebugAndroidTest)",
      "Bash(./gradlew testDebugUnitTest)",
      "Bash(./gradlew testDebugUnitTest *)",
      "Bash(./gradlew lint)",
      "Bash(./gradlew pixel6Api35DebugAndroidTest)",
      "Bash(./gradlew pixel6Api35DebugAndroidTest *)",
      "Bash(./gradlew installDebug)",
      "Bash(./gradlew tasks *)",
      "Bash(./gradlew --version)",
      "Bash(./gradlew help)",
      "Bash(./gradlew clean)",
      "Bash(sh scripts/docs-check.sh)",
      "Bash(sh scripts/ledger-move.sh *)",
      "Bash(android emulator *)",
      "Bash(android sdk list *)",
      "Bash(android docs *)",
      "Bash(adb devices*)",
      "Bash(git push -u origin t-*)",
      "Bash(git push -u origin f-*)",
      "Bash(git push -u origin n-*)",
      "Bash(git push -u origin fb-*)",
      "Bash(git push --force-with-lease origin t-*)",
      "Bash(git push --force-with-lease origin f-*)",
      "Bash(git push --force-with-lease origin n-*)",
      "Bash(git push --force-with-lease origin fb-*)",
      "Bash(gh pr view*)",
      "Bash(gh pr list*)",
      "Bash(gh pr diff*)",
      "Bash(gh pr checks*)",
      "Bash(gh pr create*)",
      "Bash(gh run view*)",
      "Bash(gh run list*)",
      "Bash(gh run watch*)",
      "Bash(gh repo view*)",
      "Bash(gh api repos/*)"
    ],
    "ask": [
      "Bash(gh pr merge*)",
      "Bash(gh pr close*)",
      "Bash(gh pr edit*)",
      "Bash(gh repo edit*)",
      "Bash(gh release*)",
      "Bash(gh run rerun*)",
      "Bash(gh run cancel*)",
      "Bash(gh workflow run*)",
      "Bash(git push origin main*)",
      "Bash(android sdk install *)",
      "Bash(android sdk remove *)"
    ],
    "deny": [
      "Bash(keytool *)",
      "Bash(apksigner *)",
      "Bash(jarsigner *)",
      "Bash(openssl *)",
      "Bash(security *)",
      "Bash(cat *.jks)",
      "Bash(cat *.keystore)",
      "Bash(cat *.p12)",
      "Bash(cat *.pfx)",
      "Bash(cat *.pem)",
      "Bash(cat *.key)",
      "Bash(cat keystore.properties)",
      "Read(**/*.jks)",
      "Read(**/*.keystore)",
      "Read(**/*.p12)",
      "Read(**/*.pfx)",
      "Read(**/*.pem)",
      "Read(**/*.key)",
      "Read(**/keystore.properties)"
    ]
  }
}
```

- [ ] **Step 2: JSON として妥当なことを確認する**

Run: `python3 -c "import json; d=json.load(open('.claude/settings.json')); print(len(d['permissions']['allow']), len(d['permissions']['ask']), len(d['permissions']['deny']))"`
Expected: `41 11 19`

- [ ] **Step 3: deny が効くことを確かめる**

メインセッションで `Bash(cat keystore.properties)` 相当（`cat keystore.properties`）を実行しようとする。
Expected: 権限で拒否される（ファイルが無いことによるエラーではなく、実行前に止まる）

- [ ] **Step 4: 台帳を閉じてコミット・push する**

```bash
sh scripts/ledger-move.sh T-08 --status '完了 2026-08-29'
sh scripts/docs-check.sh
git add .claude/settings.json docs/案件/23_技術改善バックログ.md docs/案件/closed_23_技術改善バックログ.md
git commit -m ".claude/settings.json を追加（T-08）"
git push origin main
```

---

### Task 9: CLAUDE.md の全面改訂と README（T-09）

**Files:**
- Rewrite: `CLAUDE.md`
- Create: `README.md`

**Interfaces:**
- Consumes: Task 2〜8 の成果物の名前（docs のパス、`scripts/*.sh`、agent 名、GMD タスク名）

- [ ] **Step 1: 現 CLAUDE.md の「アーキテクチャ」節（`## アーキテクチャ` から `### OSS ライセンス表示` の段落まで）と「ビルド構成の注意点」の箇条書きを控えておく**

Run: `sed -n '/^## ビルド構成の注意点/,/^## アーキテクチャ/p' CLAUDE.md | head -20; sed -n '/^## アーキテクチャ/,$p' CLAUDE.md | wc -l`
Expected: 箇条書き 6 項目と、アーキテクチャ節 40 行前後。Step 2 の該当節にそのまま貼る

- [ ] **Step 2: `CLAUDE.md` を次の内容で書き直す（「ビルド構成」「アーキテクチャ」の本文は Step 1 で控えた現行内容を入れる）**

```markdown
# CLAUDE.md — Re-Buy 開発ガイド

## プロジェクト概要

Re-Buy（仮称）は「くりかえし使える買い物リスト」。品目をプールに常駐させ、買い物のたびにカゴへ入れ → 店でチェック → 終了でプールに戻す（最終購入日付き）。対象は一人で買い物する人。端末内で完結し、Android と iOS に出す。**判断に迷ったら [憲章](./docs/仕様/11_憲章.md) の価値 C-1〜C-5 とスコープ外表に照らす。** 現在地は [ロードマップ](./docs/案件/24_ロードマップ.md)。

アプリ内の表示文言・コードコメント・docs・コミットメッセージは日本語。

## 最重要ルール: 仕様変更は docs/ 反映が先

挙動の契約はドキュメント群にある。実装中に挙動レベルの仕様判断が発生した場合、**コードより先に該当ドキュメントを更新する**。判断はオーナーとの対話でのみ決め、AI が単独で決めない。

- 価値・スコープ・収益モデル: `docs/仕様/11_憲章.md`
- 要件・画面・データモデル: `docs/仕様/12〜14`（④ で作る。それまでは憲章と本書「アーキテクチャ」節が正）
- 挙動を変えない技術活動（リファクタ・依存追随・CI/ツール整備）: `docs/案件/23_技術改善バックログ.md`（**T-XX**。挙動が絡むなら要求軸、絡まないならこちら）
- 未実装要求の案件管理: `docs/案件/22_要件バックログ.md`（④ から）

## docs の構造と書き方の規約

ファイル名は**全体で一意な 2 桁番号**を持つ（1X = 仕様 / 2X = 案件 / 3X = 検討。新規文書は該当ブロックの次番号）。`log_` / `closed_` / `guide_` / `archive_` の付属ファイルは親の番号を引き継ぐ。予約: 12・13・14・21（④）、15・17（③）。

- **詳細は下位文書に 1 か所だけ書く**。上位文書は 1〜2 行の要約＋参照リンク
- **判断の経緯（「なぜそうしたか」）は本文に書かず、同階層の `log_<文書名>.md` へ日付順に積む**。本文は現在の仕様だけ。決定ログを持たない文書は最初のエントリが出た時点で作る
- **台帳（22・23）の完了エントリは、状態更新と同じコミットで同階層の `closed_<文書名>.md` へ移す**（`sh scripts/ledger-move.sh T-XX`）
- **文書の分離・改番・節の移動をしたら、旧参照を grep で洗い出して追随させてから閉じる**。参照は docs 内だけでなく本書・README・`.claude/` にも散る
- 表のセル内に `|` を書かない（`docs-check` が列を数えられなくなる）
- 機械検査は `sh scripts/docs-check.sh`（表構造・台帳の書式・リンクとアンカーの実在。CI の `docs` ジョブが同じものを回す）

## 技術スタックとビルド

Kotlin + Jetpack Compose、単一モジュール `:app`（③ で KMP＋マルチモジュールへ）。Room / Hilt / Navigation Compose / AboutLibraries。

- `applicationId` / `namespace`: `io.github.obaya884.rebuy`（逆ドメイン部分は ⑤ の公開前に再検討）
- minSdk 31 / compileSdk 37 / targetSdk 35 / Java・JVM target 17
- AGP 9 / Gradle 9。JDK は 17 以上（Android Studio 同梱の JBR 25 で動作確認済み）
- ビルドスクリプトは Kotlin DSL。依存は必ず `gradle/libs.versions.toml` 経由で追加する

### ビルド構成の注意点

（Step 1 で控えた現行の箇条書き 6 項目をここに置く）

## アーキテクチャ

（Step 1 で控えた現行の「アーキテクチャ」節をここに置く。冒頭に次の 1 行を足す）

> ③ で `docs/仕様/15_アーキテクチャ定義書.md` へ移す。それまでは本節が正。

## サブエージェント運用

実装サイクルの各段階で以下へ委譲し、メインセッションのコンテキストを実装そのものに集中させる。読み取り中心・出力が長くなる作業を委譲し、**コードの編集はメインセッションが行う**。

| 段階 | 委譲先 | 内容 |
|---|---|---|
| 着手前の調査 | 組み込み `Explore` | 既存コードの把握・影響範囲の特定（結論だけ受け取る） |
| 実装方式の設計 | 組み込み `Plan` | 実装方針の立案・トレードオフ整理 |
| 実装の一区切り | `verifier` | build（lint・unit test）・GMD でのインストルメンテーションテスト・docs 検査 |
| 実装の一区切り | `code-quality-reviewer` | 可読性・重複・命名・簡潔さ（verifier と並列起動可） |
| docs の条項に触れたとき | `spec-reviewer` | docs との整合＋設計原則チェック |
| テストファイルに触れたとき | `test-reviewer` | テスト観点の網羅性・正しさ（spec-reviewer と並列起動可） |

- **挙動レベルの仕様判断は委譲しない**。オーナーとの対話で決め、docs/ を先に更新する
- **実装は原則メインセッションが行う**。仕様が文書で完結している自己完結的な純粋ロジックに限り、条項を指定して `general-purpose` に委譲してよい
- レビュー・検証の 4 本は互いに独立なので**バックグラウンドで並列起動**し、結果を待つ間に次の作業を進めてよい
- 軽微な変更（typo・文言・1 ファイルの小修正）ではエージェントを起動せず、メインセッションで直接 build する
- 指摘への対処（修正）はメインセッションが行い、必要なら同じエージェントを新しく起動して再検証させる

### 起動のコスト

サブエージェントは 1 本ごとに独立したリクエストを持つ。読み取り中心の調査は `Explore` を既定にし、`general-purpose` は編集を伴う自己完結タスクに限る。レビュー系のモデル指定（verifier・spec-reviewer = sonnet ／ code-quality-reviewer・test-reviewer = opus）は変えない——落とすと見逃しが「指摘なし」として返り、正常な合格と区別できない。タスクの切れ目で文脈を切る。

### 起動の契機

レビュー 4 本の起動は「作業の種類」ではなく「**差分が何に触れたか**」で引く（`git diff --name-only` で機械的に決まる）。

- **着手前** — 影響範囲が読み切れないとき: `Explore`
- **実装方式で迷うとき** — トレードオフのある設計判断: `Plan`
- **実装が一区切りしたら（コミット前）** — `verifier` と `code-quality-reviewer` を並列でバックグラウンド起動。軽微な変更では起動しない
- **差分にテストファイル（`app/src/test/**`・`app/src/androidTest/**`）が含まれるとき** — `test-reviewer`
- **差分が docs の条項に触れる／条項で定まる挙動を実装したとき** — `spec-reviewer`

## 実装完了後のフロー（レビュー → 動作確認 → コミット/push）

実装が一区切りしたら次の順序を必ず踏む。**サブエージェントのレビューとオーナーの動作確認の両方を経るまで「完了」扱いにしない。**

1. **サブエージェントによるレビュー**: `verifier`・`code-quality-reviewer` は常に、`test-reviewer`・`spec-reviewer` は「起動の契機」の条件に当たれば並列起動し、指摘があればメインセッションで対処する
2. **オーナーによる動作確認**: メインセッションが `./gradlew installDebug`（実機）または `android emulator start` → `installDebug`（エミュレータ）で入れ、**確認手順を提示する**。オーナーが実機で挙動を確認する
3. **コミット / push**: 上記 2 つを通してからコミットする。push・マージはオーナーの合図を待つ

軽微な変更は 1・2 を省略してよい。

## git 運用

**正は `docs/仕様/16_git運用定義書.md`。** 常時使う判断は次の 2 つ:

- **リスク別ルーティング**: docs・軽微・ツール整備 → `main` 直コミット／コード・挙動の変更 → ブランチ → PR → CI 緑 → 動作確認 → **合図で squash マージ**。迷ったら PR 側
- **ブランチ命名**: `<id>-<slug>`（例 `t-12-kover`、`f-101-basket-badge`）。**1 ブランチ＝1 PR、マージ後は削除**。push 済みの main から切る

## 開発コマンド

- `./gradlew build` — lint・unit test・debug/release の assemble
- `./gradlew testDebugUnitTest` — ユニットテスト（JVM）。単一クラス: `--tests "io.github.obaya884.rebuy.InstantDateFormatStringConverterTest"`
- `./gradlew pixel6Api35DebugAndroidTest` — インストルメンテーションテスト（Gradle Managed Device。エミュレータの手動起動は不要。初回はイメージのダウンロードで数分）
- `./gradlew installDebug` — 端末・エミュレータへインストール
- `./gradlew clean` — KSP（Hilt / Room）の生成コードが壊れたとき
- `sh scripts/docs-check.sh` — docs・本書・README・`.claude/` の機械検査
- `sh scripts/ledger-move.sh T-XX [--status '完了 YYYY-MM-DD']` — 台帳 23 のエントリを完了記録へ移す
- `android emulator list` / `android emulator start <name>` — エミュレータ（`android` CLI）

## このリポジトリは public

`obaya884/Re-Buy` は公開リポジトリ。**到達手段・実データ・資格情報につながる記述はコミットしない**。

- 署名鍵（`*.jks` `*.keystore`）・`keystore.properties`・Play Console / App Store Connect の内部 ID・API キーは値も所在も書かない（`.claude/settings.json` の deny で読み取りも塞いである）
- オーナーの生活が読み取れる実データ（品目名・カテゴリ名）を docs の例示に使わない
- 一度 push した内容は履歴から消せない。迷ったら書かない
- ソースは閲覧のみ可（README「ライセンス」）。他者の PR は受け付けない

## 規約

- **コードの変更は原則 Edit / Write ツールで行う**。**判断を伴う変更は 1 箇所ずつ Edit する**——どこを直すかを都度考えることに意味があり、まとめて置換すると文脈に応じた直し（周りのコメントの追随など）が抜ける
- **例外は「意味を変えない機械的な一括置換」**（識別子の改名など）。この場合は python / sed 等を使ってよいが、**実行直後に `git diff` を提示する**——部分一致で意図しない箇所まで変わっていないかをオーナーが見られるようにする
- 変異テスト（コードを壊してテストが落ちるか確かめる）はメインセッションが Edit で 1 か所ずつ行い、`verifier` には変異後の実行だけを頼む。git で退避しない
```

- [ ] **Step 3: `README.md` を作る**

```markdown
# Re-Buy（仮称）

くりかえし使える買い物リスト。品目を使い捨てにせず、買い物のたびにカゴへ入れ → 店でチェック → 終了でプールに戻す。一人で買い物する人のための、端末内で完結するアプリ。

- [憲章](./docs/仕様/11_憲章.md) — 何のために・誰のために・何をしないか
- [ロードマップ](./docs/案件/24_ロードマップ.md) — 現在地と、この先の順序

## ビルド

JDK 17 以上と Android SDK（compileSdk 37）が必要。

```sh
./gradlew assembleDebug                 # デバッグビルド
./gradlew build                         # lint・unit test 込み
./gradlew pixel6Api35DebugAndroidTest   # インストルメンテーションテスト（Gradle Managed Device）
```

## 開発体制

Claude Code による AI 主導開発。運用は [CLAUDE.md](./CLAUDE.md)、仕様は `docs/` が正。

## ライセンス

本リポジトリのソースコードおよびドキュメントは閲覧のために公開している。**複製・改変・再配布・商用利用は許諾しない。** 著作権は obaya884 が保持する。Pull Request は受け付けない。
```

- [ ] **Step 4: docs-check とリンクの実在を確認する**

Run: `sh scripts/docs-check.sh; echo "exit=$?"`
Expected: `exit=0`（本書・README の相対リンクとアンカーも検査される）

- [ ] **Step 5: CLAUDE.md が Claude Code に正しく読まれることを確認する**

新しい Claude Code セッションを開く代わりに、メインセッションで `Explore` エージェントに「CLAUDE.md を読み、レビュー 4 本の起動の契機を要約して」と頼む。
Expected: 差分ベースの 4 条件が返る

- [ ] **Step 6: 台帳を閉じてコミット・push する**

```bash
sh scripts/ledger-move.sh T-09 --status '完了 2026-08-29'
sh scripts/docs-check.sh
git add CLAUDE.md README.md docs/案件/23_技術改善バックログ.md docs/案件/closed_23_技術改善バックログ.md
git commit -m "CLAUDE.md を運用ガイドへ全面改訂し、README を追加（T-09）"
git push origin main
```

---

### Task 10: リポジトリの public 化（T-10）

**Files:**
- Modify: `docs/案件/24_ロードマップ.md`（② 完了）
- Move: `docs/案件/25_開発基盤実装計画.md` → `docs/案件/archive_25_開発基盤実装計画.md`

**Interfaces:**
- Consumes: Task 1〜9 がすべて完了し、台帳 23 の T-01〜T-09 が `closed_23` にあること

- [ ] **Step 1: 前提を確認する**

Run: `grep -c '^| T-' docs/案件/23_技術改善バックログ.md; git status --short | wc -l; git status -sb | head -1`
Expected: `1`（T-10 のみ）、`0`、`## main...origin/main`

- [ ] **Step 2: 履歴に資格情報・到達手段が無いことを確認する**

Run: `git log -p --all | grep -inE 'password|secret|api[_-]?key|token|keystore|\.jks|storePassword|keyPassword' | grep -v 'settings.json\|CLAUDE.md\|31_開発基盤検討\|25_開発基盤実装計画\|archive_25' | head`
Expected: 出力なし（設定・docs で deny 対象の語を列挙している行は除外している）。もし実際の値が出たら**public 化を中止**し、オーナーに報告する（履歴の書き換えはオーナー判断）

Run: `git log --all --diff-filter=A --name-only --format= | grep -iE '\.(jks|keystore|p12|pfx|pem|key)$|keystore\.properties|local\.properties' | sort -u`
Expected: 出力なし

- [ ] **Step 3: ロードマップを更新する**

`docs/案件/24_ロードマップ.md`:
- `- ステータス: 確定（① 完了、② に着手）` → `- ステータス: 確定（①② 完了、③ に着手）`
- `- 更新日: 2026-08-29` は完了当日の日付に
- §4 の「### ② 開発基盤」の末尾に `- **完了（2026-MM-DD）**。成果物は [開発基盤検討](../検討/31_開発基盤検討.md) §12 のとおり。実装の記録は [archive_25_開発基盤実装計画](./archive_25_開発基盤実装計画.md)` を追加

- [ ] **Step 4: 本書を凍結する**

```bash
git mv docs/案件/25_開発基盤実装計画.md docs/案件/archive_25_開発基盤実装計画.md
```

`docs/案件/archive_25_開発基盤実装計画.md` の冒頭「位置づけ」行の末尾に `**② 完了により凍結（2026-MM-DD）。以後は書き換えない**` を足す。`docs/案件/23_技術改善バックログ.md` の T-10 の詳細節や他文書に `25_開発基盤実装計画.md` への参照があれば `archive_25_...` へ直す（`grep -rn '25_開発基盤実装計画' docs CLAUDE.md README.md`）。

- [ ] **Step 5: 台帳を閉じ、docs-check を通してコミット・push する**

```bash
sh scripts/ledger-move.sh T-10 --status '完了 2026-08-29'
sh scripts/docs-check.sh; echo "exit=$?"
git add docs
git commit -m "ロードマップ ② を完了にし、実装計画を凍結（T-10）"
git push origin main
```

Expected: `exit=0`。CI が main で緑（`gh run watch`）

- [ ] **Step 6: オーナーの合図を待って public にする（メインセッションが行う）**

サブエージェントはここで止まり、Step 2 の結果と CI の状態を報告する。オーナーが承認したらメインセッションが:

```bash
gh repo edit obaya884/Re-Buy --visibility public --accept-visibility-change-consequences
gh repo view obaya884/Re-Buy --json visibility --jq .visibility
```

Expected: `PUBLIC`

- [ ] **Step 7: ローカルディレクトリの移動をオーナーに案内する（セッションの切れ目に）**

spec §11 の手順をそのまま提示する:

1. Android Studio を閉じる
2. `mv ~/FavBasket ~/Re-Buy`
3. `cp -R ~/.claude/projects/-Users-obayashitakumi-FavBasket ~/.claude/projects/-Users-obayashitakumi-Re-Buy`
4. `cd ~/Re-Buy` で Claude Code を起動し、Android Studio で開き直す

---

## 自己レビュー

- spec §3（リポジトリ）→ Task 1・9（README）・10（public 化・履歴確認）
- spec §4（docs 構造）→ Task 2。`closed_22` は docs-check の `LEDGERS["22"]["closed"]` が要求するため spec に無いが追加した
- spec §5（CLAUDE.md）→ Task 9
- spec §6（agents）→ Task 7
- spec §7（scripts）→ Task 3・4
- spec §8（CI・Dependabot・GMD）→ Task 5・6
- spec §9（settings.json）→ Task 8
- spec §10（git 運用）→ Task 2（16 の作成）・Task 6（PR を 1 本通す）
- spec §11（ディレクトリ移動）→ Task 10 Step 7
- spec §12（完了条件）→ 「③ の最初の PR でレビュアー 4 本が回る」だけは ③ で確認。それ以外は Task 10 までで満たす
- 名前の整合: GMD タスク名 `pixel6Api35DebugAndroidTest`（Task 5・6・7・8・9）、スクリプト名 `docs-check.sh` / `ledger-move.sh`（Task 3・4・7・9）、agent 名 4 つ（Task 7・9）、台帳パス（Task 2・3・4）は全タスクで同一
