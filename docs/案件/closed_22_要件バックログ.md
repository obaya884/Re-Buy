# 要件バックログ完了記録

- 作成日: 2026-08-29
- 目的: 完了した要求（F-XXX / N-XX）の記録を保管する。ライブ台帳は[要件バックログ](./22_要件バックログ.md)
- 並びは**新しい順**（先頭に積む）。移送は手作業（1 行を `## ID タイトル（完了日）` の節に展開する）

## F-012 カテゴリの管理画面（2026-09-01）

- 対応: 画面 09 と 09b を `screen/manage/` に新設し、**旧カテゴリー画面を置き換えた**。ハンドル ≡ を掴んで並び替え（離した時点で保存）、末尾の破線行から 02b、行の長押しで 09b（保存・削除）。削除は確認ダイアログで**影響を件数で示し**、品目は消えない
- **09 は同型の 2 画面**（画面定義書）なので、最初からルートの `Screen.Manage(target)` で切り替わる 1 つの画面として作った。**行き先向きも同じ画面が動く**——F-013 に残るのは設定の行と行き先向きの網だけ
- 並び替えの保存は `@Transaction` で 1 回の変更にし、**渡された順に 1..n を振り直す**（1 件だけ動かす API では、詰まった連番で周りが押し出される）。位置が変わらない行は書かない
- ドラッグは `Modifier.draggable` で取る。**自前の `detectDragGestures` では掴めなかった**（タッチスロップの扱いで、テストからも実機からも drag が始まらない）
- 併せて消えたもの: `TextFieldAddDialog` / `TextFieldEditDialog`（唯一の呼び出し元が旧画面だった。[T-38](./closed_23_技術改善バックログ.md#t-38) を「対処不要」で完了）。`NewNameTarget` は 09 でも使うので `NameTarget` へ改名した
- **ハンドルはドラッグ、名前は長押し**と受け口を分けた。カード全体で長押しを取ると、`draggable` がタッチスロップを超えるまで何も消費しないので、**ハンドルを掴んだまま止めた瞬間に編集シートが開く**（テストレビューが検出し、変異で実測）
- 反映先: [要件定義書](../仕様/12_要件定義書.md) §5.2（F-013 は画面ではなく行と網に絞った）／[画面定義書](../仕様/13_画面定義書.md) 画面 09（長い名前は 1 行に省略）・[決定ログ](../仕様/log_13_画面定義書.md) ／ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3・§6 ／ PR #41
- 残したもの: **端まで動かしたときの自動スクロールと、読み上げからの並び替え**は [T-54](./23_技術改善バックログ.md#t-54)。件数が画面 1 枚に収まるうちは前者が表面化しない

## F-011 設定画面（2026-09-01）

- 対応: 画面 07 を条項どおりに作り直した。行は「カテゴリの管理」「テーマ」（右端に現在のテーマ名）「ライセンス」で、最下部にバージョン「Re-Buy x.y.z」。**利用規約・プライバシーポリシー・問い合わせの行は消した**（画面 07。リリース前まで出さない）
- パレットの表示名の対応（`ThemePalette.labelResource()`）を 07 と 08 で共有できる場所へ出した
- 併せて直したもの: **テストの実行順で結果が変わる形**を塞いだ。`ThemeRepository` は `single` で生成時に 1 度だけ保存先を読むので、テーマを変えるテストの後ろに並んだテストが巻き添えになる。`startTestKoin` が既定へ選び直すようにした（07 のテストで実際に踏んだ）
- バージョンは**画面の下端に固定**する（オーナー判断。[決定ログ](../仕様/log_13_画面定義書.md) 2026-09-01）。条項の「最下部に」は行の末尾とも読めたので、13 の文言も確定させた
- 反映先: [要件定義書](../仕様/12_要件定義書.md) §5.2（「行き先の管理」への導線を F-013 へ移した）／[画面定義書](../仕様/13_画面定義書.md) 画面 07・[決定ログ](../仕様/log_13_画面定義書.md) ／ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 ／ PR #40
- 残したもの: 「行き先の管理」の行は F-013（管理画面がまだ無い）。**この振り分けは要件定義書の F-011 / F-013 の定義文にも反映した**——台帳にだけ書くと、12 が F-011 に含めたままの導線が「完了」と食い違う（仕様レビューが検出）。「カテゴリの管理」は旧画面へ暫定で繋いである（F-012）

## F-010 気づいたものを足すシート（2026-09-01）

- 対応: 画面 05 を `screen/add_noticed/` に新設し、04 の一覧末尾に破線行「＋ 気づいたものを足す」を足した。初期表示は未追加のもの（今の行き先 → 区切り → どこでも買えるもの）、打ち始めると単純な部分一致で全品目を探し、**追加済みも当たりに含めて「追加済み」と添える**——見つからないと、もう入れたのか入れ忘れたのかが分からないため
- **他の行き先のものを足しても今の一覧には現れない**ので、スナックバー「◯◯に追加しました」で知らせる（§2 の「画面に変化が見えない操作」）。行にも行き先名を添える（[決定ログ](../仕様/log_13_画面定義書.md) 2026-09-01）
- 「＋ この名前で登録する」は行き先＝今の店・カテゴリなしで登録して即カゴ入り。検証は §2 と同じで、弾かれたらシートは閉じない
- 04 の `SystemBackHandler` はシートが開いている間は受けないようにした（画面 04 の「05 表示中のバックはシートを閉じるだけ」）。**最初の実装では横取りしていた**——テストレビューが検出し、instrumented に網を足して変異で確かめた
- 反映先: [画面定義書](../仕様/13_画面定義書.md) 画面 05 ／ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 ／ PR #39

## F-009 買い物モード（2026-09-01）

- 対応: 画面 04 を新設。行き先ごとの一覧（選んだ行き先の品目 → 区切り「どこでも買えるもの」）・アプリバーの進捗「x / n」・行タップでのチェックの付け外し・「買い物を終了する」で**一覧のチェック済みだけ**をプールへ戻す・← とシステムバックの離脱確認。03 の行タップが `Screen.Shopping(destinationId)` へ繋がり、**暫定配線が無くなった**
- 併せて片づけたもの: 買い物側にだけ残っていたボトムナビ（`BottomNavigation*` 3 ファイル）を削除し、**backstack を 1 本に畳んだ**（タブが無くなったため。`Navigator.navigateAsRoot` → `popToRoot`）。旧画面の終了時 500ms のスピナーも消えた（[T-27](./closed_23_技術改善バックログ.md#t-27)）
- 反映先: [画面定義書](../仕様/13_画面定義書.md) 画面 04・[決定ログ](../仕様/log_13_画面定義書.md) ／ [アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md) §4.1 ／ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3・§6 ／ PR #38
- 残したもの: 一覧末尾の破線行「＋ 気づいたものを足す」は F-010

## F-008 買い物開始シート（2026-09-01）

- 対応: 画面 03 を `screen/shopping_start/` に新設し、**プールの CTA の暫定配線を置き換えた**。カゴの中身を行き先別に出し（行＝行き先名＋登録順の先頭 2 件のプレビュー＋「n＋m 件」）、行タップで買い物へ入る。行き先付きが 1 件も無いときは「n 件で開始」の 1 行だけ
- **どこでも買えるものは独立した行にせず、各行の件数に「＋m」として足す**（どの店へ行っても持っていくものなので、行き先ごとの見出しの下に重複させない）
- 反映先: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 ／ PR #37
- 暫定で残したもの: 行タップの先の 04 買い物モードは F-009。それまで行き先を持たない旧画面へ繋いである

## F-007 品目編集シート（2026-09-01）

- 対応: 画面 06 を `screen/item_edit/` に新設し、**プールの行の長押しから開く**。保存は名前・カテゴリ・行き先を 1 回のトランザクションで反映し（名前が弾かれたら何も書かない）、削除は確認ダイアログを挟む。「なし」チップで選択を外せる
- 02 と共通する部分（チップ列・02b のダイアログとその保存）を `ChipRow` / `NewNameDialog` / `NewNameEditor` へ切り出し、文言とテストタグも `item_form_*` の中立な名前に揃えた
- **旧アイテム一覧画面を削除**。品目の登録・編集・削除がすべて新画面に揃った
- 反映先: [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 ／ PR #36
- 途中で直したもの: **ボトムシートが半開きで下のボタンが画面の外に出ていた**件（2 つのシートとも全開に）

## F-006 登録シート（2026-09-01）

- 対応: 画面 02（登録シート）と 02b（新しいカテゴリ／行き先ダイアログ）を `screen/register/` に新設し、**プールの ＋ の暫定配線を置き換えた**。名前だけで登録でき、「続けて登録」は名前だけ消してチップを残す。02b で作ったものは選択済みで現れる（そのため `SaveResult.Saved` が書いた行の id を返すようにした）
- 反映先: [画面定義書](../仕様/13_画面定義書.md) §2（チップの解除作法を画面 01 から共通作法へ。経緯は [log_13](../仕様/log_13_画面定義書.md)）／[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md)（シートとダイアログは画面ではない・自前の ViewModel を持つなら閉じるときに捨てる）／[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 ／ PR #35
- 途中で直したもの: `NameTextField` の `modifier` が外枠に付いていて**自動フォーカスが効いていなかった**件。**一度登録すると二度とシートが開けなくなる**件（ViewModel がシートより長生きするため）
- 暫定で残したもの: 品目の編集・削除は 06（F-007）まで設定の下の旧アイテム一覧

## F-005 プール画面（2026-09-01）

- 対応: 画面 01 を `screen/pool/` に新設し、**旧ホーム画面を置き換えてルートを `Screen.Pool` に改名**。一覧（登録順・カテゴリ/行き先タグ・前回 M/D）、行タップでカゴの出し入れ、絞り込みチップ（AND・「どこでも」は行き先なしだけ・個別解除）、下部 CTA（カゴ件数バッジ・0 件で無効）、空状態と絞り込み 0 件の出し分け。一覧の日付用に `formatMonthDay` を新設
- 反映先: [画面定義書](../仕様/13_画面定義書.md) 画面 01（チップの個別解除・行き先が無い行はタグを出さないこと。経緯は [log_13](../仕様/log_13_画面定義書.md)）／[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3・§6 ／[アーキテクチャ定義書](../仕様/15_アーキテクチャ定義書.md)（派生値の置き方の例示）／ PR #34
- 暫定で残したもの: ＋ と CTA は旧画面への配線（`// 暫定:` で grep）。行の長押しは F-007。カテゴリの管理は設定の下（F-011）。買い物画面のボトムナビは F-009 まで

## F-004 テーマとデザイン基盤（2026-09-01）

- 対応: 配色トークン `ReBuyColors`（11 種 × 3 パレット × 明暗）と Material の `ColorScheme` への橋渡し、角丸（行 13 / CTA 15 / シート 22dp）、等幅数字、テーマ画面（画面 08。タップで即時反映）、選択の端末内保存（`SettingsStore`）。書体は丸ゴシック（Zen Maru Gothic Bold）を見出しと CTA にだけ同梱し、本文は OS 既定
- 反映先: [画面定義書](../仕様/13_画面定義書.md) §5（書体の同梱方針。経緯は [log_13](../仕様/log_13_画面定義書.md)）／[データモデル定義書](../仕様/14_データモデル定義書.md) §9 ／[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 ／[アーキテクチャ定義書の決定ログ](../仕様/log_15_アーキテクチャ定義書.md)（設定の保存機構）／ PR #33
- ついでに塞いだもの: iOS の画面テストで `DestinationDao`（F-002 で追加）が fake に差し替わっていなかった件。生成物 `aboutlibraries.json` がライセンス本文の取得元で揺れて CI が落ちる件（同梱フォントのライセンスを手元に持つ形へ）
- 残したもの: 設定画面のテーマ行に現在のテーマ名を出すのは F-011

## F-003 名前のバリデーション（2026-09-01）

- 対応: `NameRule`（トリム・空・30 文字上限・打ち切り）と `NameError` / `SaveResult` を `:shared:domain` に置き、検証の手順を `saveWithValidatedName` 1 か所に集約。3 つの DAO に `existsName(name, exceptId)` を足し、Repository の保存系は結果型を返してトリム後の名前を保存する。UI 側は `NameTextField`（打ち切りとエラー表示）を新設し、弾かれたらダイアログを開いたままにする
- 反映先: [画面定義書](../仕様/13_画面定義書.md) §2（上限超えの文言と「エラーは必ず文言を持つ」規則。経緯は [log_13](../仕様/log_13_画面定義書.md)）／[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3・§6 ／[アーキテクチャ定義書の決定ログ](../仕様/log_15_アーキテクチャ定義書.md)（`nameError` を `uiState` に畳まない判断）／ PR #32
- ついでに直したもの: 打ち切りがサロゲートペアを割る穴、改名ダイアログの `SideEffect` による毎回代入。**F-002 で開いた重複名のクラッシュ 4 経路もここで塞いだ**

## F-002 行き先とデータモデルの刷新（2026-09-01）

- 対応: destinations テーブルと `Destination` / `DestinationDao` / `DestinationRepository` を新設。`items.destinationId`（外部キー・`SET_NULL`）、`categories` / `destinations` の `sortOrder`（新規は末尾）、3 テーブルの `name` に UNIQUE インデックス。`OnConflictStrategy` は `REPLACE` から `ABORT` へ（同名の登録で既存の行が消えるのを止めるため）
- 反映先: [データモデル定義書](../仕様/14_データモデル定義書.md) §2・§4・§6・§7・§8 ／ [テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3・§6 ／ PR #31
- 残したもの: 破壊的マイグレーションを外す作業は [T-51](./23_技術改善バックログ.md#t-51)。重複名で旧画面が落ちる 4 経路は F-003 へ申し送り

## F-001 品目名・カテゴリー名の入力を検証する（2026-08-31 吸収）

- 対応: 実装せず、④ で起こした F-003（名前のバリデーション。[要求定義書](../仕様/11_要求定義書.md) §6）へ吸収した。空・前後の空白・重複の扱いは[データモデル定義書](../仕様/14_データモデル定義書.md) §5 で確定
- 経緯: ③ の動作確認で発見（2026-08-30）。オーナー判断で ④ にまとめた
