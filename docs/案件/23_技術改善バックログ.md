# 技術改善バックログ

- 作成日: 2026-08-29
- 目的: 技術負債の返済・リファクタリング・開発ツール整備など、**プロダクト挙動を変えない技術活動**を、要求（F-XXX / N-XX）とは別軸で管理・計画する
- 関連文書: [要件バックログ](./22_要件バックログ.md)（プロダクト軸の台帳）

## この文書の守備範囲

プロダクトの**挙動**に関わる変更は扱わない。ここに書くのは、ユーザーから見た挙動を変えずに**開発の健全性を上げる活動**——依存追随・CI/ツール整備・内部設計の改善・テスト補強・調査など。

- 採番は **T-XX**。1 系列で通し、種別で区別する
- 種別: **何に触るか＝着手時に何が網になるか**で分ける（下表）
- 優先度: `高` / `中` / `低` / `様子見`
- 状態: `未着手` / `進行中` / `完了` / `保留`
- **1 エントリ＝[一覧](#一覧)の 1 行＋[詳細](#詳細)の 1 節**。一覧は索引に徹し `ID / タイトル / 種別 / 優先度 / 状態 / 詳細` の 6 列を持つ
  - **見出しは `### T-XX` の ID のみ**
  - **セル内に `|` を書かない**
  - **種別・優先度・状態を詳細節に書かない**。一覧が正
  - 詳細節の項目は `背景`（必須）に続けて `対応方針` / `着手条件` / `優先度の根拠` / `関連` を必要なだけ
  - 詳細節の並び順は一覧と同じにする
- **完了になったエントリは、状態更新と同じコミットで[closed_23_技術改善バックログ.md](./closed_23_技術改善バックログ.md)へ移す**（本書には未着手・進行中・保留だけを残す）。**一覧の行と詳細節はセットで移す**——`sh scripts/ledger-move.sh T-XX` が両方を移す
- 判断の経緯は本文に書かず、`log_23_技術改善バックログ.md` へ日付順に積む（最初のエントリが出た時点で作る）

### 種別

| 種別 | 何に触るか | 着手時に何が網になるか |
|---|---|---|
| 内部設計 | 本番コード（`androidApp/src/main`・`shared/*/src/{commonMain,androidMain,iosMain}`）の構造（挙動不変） | 既存テスト。**変異させて振る舞い保存を確かめる** |
| テスト | テストコードのみ（本番コード不変） | 本番コードに触れないので回帰の心配がない |
| ツール整備 | `scripts` / CI / `.claude` / 開発環境 | **自動テストの網が無い**。実行して確かめる |
| 依存追随 | 依存ライブラリ・Gradle・AGP の更新 | 上流待ちが多く自分では動かせない。**再確認の契機を必ず書く** |
| 調査 | 何も変えない。判断を記録する | 成果物は決定そのもの |

## 現況

④ の MVP 実装フェーズ（主戦場はプロダクト軸の[要件バックログ](./22_要件バックログ.md)）。③ の残りは [T-44](#t-44)（iOS の外枠をネイティブへ。前提だった画面定義書 13 は 2026-08-31 に確定済み）。

## 一覧

| ID | タイトル | 種別 | 優先度 | 状態 | 詳細 |
|---|---|---|---|---|---|
| T-11 | GMD のシステムイメージと emulator の CI キャッシュ | ツール整備 | 様子見 | 未着手 | [詳細](#t-11) |
| T-12 | GitHub Actions の SHA 固定 | ツール整備 | 低 | 未着手 | [詳細](#t-12) |
| T-17 | RoomMigrationTest のスキーマ検証 | テスト | 低 | 未着手 | [詳細](#t-17) |
| T-21 | インストルメンテーションテストの DB を in-memory にする | テスト | 中 | 未着手 | [詳細](#t-21) |
| T-25 | 日時の DB 越しの往復と旧 DB 起動時の挙動のテスト | テスト | 中 | 未着手 | [詳細](#t-25) |
| T-30 | ブランチ保護で CI を必須チェックにする | ツール整備 | 中 | 未着手 | [詳細](#t-30) |
| T-33 | convention plugin をモジュールの原型で切り直す | ツール整備 | 低 | 未着手 | [詳細](#t-33) |
| T-34 | RoomMigrationTest を本番と同じ driver 経路に揃える | テスト | 中 | 未着手 | [詳細](#t-34) |
| T-36 | 使われていない画面文言 12 件の去就 | 調査 | 低 | 未着手 | [詳細](#t-36) |
| T-37 | strings.xml の書式指定を機械で縛る | ツール整備 | 低 | 未着手 | [詳細](#t-37) |
| T-38 | 2 つのテキスト入力ダイアログを整理する | 内部設計 | 低 | 未着手 | [詳細](#t-38) |
| T-40 | 設定画面の文言 4 件を Compose Resources へ寄せる | 内部設計 | 低 | 未着手 | [詳細](#t-40) |
| T-41 | iOS の `is <data object>` 不一致を上流に報告する | 調査 | 中 | 未着手 | [詳細](#t-41) |
| T-43 | CI をワークフロー単位で分ける | ツール整備 | 低 | 未着手 | [詳細](#t-43) |
| T-44 | ③ 段 4 iOS の外枠をネイティブに置き換える | 内部設計 | 高 | 未着手 | [詳細](#t-44) |
| T-45 | 0 件検査が届かないところを塞ぐ | ツール整備 | 中 | 未着手 | [詳細](#t-45) |
| T-46 | iOS の実物の `.app` を起動して触る層を持つ | テスト | 低 | 未着手 | [詳細](#t-46) |
| T-47 | 対象と置き場所がずれている instrumented 2 件を移す | テスト | 低 | 未着手 | [詳細](#t-47) |
| T-49 | 外部キー制約が効いていることを見る | テスト | 低 | 未着手 | [詳細](#t-49) |
| T-50 | 実機ターゲット iosArm64 を CI で検査する | ツール整備 | 中 | 未着手 | [詳細](#t-50) |
| T-51 | MVP 投入前に破壊的マイグレーションを外す | 内部設計 | 高 | 未着手 | [詳細](#t-51) |
| T-52 | FakeDatabase をモジュール境界に合わせる | テスト | 低 | 未着手 | [詳細](#t-52) |

## iOS 関連の着手順

**④ の前にやる iOS 案件はもう無い**（2026-08-31 時点）。内訳は次のとおり。

- **完了**: [T-42](./closed_23_技術改善バックログ.md#t-42)（`iosTest` に画面の網）／[T-48](./closed_23_技術改善バックログ.md#t-48)（テストで DAO を差し替えられるように）／[T-35](./closed_23_技術改善バックログ.md#t-35)（Room と DI が iOS で動くことを見る）／[T-39](./closed_23_技術改善バックログ.md#t-39)（ライセンス一覧を iOS にも合わせる）
- **④ の後**: [T-44](#t-44)（段 4）。[T-50](#t-50)（実機ターゲットの CI 検査）はその前に
- **いつでも**: [T-41](#t-41)（上流への報告。ただし手元で再現しない）／[T-46](#t-46)（実物の `.app` を起動する層。T-42 の取りこぼしが実際に見えてから）

**網を先に置いた。** ④ で作る機能は `commonMain` に入るので**そのまま iOS にも載る**。網が無いまま積むと、iOS 固有の壊れ方が気づかれないまま溜まる。**「実装に入る前にやる」という約束は滑る**——T-35 の着手条件が実際に滑った。先に置けば約束ではなく事実になる。

**番号順ではない。** 付け直さないのは、`T-35` / `T-41` / `T-42` が**マージ済みコミットのメッセージから参照されている**ため——番号を動かすと、書き換えられない履歴が存在しない番号を指すことになる。各案件の `着手条件` にも相互リンクがあるので、詳細から辿っても同じ順序に行き着く。

## 詳細

### T-11

- 背景: `instrumented` ジョブは毎回 Android Emulator（約 300MB）と aosp-atd のシステムイメージ（約 500MB）をダウンロードしている。`actions/cache` でイメージのディレクトリだけを復元すると、GMD が emulator の導入をスキップして `pixel6Api35Setup` が失敗した（main の run 33232649012）
- 対応方針: キャッシュするなら `$ANDROID_HOME/emulator` と `system-images/android-35/aosp_atd/x86_64` の両方を、emulator のバージョンをキーに含めて復元する。効果（短縮時間）を計測してから採否を決める
- 着手条件: instrumented ジョブの所要時間が気になり始めたら。public 化後は実行時間が無制限なので急がない

### T-12

- 背景: `ci.yml` の Action は major タグ固定（`@v7` 等）。現状はトークンが `contents: read` でシークレット参照も無く、Dependabot が追随するので妥当な水準
- 対応方針: ⑤ で署名鍵を CI に持ち込む段階で、各 Action をコミット SHA に固定する（Dependabot はコメントのバージョン表記を頼りに SHA も更新できる）
- 着手条件: CI にシークレットを入れるとき

### T-17

- 背景: `RoomMigrationTest` は assert が 0 件・`ALL_MIGRATIONS` が空・`runMigrationsAndValidate` 未使用で、実質「現行バージョンの DB が開ける」ことだけを見る煙テスト。T-24 で `version = 2` に上げたが Migration を書かなかったため、検証対象の遷移がまだ 1 本も無い
- 対応方針: `runMigrationsAndValidate` へ切り替え、`ALL_MIGRATIONS` に追加した `Migration` を登録する
- 着手条件: 最初の `Migration` を書くとき（＝公開後にスキーマを変えるとき。CLAUDE.md「スキーマ変更時の手順」と同時に行う）
- 関連: T-13 のレビューで test-reviewer が指摘

### T-21

- 背景: instrumented テストは本物の `ReBuyApplication`（Koin を起動する）の上で走り、DB を差し替えていないため、端末の本番 DB を読み書きする。使い捨てのエミュレータ（GMD）では実害が薄いが、実機で `connectedAndroidTest` を回すとオーナーの実データに依存し、要求定義書 §11 の姿勢とも噛み合わない。DB を用意できないことが理由で「買い物を終わるとホームへ戻る」「空状態のボタンからアイテム一覧へ遷移する」の 2 経路が自動化できず手動確認に残っている
- 対応方針: `loadKoinModules` で `AppDatabase` の定義を in-memory のものに差し替え、DAO へ直接シードしてから画面を操作する形にする。テスト側で `startKoin` を呼ぶと本物の `Application` と衝突するので、差し替えは `loadKoinModules` / `unloadKoinModules` で行う。そのうえで上記 2 経路を自動化する。あわせて `ViewModelScopeTest` の逆向き（entry が backstack に残っている間は ViewModel が保持されること）も書く——買い物画面の終了確認ダイアログを開くのにチェック済みの品目が要るため、シードできるようになって初めて書ける
- **追記（2026-08-31、T-48 の実測から）: 差し替えの機構は確かめたが、Android では順序が問題になる。**
  - **機構は使える。** `startKoin { allowOverride(false) }` は**起動時の `modules()` にしか効かず**、起動後の `loadModules` は既定で `allowOverride = true`（Koin 4.1.0 のソースで確認）。本番の方針を緩めずに差し替えられる
  - **ただし上書きは「これから作るもの」にしか効かない。** `AppDatabase` → DAO → Repository はすべて `single` なので、1 度でも解決された後に差し替えても Repository は古い DAO を掴んだままになる（iOS で実測）
  - **Android は iOS より条件が厳しい。** iOS はテスト自身が Koin を起動するので解決前に差し込めたが、Android は本番の `ReBuyApplication` が**プロセス起動時**に Koin を立て、`createAndroidComposeRule<MainActivity>()` が `ActivityScenarioRule` で **Activity を起動してから**テスト本体へ入る。**テスト本体で `loadKoinModules` を呼ぶ頃には鎖が実体化している可能性が高い**
  - **ここは未実測。** ルールが `ActivityScenarioRule` を使うことはソースで確認したが、composition がどこまで進むかは測っていない。**着手時に最初にこれを測る**
  - 回避策の候補は 2 つで、どちらも代償がある。(a) `createComposeRule()` にして自分で `setContent { ReBuyApp() }` する——差し替えは効くが「**実物の `MainActivity` を起動している**」という価値が消える（[T-47](#t-47) が「移さない理由」に挙げたのと同じもの）。(b) テスト用の `Application` を用意する——起動前に差し込めるが `testInstrumentationRunner` まわりの仕掛けが増える
- 優先度の根拠: 実機でテストを回した瞬間に実データを触る。手動確認に残っている経路も減らせる
- 補足: `RoomMigrationTest` と `KoinGraphTest` は検証対象が `:shared:data` なのに `:androidApp` の androidTest にある（GMD を 1 本に保つための判断。段 2 の計画を参照）。そのため schemas の assets 指定がモジュール境界をまたぐ。**置き場所を動かすかは [T-47](#t-47) が持つ**——この案件と同時に決める
- 関連: T-18 のレビューで test-reviewer が指摘

### T-25

- 背景: T-24 の日時テストはコンバータ単体（純粋関数）で止まっており、DB を越える経路が未検証。押さえられていないのは 3 点。(a) 列の affinity が `INTEGER` であること——`2.json` はビルドのたびに生成し直されるので退行検知にならない、(b) `lastBoughtAt = null` の往復——Room が生成する null 分岐はコンバータの非 null シグネチャでは表現できず、`getString` → `getLong` へ経路が変わった箇所、(c) 旧 v1 の DB を持つ端末が起動時に落ちること——T-24 でオーナーが決めた挙動なのに、誰かが `fallbackToDestructiveMigration()` を足しても全件緑のまま通る
- 対応方針: (a)(b) は in-memory DB で DAO の往復を見る。(c) は `AppDatabase.getDatabase` が DB 名を固定して `INSTANCE` をキャッシュしているため、実データを触らずに production のビルダー設定を通せる形（テスト用に DB 名を差せる口）が要る
- **追加（2026-08-30、T-31 ステップ 8）**: (d) **旧経路で作った DB を新経路で開けること**。本番の DB オープンは「framework SQLite ＋ 相対名」から「`BundledSQLiteDriver` ＋ 絶対パス」へ変わったが、これを通るテストが 1 本も無い。手動のアップグレードインストールでしか確かめていない。データ消失という最悪の失敗モードに対して網が無い
- 着手条件: T-21 で in-memory DB への差し替えを入れるとき。同じ足場を使う
- 優先度の根拠: (c) はデータが黙って消える方向への退行を素通しする。ただし ⑤ までは守るべき実データが無いので急がない
- 関連: T-24 のレビューで test-reviewer が指摘

### T-30

- 背景: `main` にブランチ保護が無く、必須チェックも設定していない。そのため CI が pending のままでもマージできる。実際に T-29 で `gh pr merge --auto` を使ったところ、必須チェックが未設定のリポジトリでは auto-merge が成立せず即マージにフォールバックし、`build` と `instrumented` が走り切る前にマージされた（結果は緑だったが、それは結果論）。いまは「オーナーの合図」と「AI が CI を見る」という運用だけが守っている
- 対応方針: `main` にブランチ保護を設定し、`docs` / `build` / `instrumented` / `ios` を必須チェックにする。これで `gh pr merge --auto` が意図どおり働き、pending のままマージすることが構造的にできなくなる。**直コミットを塞ぐかは別問題**——[git 運用定義書](../仕様/16_git運用定義書.md) は docs と軽微な変更の `main` 直コミットを認めているので、その運用を壊さない設定にする（`Require a pull request before merging` は入れない、または管理者を除外する）
- 優先度の根拠: 機械で守れることを運用で守っている。一人開発なので事故の頻度は低いが、`--auto` の挙動のように「守っているつもり」になる形が最も危ない
- 関連: T-29 のマージで実際に踏んだ。[git 運用定義書](../仕様/16_git運用定義書.md) §1.1 のリスク別ルーティング

### T-33

- 背景: convention plugin が `rebuy.android.base`（application / library / KMP の 3 系統を `withId` で振り分ける）と `rebuy.kmp.ios`（iOS ターゲット）の 2 つに分かれている。前者は自分のファイル冒頭で「各モジュールの **Android** の土台」と宣言しているのに `:shared:*` の KMP 設定も抱えており、名前と役割がずれている。モジュール側は 2 つの id を書く必要があり、書き忘れの余地も残る
- 対応方針: 分ける軸を「プラットフォーム」から「**モジュールの原型**」に変える。`rebuy.kmp.library`（KMP の android ターゲット設定 ＋ iOS ターゲット）と `rebuy.android.app` の 2 つにすると、モジュール側の id が 1 つになり、名前と役割も一致する
- 着手条件: 段 3 が終わって `:shared:*` の形が固まってから。段 3 の途中は source set の付け外しが続くので、土台を動かすと切り分けが難しくなる
- 優先度の根拠: いまの形でも動く。読みやすさと書き忘れの余地の話なので急がない
- 関連: T-31 のステップ 6 のレビューで code-quality-reviewer が提案

### T-34

- 背景: 本番の DB オープンは T-31 ステップ 8 で `BundledSQLiteDriver` ＋ 絶対パスになったが、`RoomMigrationTest` は旧オーバーロード `Room.databaseBuilder(context, klass, name)` ＋ `openHelper` のままで、**framework SQLite の経路を開いている**。緑なのは「壊れなかった」からではなく「本番と違うものを見ている」から。⑤ で `Migration` を書き始めたとき、本番と違う経路で検証したことになる
- 対応方針: `MigrationTestHelper` の driver ベースのコンストラクタへ寄せ、`openHelper.writableDatabase.close()` を `db.close()` にする
- 着手条件: 最初の `Migration` を書くとき（T-17 と同時）。それまでは検証対象の遷移が 1 本も無い
- 優先度の根拠: いま緑なので急がないが、緑の意味が本来と違う
- 関連: T-31 のステップ 8 のレビューで test-reviewer と code-quality-reviewer が指摘

### T-36

- 背景: `shared/ui/src/commonMain/composeResources/values/strings.xml` の 49 件のうち 12 件がどこからも参照されていない。買い物の中断確認 4 件・ホームのボトムシート 4 件・設定の行 2 件・空状態の副文 2 件で、いずれも作られなかった画面の名残。**T-31 のステップ 11 で Compose Resources へ移したことで、Android lint の `UnusedResources` の視界からも外れた**——生成される `allStringResources` が全件を参照するので、今後増えても機械では気づけない
- 対応方針: ④ の機能見直しで「その画面を作るのか」を決め、作らないものは消す。あわせて未使用を機械で見つける手立てを置くかを決める
- 着手条件: ④ の要件定義のとき。消す・残すは要求の判断なので、それより前には決められない
- 関連: T-31 のステップ 11

### T-37

- 背景: Compose Resources の書式化は `String.format` ではなく `%(\d+)\$[ds]` の正規表現置換で、**解釈できない書式（`%s`・`%.2f`・`%%`）は例外を出さずに生のまま画面へ出る**。いま `strings.xml` にあるのは `%1$s` 3 件だけで、うち 3 件とも `StringResourceFormatTest` が期待値リテラルで固定している。**新しく足した文言は誰も見ていない**
- 対応方針: `strings.xml` に対する静的検査（`scripts/docs-check.sh` と同じ形の 1 スクリプト）で、`%` が `%N$s` / `%N$d` 以外の形で現れることを禁じる。文言を足すたびに固定テストを増やすより広く効き、維持費がほぼゼロ
- 着手条件: **当初はステップ 16 と同時のつもりだったが、そこでは入れなかった**（CI の 4 ジョブ化と T-32 で手一杯になった）。いつでも着手できる。検査は `scripts/` に置き、`docs` ジョブから呼ぶ形になる
- 関連: T-31 のステップ 11 のレビューで test-reviewer が提案

### T-38

- 背景: `TextFieldAddDialog` と `TextFieldEditDialog` は初期値・`onConfirm` の引数・ボタン文言 2 件しか違わない。加えて `TextFieldEditDialog` は `remember { mutableStateOf("") }` ＋ `SideEffect { inputString = editName }` という書き方をしている。**いまは壊れていない**——`SideEffect` は関数本体のスコープにあり、入力で再コンポーズされるのは `inputString` を読んでいる内側のラムダだけなので `SideEffect` は再実行されない（実機で入力できることを確認済み）。ただし composable の構造に依存した危うい形で、`remember(editName) { mutableStateOf(editName) }` なら構造に依らず正しい
- 対応方針: `SideEffect` を落として `remember(key)` にする。そのうえで 2 つのダイアログを 1 つに畳めるかを見る。どちらも表示と挙動は変えない
- 着手条件: T-31 のステップ 13 の後。ダイアログは `commonMain` へ移送済みだが、呼び出し元の画面はステップ 13 で移る。**入力→確定→`onConfirm` の値を見る instrumented テストが 1 本も無い**ので、直す前にそれを置く
- 関連: T-31 のステップ 12 のレビューで code-quality-reviewer が指摘

### T-40

- 背景: `SettingScreen` の 4 行（利用規約・プライバシーポリシー・ライセンス・お問い合わせ）と `LicenseScreen` のタイトルだけが `strings.xml` の外にある。**画面文言は Compose Resources に置く**という本書の規約から外れており、そのぶん「ライセンス」という文字列が instrumented テスト 3 本にコピーされている
- 対応方針: `license_title` ほか 4 件を `strings.xml` へ移し、テスト側の `private val licenseLabel` を `Res.string` 経由に揃える。表示は 1 文字も変えない
- 着手条件: 4 行のうち 3 行は遷移先が未実装の置き場（`{ }` の空ラムダ）。**その行を残すかどうかは ④ の機能見直しの判断**なので、1 件だけ先に移すと画面内で不揃いになる。④ で行の去就が決まってから 4 件まとめて移す
- 優先度の根拠: 表示は正しく、壊れてもいない。テスト側の重複 3 か所は注記付きで意図が読める状態にある
- 関連: T-31 のステップ 14 のレビューで code-quality-reviewer が指摘。[T-36](#t-36) と同じく ④ 待ち

### T-41

- 背景: iOS で `is <data object>` が、分岐に `@Composable` 呼び出しを含む `when` の中で一致しない。`tab is HomeTab.All` が `true` を返した直後に、同じ `tab` を見る `when` が `NoWhenBranchMatchedException` を投げる（Kotlin 2.4.10 / Compose Multiplatform 1.12.0 で実測）。**Android では起きない。** T-31 のステップ 15 で、等値の分岐に書き換えて回避した
- 対応方針: 最小再現を作り、Compose Multiplatform か Kotlin のどちらの issue かを切り分けて報告する。直ったら回避が不要になるが、**等値のほうが `data object` の判定として読みやすいので書き戻す必要は無い**
- **2026-08-31 追記: 3 か所すべてで再現しなかった**（T-42 で変異させて実測。経緯は log_23）。カゴタブに入ってボタンの文言を assert する検出器を置いたうえで `is` に戻しても緑のまま。**踏んだときと Kotlin も CMP も同じ版**なので、再現には別の条件が要る
- 着手条件: **iOS 関連の最後**。回避済みで実装が動かないので、手が空いたときでよい。**まず最小再現を作る**——手元で再現しない状態では報告を書けない。**画面の中の 3 か所は潰したので、次は画面の外**（`when` の形・分岐数・呼び出しの深さを変えた小さなサンプル）から当たる
- 優先度の根拠: 回避済みで実害は止まっているが、**同じ書き方をすると再発し、Android のテストでは捕まらない**。書き方の禁止事項として本書と実装計画の落とし穴 22 に残してあるだけなので、上流が直るまでは人の注意に頼っている
- 関連: T-31 のステップ 15 で判明

### T-43

- 背景: CI は `ci.yml` 1 本に `docs` / `build` / `instrumented` / `ios` の 4 ジョブを持つ。`ios` は 9 分と最も長く、macOS ランナーは枠が少ない。ワークフローを分ければ `paths:` で iOS を丸ごとスキップできる
- 対応方針: **いまは分けない。** 分割の主な利点は `paths:` フィルタだが、**直近 14 コミットのうち 13 が `shared/**` を触っており**、このリポジトリでは効かない。`shared/**` が本体である以上この比率は当面変わらない。必須チェックの個別指定はジョブ名でもできるので、ワークフローを分ける理由にならない
- 着手条件: 次のどちらかのとき。(a) [T-42](./closed_23_技術改善バックログ.md#t-42) で iOS の UI テストが入り、iOS 側が複数ジョブのパイプラインになったとき——そこで初めて Android の `build` / `instrumented` と対等な構造になる。(b) [T-30](#t-30) でブランチ保護を入れるとき——**必須チェックに指定したジョブが `paths:` で走らないと PR が永久にマージできなくなる**ので、「常に走って成功を報告するダミージョブ」の設計を同時に決める必要がある
- 優先度の根拠: いまの構成で困っていない。public リポジトリなので実行時間の制約も無い
- 関連: T-31 のステップ 16a でオーナーが提起。[T-30](#t-30) ／ [T-42](./closed_23_技術改善バックログ.md#t-42)

### T-44

- 背景: T-31（段 3）で iOS シミュレータに全画面が出るようになったが、**外枠は Compose 1 枚のまま**。③ の完了条件は「iOS シミュレータで全画面が動き、**外枠が SwiftUI のタブバー・ナビゲーションバーになっている**」なので、③ はまだ終わっていない。T-31 は段 3 だけを守備範囲にしていたため、閉じると段 4 が台帳から落ちる
- 対応方針: 実装計画を `plan_23_kmp段4実装計画.md` に起こしてから着手する。段 3 と同じく、Android の挙動を変えないことを制約に置く。**iOS 側の見た目の作り込み（Liquid Glass 前提の画面設計）は ④ の範囲**なので持ち込まない
- 着手条件: **④ の後**（2026-08-31 オーナー判断）。④ で画面構成が変わると外枠は作り直しになるので、構成が固まってから載せ替える。網（[T-42](./closed_23_技術改善バックログ.md#t-42)）は ④ の前に入っている前提。段 4 は iOS だけを触る変更なので **Android のテストが 1 件も網にならない**
- 優先度の根拠: ③ の残り。④（機能・UX 見直し）の前提
- 関連: [T-42](./closed_23_技術改善バックログ.md#t-42) ／ [T-35](./closed_23_技術改善バックログ.md#t-35) ／ [T-39](./closed_23_技術改善バックログ.md#t-39) ／ [T-41](#t-41)

### T-45

- 背景: T-32 の `failOnZeroTests()` は **KMP ライブラリプラグインを適用したモジュールにしか効かない**。届かないところが 3 つある。(a) **`:androidApp` の instrumented**——[テスト戦略定義書](../仕様/17_テスト戦略定義書.md) §3 で「instrumented にしか置けない」と宣言している守り（Room のマイグレーション・リソースが APK に載っているか・プロセス death からの復元・DI のグラフ）が全部ここに載っているのに、0 件になっても止まらない。(b) **ターゲットや source set を増やしたとき**——監視するタスク名と source set 名をハードコードしているので、`jvm()` や `iosX64` を足しても検査は追随せず、外れたことに気づく手段も無い。(c) **モジュールが `rebuy.android.base` を付け忘れた場合**——プラグインごと来ないので検査も来ない
- 対応方針: (a) は instrumented の結果 XML（`androidApp/build/outputs/androidTest-results/managedDevice/`）を見る検査を足す。**ラッパーの `<testsuites tests="N">` を二重に数えないこと**。(b) は監視対象を Kotlin の compilation モデルから引く（`allKotlinSourceSets`）。(c) はビルドロジック側では原理的に捕まらないので、ルートで「KMP モジュールなのにプラグインが付いていない」を見るか docs に寄せる
- 着手条件: いつでも。(a) が最も実害に近い
- 優先度の根拠: T-32 で「0 件で緑」を止めたつもりになっているが、**いちばん守りが集まっている instrumented には届いていない**。つもりになっている状態がいちばん危ない
- 関連: T-31 のステップ 16a のレビューで code-quality-reviewer と test-reviewer が独立に指摘。[T-32](./closed_23_技術改善バックログ.md)

### T-46

- 背景: [T-42](./closed_23_技術改善バックログ.md#t-42) で iOS の網を `runComposeUiTest` に決めたが、これは Compose のセマンティクスを**中から**叩くもので、**実物の `.app` を起動しない**。触れないものが 3 つ残る。(a) Swift の起動経路（`iOSApp.swift` → `setupKoin()` → `ReBuyViewController()`）——いまは CI の `xcodebuild build` がコンパイルまでしか見ていない。(b) `.app` へのリソース同梱——Android には APK について見る `LicenseLibrariesTest` があるが、iOS には対応するものが無い。(c) OS 側の挙動（キーボード・回転・バックグラウンド復帰）。(d) **本番の DB ファイルを実際に開くこと**——`DataModuleIosTest` は組み立てまでしか見ない。テストバイナリはアプリのサンドボックスで動かないので、`NSDocumentDirectory` を開く経路はここでしか通らない
- 対応方針: `iosApp/` に XCUITest ターゲットを足し、`xcodebuild test` で回す。**アクセシビリティ経由で触れることは T-42 で実測済み**——`testTag` が `AXIdentifier` に対応づく（`top_app_bar_title` など）ので、選択子は `TestTags` と同じものが使える。**文言では assert しない**——Swift から `Res.string.*` を引けないので、文言で書くと「文言を変えたらテストが古い値を主張する」状態になる
- 着手条件: **T-42 の網が入り、それでも取り逃していると分かったとき。** いま入れない理由は、同じ「外から動かす」枠なら Maestro（T-42 の (d)）のほうが安いこと——YAML 1 本が Android と iOS の両方で回り、Swift を書かなくて済む。**先に (d) を検討してから (b) を選ぶこと**
- 優先度の根拠: 取り逃す範囲は Swift の起動経路と同梱に限られ、どちらも壊れれば起動して即クラッシュするので気づける。網としての緊急度は低い
- 関連: [T-42](./closed_23_技術改善バックログ.md#t-42)（(b) はそこで比べた 3 案のひとつ）／ [T-43](#t-43)（CI のジョブ構成）

### T-47

- 背景: `RoomMigrationTest` と `KoinGraphTest` は検証対象が `:shared:data` なのに `androidApp/src/androidTest` にある。段 2 で GMD を 1 か所に保つために置いた妥協で、そのぶん schemas の assets 指定がモジュール境界をまたいでいる。KMP ライブラリプラグインには `withDeviceTest {}`（`androidDeviceTest` source set）があるので、いまなら対象と同じモジュールに置ける
- 対応方針: `:shared:data` で `withDeviceTest {}` を開け、2 件を `shared/data/src/androidDeviceTest` へ移す。詰めることが 3 つある。(a) **GMD の定義は `:androidApp` の `testOptions` に 1 つしか無い**ので `rebuy.android.base` へ移すか `:shared:data` にも置く、(b) **エミュレータの起動がモジュールの数だけ増える**ので CI の `instrumented` ジョブの所要時間を測ってから決める、(c) Koin はいま実物の `ReBuyApplication` が起動しているので、移した先ではテストが自分で `initKoin()` する
- **この案件は `:shared:data` に行くべきものだけを扱う。** 動機が「ずれの解消」なので、モジュール単位で区切ると案件が閉じる
  - **`:androidApp` に残すもの**: `NavigationTest` と `ViewModelScopeTest` は `createAndroidComposeRule<MainActivity>()` で**実物のアプリを起動している**ことが価値で、`:shared:ui` へ移すと `createComposeRule()` ＋ 自前の `setContent` になってその性質が消える。`LicenseLibrariesTest` は**アプリの APK に資産が載っているか**を見ており（③ 段 3 で踏んだ「Compose Resources が assets ごと消える」事故の網）、ライブラリのテスト APK を見る形に変わると同じ事故を止められない
  - **この案件では扱わないもの**: `NavigationStateRestorationTest` と `StringResourceFormatTest` は検証対象が `:shared:ui` なので、移すなら `:shared:ui` にも `withDeviceTest {}` を開けることになり、上の懸念 (b)（エミュレータの起動がモジュール数だけ増える）が 2 モジュール分になる。**移すか残すかは未判断**——とくに `StringResourceFormatTest` は「APK に載った文言が `\n` と `%1$s` を含めて期待どおり解釈されるか」を見ており、性質としては `LicenseLibrariesTest` の親戚で `:androidApp` に残す側かもしれない（**未確認の仮説**）
- 着手条件: [T-21](#t-21) と同時。あちらで in-memory DB への差し替えを入れるときに足場が同じになる
- 優先度の根拠: いま困っていない。**名前の一貫性を目的にしない**——動機は「検証対象と置き場所がずれている」ことだけで、`androidTest` を `androidDeviceTest` へ寄せること自体には価値を置かない
- 関連: [T-21](#t-21)（補足でこの判断を保留していた）／ [T-46](#t-46)（逆に、実物を起動する層は増やす方向）

### T-49

- 背景: `Item` は `Category` への外部キーを持ち、親の削除で `SET_NULL` になる。**この制約が実際に効いているかを見るテストが Android にも iOS にも無い。** `FakeDatabase` は挙動を真似ているだけで、本物の SQLite で `PRAGMA foreign_keys` が有効かは別問題——**接続の設定に依存する**ので、driver を変えた iOS ではとくに保証が無い
- 対応方針: `:shared:data` の `iosTest` の `AppDatabaseIosTest` に in-memory で 2 件。存在しない `categoryId` を持つ `Item` の insert が失敗すること、`Category` を消すと紐づく `Item` の `categoryId` が null になること。Android 側は instrumented（[T-21](#t-21) で in-memory を入れるときに同じ足場で）
- **行き先側は F-002 で入った**（`AppDatabaseIosTest` の「行き先を消しても品目は残る」が native の `SET_NULL` を見ている）。残っているのはカテゴリー側の 2 件と Android 側
- 着手条件: いつでも。iOS 側は `AppDatabaseIosTest` があるので今すぐ足せる
- 優先度の根拠: 壊れてもすぐには気づかず、**データの整合性が静かに崩れる**方向の失敗。ただし現状のアプリはカテゴリー削除をあまり踏まない
- 関連: [T-35](./closed_23_技術改善バックログ.md#t-35) のレビューで test-reviewer が指摘。[T-21](#t-21)

### T-50

- 背景: CI の `ios` ジョブは `iosSimulatorArm64Test` と、シミュレータ向けの `xcodebuild` しか回さない。**実機ターゲット `iosArm64` は誰も検査していない。** `shared/*/build.gradle.kts` の `add("kspIosArm64", ...)` を消しても CI は緑のまま（`rebuy.kmp.ios` のコメント自身が「書き忘れてもビルドは通り、そのターゲットだけリンク時に落ちる」と書いている穴）
- 対応方針: CI に実機向けのリンクを 1 本足す。テストではなくビルドで見るのが筋（実機は CI に無い）。`linkDebugFrameworkIosArm64` か、`xcodebuild -destination 'generic/platform=iOS'` のどちらが安いかを測ってから決める
- 着手条件: **[T-44](#t-44)（段 4）の前**。あそこで実機配布に向けた設定を触るので、その前に実機ターゲットが壊れていないことを見られるようにしておく
- 優先度の根拠: 実機ビルドは ⑤ のリリースで必ず通る道で、そこで初めて壊れているのが分かると原因の切り分けが遠い
- 関連: [T-35](./closed_23_技術改善バックログ.md#t-35) のレビューで test-reviewer が指摘。[T-44](#t-44)

### T-51

- 背景: ④ の実装中はスキーマ変更のたびに DB を入れ直すので、`DataModule.android.kt` と `DataModule.ios.kt` の両方に `fallbackToDestructiveMigration(dropAllTables = true)` が入っている（F-002 で追加）。**これを付けたまま MVP をオーナーの日常利用に入れると、次のスキーマ変更で買い物リストが黙って消える**
- 対応方針: 両プラットフォームから `fallbackToDestructiveMigration` を外し、以後は version ごとに明示的な `Migration` とそのテストを書く（[データモデル定義書](../仕様/14_データモデル定義書.md) §8）。**外した状態で旧 version の DB を開くテストを 1 本置く**——外しただけでは「消えない」ことを誰も見ていない
- あわせて `RoomMigrationTest` を見直す。いまは最新スキーマで作って開くだけで、**version 番号が `AppDatabase` と二重管理**になっている（追随を忘れると落ちる側なので安全ではあるが、Migration が戻ってくればこのテストの形自体が変わる）
- 着手条件: **MVP をオーナーが日常利用に入れる前**。④ の F がすべて closed になった時点で引く
- 優先度の根拠: 失敗の向きがデータ消失で、しかも起きたときには手元にバックアップが無い
- 関連: [T-25](#t-25)（旧 version の DB を持つ端末の起動）／ [T-17](#t-17)（スキーマ検証）／ [T-34](#t-34)（`RoomMigrationTest` の driver 経路）

### T-52

- 背景: `FakeDatabase` は DAO（`:shared:data`）の差し替えなのに `:shared:ui` の `commonTest` にある。ViewModel テストがそこにあるうちは自然だったが、F-002 で **`:shared:domain` の Repository を見るテスト（`DataModelRepositoryTest`）まで `:shared:ui` に置くことになった**。KMP ではテスト用ソースセットをモジュール間で共有する素直な手が無い（`java-test-fixtures` は使えない）ので、置き場所の歪みがそのまま出ている
- 対応方針: 案は 2 つ。(a) `FakeDatabase` を `:shared:data` の**別モジュール**（`:shared:data-testing` のような production source set）へ出し、各モジュールが `testImplementation` で引く。(b) 歪みを受け入れ、`:shared:ui` の `commonTest` を「共通のテスト置き場」と明示する。**(a) はモジュールが 1 つ増える**ので、テストの本数と越境の頻度が増えてから決める
- 着手条件: いつでも。ただし ④ の MVP 実装中は触らない（fake は 130 件超のテストの土台で、動かすと差分の切り分けが難しくなる）
- 優先度の根拠: いま困っているのは読み手の混乱だけで、`DataModelRepositoryTest` の KDoc が理由を明示している
- 関連: F-002 のレビューで code-quality-reviewer と test-reviewer の両方が指摘

